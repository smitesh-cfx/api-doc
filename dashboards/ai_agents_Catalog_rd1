{
  "name": "ai_agents_Catalog_rd1",
  "label": "Fabrix AI Agent Catalog",
  "description": "Fabrix AI Agent Catalog Storyboard",
  "enabled": true,
  "dashboard_type": "template",
  "dashboard_folder": "Default",
  "live_edit": true,
  "dashboard_style": "auto",
  "default_storyboard": "AI Agent Catalog",
  "dashboard_sections": [
    {
      "title": "LLMs",
      "show_filter": true,
      "widgets": [
        {
          "title": "Agent Catalog2",
          "widget_type": "persistent_custom_widget",
          "widget_implementation": "ai_agents_Catalog_rd1/agent_catalog_minio_with_proj",
          "min_width": 12,
          "max_width": 12,
          "height": 10,
          "widget_id": "agent_catalog_minio_with_proj"
        }
      ]
    }
  ],
  "custom_widgets": {
    "agent_catalog_minio_with_proj": {
      "artifacts": {
        "main": {
          "attachment": "agent_catalog_minio_with_proj.html",
          "content_type": "text/html",
          "is_template": true
        }
      }
    }
  },
  "use_storyboard": false,
  "menu_folder_path": [
    "Top Level"
  ],
  "attachments": [
    {
      "name": "agent_catalog_minio_with_proj.html",
      "value": "<!DOCTYPE html>\r\n<html lang=\"en\" data-theme=\"dark\"><head><meta http-equiv=\"Content-Type\" content=\"text/html; charset=UTF-8\">\r\n\r\n<meta name=\"viewport\" content=\"width=device-width,initial-scale=1\">\r\n<title>Agent Marketplace</title>\r\n<style>\r\n  :root{\r\n    /* Material Design 2 Color System - Dark Theme */\r\n    --primary: #1976D2; /* colors.blue.darken1 */\r\n    --secondary: #FF9800; /* colors.orange.darken1 */\r\n    --error: #F44336;\r\n    --warning: #FF5722;\r\n    --info: #03a9f4;\r\n    --success: #4CAF50;\r\n    --cfx3rdlevel: #313946;\r\n    --cfxcard: #282F3A;\r\n    --cfxbackground: #161B21;\r\n    --cfxreportheader: #1565C0; /* colors.blue.darken4 */\r\n    --cfxtabtext: #E0E0E0; /* colors.grey.lighten2 */\r\n    --cfxtext: #FAFAFA; /* colors.grey.lighten4 */\r\n    --cfxgrey: #282F3A;\r\n    --cfxlinkhover: #0D47A1; /* colors.blue.darken3 */\r\n    \r\n    /* Derived colors */\r\n    --bg: var(--cfxbackground);\r\n    --bg-secondary: var(--cfxcard);\r\n    --card: var(--cfxcard);\r\n    --text: var(--cfxtext);\r\n    --text-secondary: var(--cfxtabtext);\r\n    --text-muted: #9E9E9E;\r\n    --border: var(--cfx3rdlevel);\r\n    --accent: var(--primary);\r\n    --accent-hover: var(--cfxlinkhover);\r\n    --accent-light: rgba(144,202,249,.12);\r\n    --accent-border: rgba(144,202,249,.24);\r\n    --green: var(--success);\r\n    --red: var(--error);\r\n    --shadow: rgba(0,0,0,.3);\r\n    \r\n    /* Material Design 2 Elevation */\r\n    --elevation-0: 0px 0px 0px 0px rgba(0,0,0,0);\r\n    --elevation-1: 0px 2px 1px -1px rgba(0,0,0,0.2), 0px 1px 1px 0px rgba(0,0,0,0.14), 0px 1px 3px 0px rgba(0,0,0,0.12);\r\n    --elevation-2: 0px 3px 1px -2px rgba(0,0,0,0.2), 0px 2px 2px 0px rgba(0,0,0,0.14), 0px 1px 5px 0px rgba(0,0,0,0.12);\r\n    --elevation-3: 0px 3px 3px -2px rgba(0,0,0,0.2), 0px 3px 4px 0px rgba(0,0,0,0.14), 0px 1px 8px 0px rgba(0,0,0,0.12);\r\n    --elevation-4: 0px 2px 4px -1px rgba(0,0,0,0.2), 0px 4px 5px 0px rgba(0,0,0,0.14), 0px 1px 10px 0px rgba(0,0,0,0.12);\r\n    --elevation-6: 0px 3px 5px -1px rgba(0,0,0,0.2), 0px 6px 10px 0px rgba(0,0,0,0.14), 0px 1px 18px 0px rgba(0,0,0,0.12);\r\n    --elevation-8: 0px 5px 5px -3px rgba(0,0,0,0.2), 0px 8px 10px 1px rgba(0,0,0,0.14), 0px 3px 14px 2px rgba(0,0,0,0.12);\r\n    --elevation-12: 0px 7px 8px -4px rgba(0,0,0,0.2), 0px 12px 17px 2px rgba(0,0,0,0.14), 0px 5px 22px 4px rgba(0,0,0,0.12);\r\n    --elevation-16: 0px 8px 10px -5px rgba(0,0,0,0.2), 0px 16px 24px 2px rgba(0,0,0,0.14), 0px 6px 30px 5px rgba(0,0,0,0.12);\r\n    --elevation-24: 0px 11px 15px -7px rgba(0,0,0,0.2), 0px 24px 38px 3px rgba(0,0,0,0.14), 0px 9px 46px 8px rgba(0,0,0,0.12);\r\n  }\r\n\r\n  [data-theme=\"light\"]{\r\n    /* Material Design 2 Color System - Light Theme */\r\n    --primary: #1976D2; /* colors.blue.darken1 */\r\n    --secondary: #FF9800; /* colors.orange.base */\r\n    --error: #D32F2F; /* colors.red.darken1 */\r\n    --warning: #FF5722;\r\n    --info: #03a9f4;\r\n    --success: #4CAF50;\r\n    --cfx3rdlevel: #F7F7F7;\r\n    --cfxcard: #FFFFFF;\r\n    --cfxbackground: #f9f9f9;\r\n    --cfxreportheader: #E3F2FD; /* colors.blue.lighten5 */\r\n    --cfxtabtext: #424242; /* colors.grey.darken1 */\r\n    --cfxtext: #212121; /* colors.grey.darken4 */\r\n    --cfxgrey: #757575; /* colors.grey.base */\r\n    --cfxlinkhover: #0D47A1; /* colors.blue.darken3 */\r\n    \r\n    /* Derived colors */\r\n    --bg: var(--cfxbackground);\r\n    --bg-secondary: var(--cfxcard);\r\n    --card: var(--cfxcard);\r\n    --text: var(--cfxtext);\r\n    --text-secondary: var(--cfxtabtext);\r\n    --text-muted: var(--cfxgrey);\r\n    --border: #E0E0E0;\r\n    --accent: var(--primary);\r\n    --accent-hover: var(--cfxlinkhover);\r\n    --accent-light: rgba(25,118,210,.08);\r\n    --accent-border: rgba(25,118,210,.24);\r\n    --green: var(--success);\r\n    --red: var(--error);\r\n    --shadow: rgba(0,0,0,.12);\r\n    \r\n    /* Light theme elevation adjustments */\r\n    --elevation-0: 0px 0px 0px 0px rgba(0,0,0,0);\r\n    --elevation-1: 0px 2px 1px -1px rgba(0,0,0,0.2), 0px 1px 1px 0px rgba(0,0,0,0.14), 0px 1px 3px 0px rgba(0,0,0,0.12);\r\n    --elevation-2: 0px 3px 1px -2px rgba(0,0,0,0.2), 0px 2px 2px 0px rgba(0,0,0,0.14), 0px 1px 5px 0px rgba(0,0,0,0.12);\r\n    --elevation-3: 0px 3px 3px -2px rgba(0,0,0,0.2), 0px 3px 4px 0px rgba(0,0,0,0.14), 0px 1px 8px 0px rgba(0,0,0,0.12);\r\n    --elevation-4: 0px 2px 4px -1px rgba(0,0,0,0.2), 0px 4px 5px 0px rgba(0,0,0,0.14), 0px 1px 10px 0px rgba(0,0,0,0.12);\r\n    --elevation-6: 0px 3px 5px -1px rgba(0,0,0,0.2), 0px 6px 10px 0px rgba(0,0,0,0.14), 0px 1px 18px 0px rgba(0,0,0,0.12);\r\n    --elevation-8: 0px 5px 5px -3px rgba(0,0,0,0.2), 0px 8px 10px 1px rgba(0,0,0,0.14), 0px 3px 14px 2px rgba(0,0,0,0.12);\r\n    --elevation-12: 0px 7px 8px -4px rgba(0,0,0,0.2), 0px 12px 17px 2px rgba(0,0,0,0.14), 0px 5px 22px 4px rgba(0,0,0,0.12);\r\n    --elevation-16: 0px 8px 10px -5px rgba(0,0,0,0.2), 0px 16px 24px 2px rgba(0,0,0,0.14), 0px 6px 30px 5px rgba(0,0,0,0.12);\r\n    --elevation-24: 0px 11px 15px -7px rgba(0,0,0,0.2), 0px 24px 38px 3px rgba(0,0,0,0.14), 0px 9px 46px 8px rgba(0,0,0,0.12);\r\n  }\r\n\r\n  *{box-sizing:border-box;margin:0;padding:0}\r\n  html,body{height:100%;overflow:hidden;width:100%}\r\n  body{\r\n    background:var(--bg);\r\n    color:var(--text);\r\n    font-family: \"Avenir\", \"Avenir Next\", -apple-system, BlinkMacSystemFont, \"Segoe UI\", Roboto, \"Helvetica Neue\", Arial, sans-serif;\r\n    font-size: 14px;\r\n    line-height: 1.5;\r\n    font-weight: 400;\r\n    letter-spacing: 0.00938em;\r\n    -webkit-font-smoothing:antialiased;\r\n    -moz-osx-font-smoothing:grayscale;\r\n    transition:background .3s ease, color .3s ease;\r\n  }\r\n\r\n  /* Material Design 2 Typography Scale */\r\n  .text-h1 { font-size: 6rem; font-weight: 300; line-height: 1.167; letter-spacing: -0.01562em; }\r\n  .text-h2 { font-size: 3.75rem; font-weight: 300; line-height: 1.2; letter-spacing: -0.00833em; }\r\n  .text-h3 { font-size: 3rem; font-weight: 400; line-height: 1.167; letter-spacing: 0em; }\r\n  .text-h4 { font-size: 2.125rem; font-weight: 400; line-height: 1.235; letter-spacing: 0.00735em; }\r\n  .text-h5 { font-size: 1.5rem; font-weight: 400; line-height: 1.334; letter-spacing: 0em; }\r\n  .text-h6 { font-size: 1.25rem; font-weight: 500; line-height: 1.6; letter-spacing: 0.0075em; }\r\n  .text-subtitle1 { font-size: 1rem; font-weight: 400; line-height: 1.75; letter-spacing: 0.00938em; }\r\n  .text-subtitle2 { font-size: 0.875rem; font-weight: 500; line-height: 1.57; letter-spacing: 0.00714em; }\r\n  .text-body1 { font-size: 1rem; font-weight: 400; line-height: 1.5; letter-spacing: 0.00938em; }\r\n  .text-body2 { font-size: 0.875rem; font-weight: 400; line-height: 1.43; letter-spacing: 0.01071em; }\r\n  .text-button { font-size: 0.875rem; font-weight: 500; line-height: 1.75; letter-spacing: 0.02857em; text-transform: uppercase; }\r\n  .text-caption { font-size: 0.75rem; font-weight: 400; line-height: 1.66; letter-spacing: 0.03333em; }\r\n  .text-overline { font-size: 0.75rem; font-weight: 400; line-height: 2.66; letter-spacing: 0.08333em; text-transform: uppercase; }\r\n\r\n  .container{\r\n    display:flex;\r\n    height:100vh;\r\n    position:relative;\r\n    min-height:100vh;\r\n    flex-direction:column;\r\n  }\r\n\r\n  .main-content{\r\n    flex:1;\r\n    display:flex;\r\n    flex-direction:column;\r\n    overflow:hidden;\r\n    transition:margin-right .3s cubic-bezier(0.4, 0, 0.2, 1);\r\n    min-width:0;\r\n    width:100%;\r\n  }\r\n\r\n  .main-content.pane-open{\r\n    margin-right:520px;\r\n  }\r\n\r\n  .top-bar{\r\n    padding: 0 16px;\r\n    background: var(--card);\r\n    color: var(--text);\r\n    display: flex;\r\n    gap: 16px;\r\n    align-items: center;\r\n    box-shadow: var(--elevation-4);\r\n    position: relative;\r\n    z-index: 50;\r\n    flex-wrap: wrap;\r\n    min-height: 64px;\r\n    height: 64px;\r\n  }\r\n\r\n  .search-wrapper{\r\n    position:relative;\r\n    flex:1;\r\n    max-width:600px;\r\n    min-width:200px;\r\n  }\r\n\r\n  .search-icon{\r\n    position:absolute;\r\n    left:16px;\r\n    top:50%;\r\n    transform:translateY(-50%);\r\n    color:var(--text-muted);\r\n    pointer-events:none;\r\n  }\r\n\r\n  .search-input{\r\n    width:100%;\r\n    padding: 8px 16px 8px 48px;\r\n    background: var(--cfx3rdlevel);\r\n    border: 1px solid var(--border);\r\n    border-radius: 4px;\r\n    color: var(--text);\r\n    font-size: 16px;\r\n    font-family: inherit;\r\n    outline: none;\r\n    transition: all 0.2s cubic-bezier(0.4, 0, 0.2, 1);\r\n  }\r\n\r\n  .search-input::placeholder{\r\n    color: var(--text-muted);\r\n  }\r\n\r\n  .search-input:focus{\r\n    background: var(--cfx3rdlevel);\r\n    border-color: var(--primary);\r\n    box-shadow: 0 0 0 2px rgba(25,118,210,0.2);\r\n  }\r\n\r\n  .top-bar-actions{\r\n    display:flex;\r\n    gap:12px;\r\n    align-items:center;\r\n    flex-wrap:wrap;\r\n    min-width:0;\r\n    margin-left:auto;\r\n    margin-right:48px;\r\n  }\r\n\r\n  .theme-toggle{\r\n    display: none;\r\n  }\r\n\r\n  .theme-toggle svg{\r\n    width:20px;\r\n    height:20px;\r\n  }\r\n\r\n  .sort-select{\r\n    padding: 8px 32px 8px 16px;\r\n    background: var(--cfx3rdlevel);\r\n    border: 1px solid var(--border);\r\n    border-radius: 4px;\r\n    color: var(--text);\r\n    font-size: 14px;\r\n    font-family: inherit;\r\n    outline: none;\r\n    cursor: pointer;\r\n    appearance: none;\r\n    transition: all 0.2s cubic-bezier(0.4, 0, 0.2, 1);\r\n    min-width: 180px;\r\n    background-image: url('data:image/svg+xml;utf8,<svg xmlns=\"http://www.w3.org/2000/svg\" width=\"16\" height=\"16\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"%239E9E9E\" stroke-width=\"2\"><path d=\"m6 9 6 6 6-6\"/></svg>');\r\n    background-repeat: no-repeat;\r\n    background-position: right 12px center;\r\n  }\r\n\r\n  .sort-select:hover, .sort-select:focus{\r\n    background: var(--cfx3rdlevel);\r\n    border-color: var(--primary);\r\n    box-shadow: 0 0 0 2px rgba(25,118,210,0.2);\r\n  }\r\n\r\n  .sort-select:focus{\r\n    outline: 2px solid var(--primary);\r\n    outline-offset: 2px;\r\n  }\r\n\r\n  .sort-select option{\r\n    background:var(--card);\r\n    color:var(--text);\r\n  }\r\n\r\n  .sections-container{\r\n    flex:1;\r\n    overflow-y:auto;\r\n    overflow-x:hidden;\r\n    padding:28px 32px;\r\n  }\r\n\r\n  .search-results-container{\r\n    display:none;\r\n  }\r\n\r\n  .search-results-container.active{\r\n    display:block;\r\n  }\r\n\r\n  .sections-container.search-active .category-section{\r\n    display:none;\r\n  }\r\n\r\n  .search-results-grid{\r\n    display:flex;\r\n    flex-direction:column;\r\n    gap:32px;\r\n  }\r\n\r\n  .search-category-group{\r\n    display:flex;\r\n    flex-direction:column;\r\n    gap:16px;\r\n  }\r\n\r\n  .search-category-header{\r\n    font-size:clamp(14px, 2.2vw, 16px);\r\n    font-weight:700;\r\n    color:var(--text);\r\n    padding:12px 0;\r\n    border-bottom:2px solid var(--accent);\r\n    letter-spacing:.3px;\r\n  }\r\n\r\n  .search-category-tiles{\r\n    display:grid;\r\n    grid-template-columns:repeat(auto-fill,minmax(340px,1fr));\r\n    gap:20px;\r\n  }\r\n\r\n  /* Flexbox fallback for search category tiles */\r\n  @supports not (display: grid) {\r\n    .search-category-tiles{\r\n      display:flex;\r\n      flex-wrap:wrap;\r\n      gap:20px;\r\n    }\r\n    .search-category-tiles .card{\r\n      flex:1 1 340px;\r\n      max-width:calc(50% - 10px);\r\n    }\r\n  }\r\n\r\n  .search-header{\r\n    margin-bottom:24px;\r\n    padding-bottom:16px;\r\n    border-bottom:1px solid var(--border);\r\n  }\r\n\r\n  .search-header h2{\r\n    font-size:clamp(16px, 2.5vw, 18px);\r\n    color:var(--text);\r\n    font-weight:700;\r\n  }\r\n\r\n  .search-header .count{\r\n    color:var(--text-secondary);\r\n    font-size:clamp(12px, 1.8vw, 14px);\r\n    margin-top:6px;\r\n  }\r\n\r\n  .category-section{\r\n    margin-bottom:28px;\r\n    overflow:visible;\r\n  }\r\n\r\n  .section-header{\r\n    display:flex;\r\n    align-items:center;\r\n    justify-content:space-between;\r\n    padding:16px 20px;\r\n    background:var(--card);\r\n    border:none;\r\n    border-radius:4px;\r\n    cursor:pointer;\r\n    transition:all 0.2s cubic-bezier(0.4, 0, 0.2, 1);\r\n    user-select:none;\r\n    margin-bottom:16px;\r\n    box-shadow:var(--elevation-2);\r\n    min-height:44px;\r\n  }\r\n\r\n  .section-header:hover{\r\n    background:var(--accent-light);\r\n    box-shadow:var(--elevation-4);\r\n  }\r\n\r\n  .section-header:focus{\r\n    outline: 2px solid var(--primary);\r\n    outline-offset: 2px;\r\n  }\r\n\r\n  .section-title{\r\n    display:flex;\r\n    align-items:center;\r\n    gap:12px;\r\n  }\r\n\r\n  .section-title h2{\r\n    font-size:clamp(14px, 2.2vw, 16px);\r\n    font-weight:700;\r\n    color:var(--text);\r\n  }\r\n\r\n  .section-count{\r\n    background:var(--accent-light);\r\n    padding:4px 10px;\r\n    border-radius:12px;\r\n    font-size:clamp(10px, 1.5vw, 12px);\r\n    font-weight:700;\r\n    color:var(--accent);\r\n    border:1px solid var(--accent-border);\r\n  }\r\n\r\n  .toggle-icon{\r\n    transition:transform .3s;\r\n    color:var(--text-secondary);\r\n    font-size:18px;\r\n  }\r\n\r\n  .section-header.collapsed .toggle-icon{\r\n    transform:rotate(-90deg);\r\n  }\r\n\r\n  .section-grid{\r\n    display:grid;\r\n    grid-template-columns:repeat(auto-fill,minmax(340px,1fr));\r\n    gap:20px;\r\n    max-height:2000px;\r\n    overflow:visible;\r\n    transition:max-height .3s ease, opacity .3s ease, visibility .3s ease;\r\n    opacity:1;\r\n    visibility:visible;\r\n    pointer-events:auto;\r\n  }\r\n\r\n  /* Flexbox fallback for older browsers */\r\n  @supports not (display: grid) {\r\n    .section-grid{\r\n      display:flex;\r\n      flex-wrap:wrap;\r\n      gap:20px;\r\n    }\r\n    .section-grid .card{\r\n      flex:1 1 340px;\r\n      max-width:calc(50% - 10px);\r\n    }\r\n  }\r\n\r\n  .section-grid.collapsed{\r\n    max-height:0 !important;\r\n    height:0 !important;\r\n    min-height:0 !important;\r\n    opacity:0;\r\n    overflow:hidden;\r\n    pointer-events:none;\r\n    visibility:hidden;\r\n    margin:0 !important;\r\n    padding:0 !important;\r\n    border:none;\r\n    gap:0;\r\n  }\r\n  \r\n  .section-grid.collapsed .card{\r\n    pointer-events:none !important;\r\n    cursor:default !important;\r\n    opacity:0;\r\n    height:0;\r\n    min-height:0;\r\n    margin:0;\r\n    padding:0;\r\n  }\r\n  \r\n  .category-section .section-grid.collapsed{\r\n    margin-bottom:0;\r\n  }\r\n\r\n  .card{\r\n    position:relative;\r\n    background:var(--card);\r\n    border:none;\r\n    border-radius:20px;\r\n    padding:16px;\r\n    transition:all 0.2s cubic-bezier(0.4, 0, 0.2, 1);\r\n    cursor:pointer;\r\n    overflow:visible;\r\n    box-shadow:var(--elevation-2);\r\n    min-height:200px;\r\n    display:flex;\r\n    flex-direction:column;\r\n    width:100%;\r\n    max-width:100%;\r\n  }\r\n  \r\n  .card:hover{\r\n    box-shadow:var(--elevation-8);\r\n    transform:translateY(-2px);\r\n  }\r\n\r\n  .card:active{\r\n    transform:translateY(0);\r\n    box-shadow:var(--elevation-4);\r\n  }\r\n\r\n  .card:focus{\r\n    outline: 2px solid var(--primary);\r\n    outline-offset: 2px;\r\n  }\r\n\r\n  .card.hidden{display:none}\r\n\r\n  .fav{\r\n    position:absolute;\r\n    top:8px;\r\n    right:8px;\r\n    background:var(--card);\r\n    border:none;\r\n    border-radius:50%;\r\n    padding:8px;\r\n    cursor:pointer;\r\n    color:var(--text-muted);\r\n    z-index:10;\r\n    transition:all 0.2s cubic-bezier(0.4, 0, 0.2, 1);\r\n    width:40px;\r\n    height:40px;\r\n    display:grid;\r\n    place-items:center;\r\n    box-shadow:var(--elevation-2);\r\n  }\r\n  \r\n  .fav:hover{\r\n    color:var(--text);\r\n    background:var(--accent-light);\r\n    box-shadow:var(--elevation-4);\r\n  }\r\n\r\n  .fav:focus{\r\n    outline: 2px solid var(--primary);\r\n    outline-offset: 2px;\r\n  }\r\n  \r\n  .fav svg{\r\n    width:20px;\r\n    height:20px;\r\n    fill:none;\r\n    stroke:currentColor;\r\n    stroke-width:2.5;\r\n    transition:all .2s;\r\n  }\r\n  \r\n  .fav.active{\r\n    color:var(--primary);\r\n    background:var(--accent-light);\r\n  }\r\n  \r\n  .fav.active svg{\r\n    fill:var(--primary);\r\n    stroke:var(--primary);\r\n  }\r\n\r\n  .head{\r\n    display:flex;\r\n    align-items:flex-start;\r\n    gap:12px;\r\n    margin-bottom:16px;\r\n    padding-right:40px;\r\n    flex-shrink:0;\r\n  }\r\n\r\n  .icon-wrap{\r\n    width:48px;\r\n    height:48px;\r\n    border-radius:4px;\r\n    display:grid;\r\n    place-items:center;\r\n    background:var(--accent-light);\r\n    flex-shrink:0;\r\n  }\r\n\r\n  .icon-wrap svg{\r\n    width:24px;\r\n    height:24px;\r\n    stroke:var(--accent);\r\n    stroke-width:2;\r\n    stroke-linecap:round;\r\n    stroke-linejoin:round;\r\n    fill:none;\r\n  }\r\n\r\n  .title-area{\r\n    min-width:0;\r\n    flex:1;\r\n    overflow:hidden;\r\n  }\r\n\r\n  .name{\r\n    font-size:clamp(14px, 2.2vw, 16px);\r\n    font-weight:600;\r\n    margin:0 0 6px;\r\n    color:var(--text);\r\n    line-height:1.4;\r\n    word-wrap:break-word;\r\n    overflow-wrap:break-word;\r\n  }\r\n\r\n  .badge{\r\n    display:inline-block;\r\n    font-size:12px;\r\n    padding:4px 8px;\r\n    border-radius:16px;\r\n    color:white;\r\n    background:#6200EE;\r\n    font-weight:500;\r\n    letter-spacing:0.4px;\r\n    text-transform:uppercase;\r\n  }\r\n\r\n  .meta{\r\n    display:grid;\r\n    grid-template-columns:repeat(2,1fr);\r\n    gap:12px;\r\n    margin-top:auto;\r\n    padding-top:16px;\r\n    border-top:1px solid var(--border);\r\n    font-size:clamp(10px, 1.6vw, 12px);\r\n    flex-shrink:0;\r\n    min-width:0;\r\n  }\r\n\r\n  /* Flexbox fallback for meta grid */\r\n  @supports not (display: grid) {\r\n    .meta{\r\n      display:flex;\r\n      flex-wrap:wrap;\r\n      gap:12px;\r\n    }\r\n    .meta .kv{\r\n      flex:1 1 calc(50% - 6px);\r\n      min-width:120px;\r\n    }\r\n    .meta .state-indicator{\r\n      flex:1 1 100%;\r\n    }\r\n  }\r\n\r\n  .kv{\r\n    display:flex;\r\n    flex-direction:column;\r\n    gap:4px;\r\n    min-width:0;\r\n  }\r\n\r\n  .kv.desc{ grid-column:1/-1 }\r\n\r\n  .k{\r\n    color:var(--text-muted);\r\n    font-size:clamp(8px, 1.2vw, 10px);\r\n    font-weight:600;\r\n    letter-spacing:.5px;\r\n    text-transform:uppercase;\r\n  }\r\n\r\n  .v{\r\n    color:var(--text);\r\n    font-size:clamp(11px, 1.8vw, 13px);\r\n    font-weight:500;\r\n  }\r\n\r\n  .v.desc-truncate{ overflow:hidden; text-overflow:ellipsis; white-space:nowrap }\r\n\r\n  .state-indicator{\r\n    grid-column:1/-1;\r\n    display:flex;\r\n    align-items:center;\r\n    gap:8px;\r\n    padding:8px 12px;\r\n    background:var(--cfx3rdlevel);\r\n    border-radius:4px;\r\n    font-size:12px;\r\n    font-weight:500;\r\n  }\r\n\r\n  .dot{\r\n    width:6px;\r\n    height:6px;\r\n    border-radius:50%;\r\n    background:var(--green);\r\n  }\r\n\r\n  .state-indicator.disabled{\r\n    color:var(--red);\r\n  }\r\n\r\n  .state-indicator.disabled .dot{\r\n    background:var(--red);\r\n  }\r\n\r\n  .state-indicator:not(.disabled){\r\n    color:var(--green);\r\n  }\r\n\r\n  .empty-section{\r\n    text-align:center;\r\n    padding:40px 20px;\r\n    color:var(--text-muted);\r\n    font-size:clamp(12px, 2vw, 14px);\r\n  }\r\n\r\n  .side-pane{\r\n    position:fixed;\r\n    top:0;\r\n    right:-520px;\r\n    width:520px;\r\n    max-width:100vw;\r\n    height:100vh;\r\n    background:var(--card);\r\n    border:none;\r\n    box-shadow:var(--elevation-16);\r\n    overflow-y:auto;\r\n    transition:right .3s cubic-bezier(0.4, 0, 0.2, 1);\r\n    z-index:100;\r\n    display:flex;\r\n    flex-direction:column;\r\n    min-width:0;\r\n  }\r\n\r\n  .side-pane.open{\r\n    right:0;\r\n  }\r\n\r\n  .pane-header{\r\n    position:sticky;\r\n    top:0;\r\n    background:var(--card);\r\n    padding:24px;\r\n    border-bottom:1px solid var(--border);\r\n    display:flex;\r\n    align-items:flex-start;\r\n    gap:16px;\r\n    z-index:10;\r\n    box-shadow:var(--elevation-2);\r\n    flex-shrink:0;\r\n    min-width:0;\r\n  }\r\n\r\n  .close-btn{\r\n    background:transparent;\r\n    border:none;\r\n    border-radius:50%;\r\n    color:var(--text-secondary);\r\n    cursor:pointer;\r\n    padding:8px;\r\n    transition:all 0.2s cubic-bezier(0.4, 0, 0.2, 1);\r\n    margin-left:auto;\r\n    width:40px;\r\n    height:40px;\r\n    display:grid;\r\n    place-items:center;\r\n  }\r\n\r\n  .close-btn:hover{\r\n    color:var(--text);\r\n    background:var(--accent-light);\r\n  }\r\n\r\n  .close-btn:focus{\r\n    outline: 2px solid var(--primary);\r\n    outline-offset: 2px;\r\n  }\r\n\r\n  .close-btn svg{\r\n    width:20px;\r\n    height:20px;\r\n  }\r\n\r\n  .pane-content{\r\n    padding:24px;\r\n    flex:1;\r\n    min-width:0;\r\n    overflow-y:auto;\r\n  }\r\n\r\n  .detail-section{\r\n    margin-bottom:24px;\r\n  }\r\n\r\n  .detail-title{\r\n    font-size:clamp(9px, 1.4vw, 11px);\r\n    font-weight:700;\r\n    letter-spacing:1px;\r\n    text-transform:uppercase;\r\n    color:var(--text-muted);\r\n    margin-bottom:12px;\r\n  }\r\n\r\n  .detail-value{\r\n    font-size:clamp(12px, 2vw, 14px);\r\n    color:var(--text-secondary);\r\n    line-height:1.6;\r\n  }\r\n\r\n  .detail-grid{\r\n    display:grid;\r\n    grid-template-columns:repeat(2,1fr);\r\n    gap:12px;\r\n    margin-bottom:24px;\r\n  }\r\n\r\n  /* Flexbox fallback for detail grid */\r\n  @supports not (display: grid) {\r\n    .detail-grid{\r\n      display:flex;\r\n      flex-wrap:wrap;\r\n      gap:12px;\r\n    }\r\n    .detail-grid .detail-item{\r\n      flex:1 1 calc(50% - 6px);\r\n      min-width:200px;\r\n    }\r\n  }\r\n\r\n  .detail-item{\r\n    background:var(--card);\r\n    padding:16px;\r\n    border-radius:4px;\r\n    border:none;\r\n    transition:all 0.2s cubic-bezier(0.4, 0, 0.2, 1);\r\n    box-shadow:var(--elevation-1);\r\n  }\r\n\r\n  .detail-item:hover{\r\n    box-shadow:var(--elevation-4);\r\n  }\r\n\r\n  .detail-item:focus{\r\n    outline: 2px solid var(--primary);\r\n    outline-offset: 2px;\r\n  }\r\n\r\n  .detail-item-label{\r\n    font-size:clamp(8px, 1.2vw, 10px);\r\n    font-weight:600;\r\n    letter-spacing:.5px;\r\n    text-transform:uppercase;\r\n    color:var(--text-muted);\r\n    margin-bottom:6px;\r\n  }\r\n\r\n  .detail-item-value{\r\n    font-size:clamp(12px, 2vw, 14px);\r\n    color:var(--text);\r\n    font-weight:600;\r\n  }\r\n\r\n  .pane-icon{\r\n    width:64px;\r\n    height:64px;\r\n    border-radius:4px;\r\n    display:grid;\r\n    place-items:center;\r\n    background:var(--accent-light);\r\n    flex-shrink:0;\r\n  }\r\n\r\n  .pane-icon svg{\r\n    width:32px;\r\n    height:32px;\r\n    stroke:var(--accent);\r\n    stroke-width:2;\r\n    stroke-linecap:round;\r\n    stroke-linejoin:round;\r\n    fill:none;\r\n  }\r\n\r\n  .pane-icon-container{\r\n    width: 64px;\r\n    height: 64px;\r\n    padding: 0;\r\n    overflow: hidden;\r\n  }\r\n  .pane-icon-img{\r\n    width: 100%;\r\n    height: 100%;\r\n    object-fit: contain;\r\n    object-position: center;\r\n    display: block;\r\n  }\r\n\r\n  .pane-title-area{\r\n    flex:1;\r\n    min-width:0;\r\n    overflow:hidden;\r\n  }\r\n\r\n  .pane-title-area h2{\r\n    font-size:clamp(16px, 2.8vw, 20px);\r\n    margin:0 0 10px;\r\n    line-height:1.3;\r\n    color:var(--text);\r\n    word-wrap:break-word;\r\n    overflow-wrap:break-word;\r\n  }\r\n\r\n  .pane-tags{\r\n    display:flex;\r\n    flex-wrap:wrap;\r\n    gap:6px;\r\n    margin-top:0;\r\n  }\r\n\r\n  .pane-tag{\r\n    display:inline-block;\r\n    font-size:12px;\r\n    padding:4px 8px;\r\n    border-radius:16px;\r\n    background:#6200EE;\r\n    color:white;\r\n    font-weight:500;\r\n    letter-spacing:0.4px;\r\n    text-transform:uppercase;\r\n  }\r\n\r\n  .action-buttons{\r\n    display:grid;\r\n    grid-template-columns:repeat(2,1fr);\r\n    gap:12px;\r\n    margin-bottom:32px;\r\n  }\r\n\r\n  /* Flexbox fallback for action buttons */\r\n  @supports not (display: grid) {\r\n    .action-buttons{\r\n      display:flex;\r\n      flex-wrap:wrap;\r\n      gap:12px;\r\n    }\r\n    .action-buttons .action-btn{\r\n      flex:1 1 calc(50% - 6px);\r\n      min-width:150px;\r\n    }\r\n  }\r\n\r\n  .action-btn{\r\n    padding:12px 24px;\r\n    background:var(--primary);\r\n    border:none;\r\n    border-radius:4px;\r\n    color:white;\r\n    font-size:14px;\r\n    font-weight:500;\r\n    cursor:pointer;\r\n    transition:all 0.2s cubic-bezier(0.4, 0, 0.2, 1);\r\n    text-align:center;\r\n    text-transform:uppercase;\r\n    letter-spacing:0.4px;\r\n    box-shadow:var(--elevation-2);\r\n    min-height:44px;\r\n  }\r\n\r\n  .action-btn:hover{\r\n    background:var(--cfxlinkhover);\r\n    box-shadow:var(--elevation-4);\r\n  }\r\n\r\n  .action-btn:active{\r\n    box-shadow:var(--elevation-1);\r\n  }\r\n\r\n  .action-btn:focus{\r\n    outline: 2px solid var(--primary);\r\n    outline-offset: 2px;\r\n  }\r\n\r\n  .tabs-wrapper{\r\n    margin-bottom:24px;\r\n  }\r\n\r\n  .tabs-nav-container{\r\n    position:relative;\r\n    margin-bottom:24px;\r\n  }\r\n\r\n  .tabs-nav{\r\n    display:flex;\r\n    gap:8px;\r\n    overflow:hidden;\r\n    padding-bottom:12px;\r\n    border-bottom:1px solid var(--border);\r\n    position:relative;\r\n  }\r\n\r\n  .tabs-scroll{\r\n    display:flex;\r\n    gap:8px;\r\n    transition:transform .3s ease;\r\n    flex-wrap:nowrap;\r\n    min-width:0;\r\n  }\r\n\r\n  .tab-btn{\r\n    padding:12px 16px;\r\n    background:transparent;\r\n    border:none;\r\n    color:var(--text-secondary);\r\n    font-size:14px;\r\n    font-weight:500;\r\n    cursor:pointer;\r\n    transition:all 0.2s cubic-bezier(0.4, 0, 0.2, 1);\r\n    white-space:nowrap;\r\n    text-transform:uppercase;\r\n    letter-spacing:0.4px;\r\n    border-radius:4px;\r\n    position:relative;\r\n    flex-shrink:0;\r\n    min-width:0;\r\n    min-height:44px;\r\n  }\r\n\r\n  .tab-btn:hover{\r\n    color:var(--text);\r\n    background:var(--accent-light);\r\n  }\r\n\r\n  .tab-btn:focus{\r\n    outline: 2px solid var(--primary);\r\n    outline-offset: 2px;\r\n  }\r\n\r\n  .tab-btn.active{\r\n    color:var(--primary);\r\n    background:var(--accent-light);\r\n  }\r\n\r\n  .tab-btn.active::after{\r\n    content:'';\r\n    position:absolute;\r\n    bottom:0;\r\n    left:0;\r\n    right:0;\r\n    height:2px;\r\n    background:var(--primary);\r\n  }\r\n\r\n  .tab-nav-btn{\r\n    position:absolute;\r\n    top:50%;\r\n    transform:translateY(-50%);\r\n    width:40px;\r\n    height:40px;\r\n    border-radius:50%;\r\n    background:var(--card);\r\n    border:none;\r\n    display:grid;\r\n    place-items:center;\r\n    cursor:pointer;\r\n    transition:all 0.2s cubic-bezier(0.4, 0, 0.2, 1);\r\n    z-index:5;\r\n    color:var(--text-secondary);\r\n    box-shadow:var(--elevation-2);\r\n  }\r\n\r\n  .tab-nav-btn:hover{\r\n    background:var(--accent-light);\r\n    color:var(--primary);\r\n    box-shadow:var(--elevation-4);\r\n  }\r\n\r\n  .tab-nav-btn:focus{\r\n    outline: 2px solid var(--primary);\r\n    outline-offset: 2px;\r\n  }\r\n\r\n  .tab-nav-btn.left{\r\n    left:-8px;\r\n  }\r\n\r\n  .tab-nav-btn.right{\r\n    right:-8px;\r\n  }\r\n\r\n  .tab-nav-btn.hidden{\r\n    display:none;\r\n  }\r\n\r\n  .tab-nav-btn svg{\r\n    width:16px;\r\n    height:16px;\r\n  }\r\n\r\n  .tab-content{\r\n    display:none;\r\n    animation:fadeIn .3s ease;\r\n  }\r\n\r\n  .tab-content.active{\r\n    display:block;\r\n  }\r\n\r\n  @keyframes fadeIn{\r\n    from{opacity:0;transform:translateY(8px)}\r\n    to{opacity:1;transform:translateY(0)}\r\n  }\r\n\r\n  .tab-section{\r\n    margin-bottom:24px;\r\n  }\r\n\r\n  .tab-section-title{\r\n    font-size:clamp(9px, 1.4vw, 11px);\r\n    font-weight:700;\r\n    letter-spacing:1px;\r\n    text-transform:uppercase;\r\n    color:var(--text-muted);\r\n    margin-bottom:12px;\r\n    padding-bottom:8px;\r\n    border-bottom:1px solid var(--border);\r\n  }\r\n\r\n  .tab-section-content{\r\n    font-size:clamp(12px, 2vw, 14px);\r\n    line-height:1.6;\r\n    color:var(--text-secondary);\r\n  }\r\n\r\n  .tab-section-content ul{\r\n    list-style:none;\r\n    padding:0;\r\n  }\r\n\r\n  .tab-section-content li{\r\n    padding:6px 0;\r\n    padding-left:18px;\r\n    position:relative;\r\n  }\r\n\r\n  .tab-section-content li:before{\r\n    content:'\u2022';\r\n    position:absolute;\r\n    left:0;\r\n    color:var(--accent);\r\n    font-weight:bold;\r\n  }\r\n\r\n  .contact-grid{\r\n    display:grid;\r\n    gap:12px;\r\n  }\r\n\r\n  .contact-item{\r\n    display:flex;\r\n    align-items:center;\r\n    gap:16px;\r\n    padding:16px;\r\n    background:var(--card);\r\n    border:none;\r\n    border-radius:4px;\r\n    transition:all 0.2s cubic-bezier(0.4, 0, 0.2, 1);\r\n    min-width:0;\r\n    box-shadow:var(--elevation-1);\r\n  }\r\n\r\n  .contact-item:hover{\r\n    box-shadow:var(--elevation-4);\r\n  }\r\n\r\n  .contact-item:focus{\r\n    outline: 2px solid var(--primary);\r\n    outline-offset: 2px;\r\n  }\r\n\r\n  .contact-icon{\r\n    width:40px;\r\n    height:40px;\r\n    display:grid;\r\n    place-items:center;\r\n    background:var(--accent-light);\r\n    border-radius:4px;\r\n    flex-shrink:0;\r\n  }\r\n\r\n  .contact-icon svg{\r\n    width:18px;\r\n    height:18px;\r\n    stroke:var(--accent);\r\n    fill:none;\r\n    stroke-width:2;\r\n  }\r\n\r\n  .contact-details{\r\n    flex:1;\r\n    min-width:0;\r\n    overflow:hidden;\r\n  }\r\n\r\n  .contact-label{\r\n    font-size:clamp(8px, 1.2vw, 10px);\r\n    color:var(--text-muted);\r\n    font-weight:600;\r\n    text-transform:uppercase;\r\n    letter-spacing:.5px;\r\n    margin-bottom:4px;\r\n  }\r\n\r\n  .contact-value{\r\n    font-size:clamp(11px, 1.8vw, 13px);\r\n    color:var(--text);\r\n    font-weight:500;\r\n    word-break:break-all;\r\n  }\r\n\r\n  .image-gallery{\r\n    display:grid;\r\n    grid-template-columns:repeat(auto-fill, minmax(150px, 1fr));\r\n    gap:12px;\r\n  }\r\n  \r\n  /* Fallback for older browsers */\r\n  @supports not (display: grid) {\r\n    .image-gallery{\r\n      display:flex;\r\n      flex-wrap:wrap;\r\n      gap:12px;\r\n    }\r\n    .image-gallery .gallery-item{\r\n      flex:1 1 150px;\r\n      min-width:150px;\r\n    }\r\n  }\r\n\r\n  .gallery-item{\r\n    width:100%;\r\n    background:var(--card);\r\n    border:none;\r\n    border-radius:8px;\r\n    overflow:hidden;\r\n    cursor:pointer;\r\n    transition:all 0.2s cubic-bezier(0.4, 0, 0.2, 1);\r\n    box-shadow:var(--elevation-2);\r\n    position:relative;\r\n    padding:8px;\r\n    aspect-ratio:16/9;\r\n    display:flex;\r\n    align-items:center;\r\n    justify-content:center;\r\n  }\r\n  \r\n  .gallery-item:hover{\r\n    box-shadow:var(--elevation-6);\r\n    transform:translateY(-2px);\r\n  }\r\n  \r\n  .gallery-item:focus{\r\n    outline: 2px solid var(--primary);\r\n    outline-offset: 2px;\r\n  }\r\n  \r\n  .gallery-item img{\r\n    width:100%;\r\n    height:100%;\r\n    display:block;\r\n    object-fit:cover;\r\n    background:var(--cfx3rdlevel);\r\n    border-radius:4px;\r\n    cursor:pointer;\r\n    transition:opacity 0.3s ease, filter 0.3s ease, transform 0.2s ease;\r\n  }\r\n  \r\n  .gallery-item img:hover{\r\n    transform:scale(1.02);\r\n  }\r\n  \r\n  .image-loading-spinner{\r\n    position:absolute;\r\n    top:50%;\r\n    left:50%;\r\n    transform:translate(-50%, -50%);\r\n    z-index:1;\r\n    color:var(--primary, #7dcfff);\r\n    animation:spin 1s linear infinite;\r\n    pointer-events:none;\r\n    display:block;\r\n  }\r\n  \r\n  .image-loading-spinner svg{\r\n    display:block;\r\n    width:32px;\r\n    height:32px;\r\n  }\r\n  \r\n  @keyframes spin{\r\n    from{transform:translate(-50%, -50%) rotate(0deg);}\r\n    to{transform:translate(-50%, -50%) rotate(360deg);}\r\n  }\r\n  \r\n  @keyframes rotate{\r\n    from{transform:rotate(0deg);}\r\n    to{transform:rotate(360deg);}\r\n  }\r\n\r\n  .gallery-item:hover{\r\n    box-shadow:var(--elevation-8);\r\n    transform:scale(1.02);\r\n  }\r\n\r\n  .gallery-item:focus{\r\n    outline: 2px solid var(--primary);\r\n    outline-offset: 2px;\r\n  }\r\n\r\n  .gallery-placeholder{\r\n    color:var(--text-muted);\r\n  }\r\n\r\n  .gallery-placeholder svg{\r\n    width:32px;\r\n    height:32px;\r\n    stroke:currentColor;\r\n    fill:none;\r\n    stroke-width:1.5;\r\n  }\r\n\r\n  .video-list{\r\n    display:flex;\r\n    flex-direction:column;\r\n    gap:12px;\r\n  }\r\n\r\n  .video-item{\r\n    display:flex;\r\n    gap:16px;\r\n    padding:16px;\r\n    background:var(--card);\r\n    border:none;\r\n    border-radius:4px;\r\n    cursor:pointer;\r\n    transition:all 0.2s cubic-bezier(0.4, 0, 0.2, 1);\r\n    min-width:0;\r\n    box-shadow:var(--elevation-1);\r\n  }\r\n\r\n  .video-item:hover{\r\n    box-shadow:var(--elevation-4);\r\n  }\r\n\r\n  .video-item:focus{\r\n    outline: 2px solid var(--primary);\r\n    outline-offset: 2px;\r\n  }\r\n\r\n  .video-thumb{\r\n    width:100px;\r\n    height:60px;\r\n    background:var(--cfx3rdlevel);\r\n    border-radius:4px;\r\n    display:grid;\r\n    place-items:center;\r\n    flex-shrink:0;\r\n  }\r\n\r\n  .video-thumb svg{\r\n    width:24px;\r\n    height:24px;\r\n    fill:var(--accent);\r\n  }\r\n\r\n  .video-info{\r\n    flex:1;\r\n    min-width:0;\r\n    overflow:hidden;\r\n  }\r\n\r\n  .video-title{\r\n    font-size:clamp(11px, 1.8vw, 13px);\r\n    font-weight:600;\r\n    color:var(--text);\r\n    margin-bottom:4px;\r\n    word-wrap:break-word;\r\n    overflow-wrap:break-word;\r\n  }\r\n\r\n  .video-duration{\r\n    font-size:clamp(9px, 1.4vw, 11px);\r\n    color:var(--text-muted);\r\n    font-weight:500;\r\n  }\r\n\r\n  .document-list{\r\n    display:flex;\r\n    flex-direction:column;\r\n    gap:10px;\r\n  }\r\n\r\n  .document-item{\r\n    display:flex;\r\n    align-items:center;\r\n    gap:16px;\r\n    padding:16px;\r\n    background:var(--card);\r\n    border:none;\r\n    border-radius:4px;\r\n    cursor:pointer;\r\n    transition:all 0.2s cubic-bezier(0.4, 0, 0.2, 1);\r\n    min-width:0;\r\n    box-shadow:var(--elevation-1);\r\n  }\r\n\r\n  .document-item:hover{\r\n    box-shadow:var(--elevation-4);\r\n  }\r\n\r\n  .document-item:focus{\r\n    outline: 2px solid var(--primary);\r\n    outline-offset: 2px;\r\n  }\r\n\r\n  .document-icon{\r\n    width:40px;\r\n    height:40px;\r\n    display:grid;\r\n    place-items:center;\r\n    background:var(--accent-light);\r\n    border-radius:4px;\r\n    flex-shrink:0;\r\n  }\r\n\r\n  .document-icon svg{\r\n    width:18px;\r\n    height:18px;\r\n    stroke:var(--accent);\r\n    fill:none;\r\n    stroke-width:2;\r\n  }\r\n\r\n  .document-info{\r\n    flex:1;\r\n    min-width:0;\r\n    overflow:hidden;\r\n  }\r\n\r\n  .document-name{\r\n    font-size:clamp(11px, 1.8vw, 13px);\r\n    font-weight:600;\r\n    color:var(--text);\r\n    margin-bottom:2px;\r\n    word-wrap:break-word;\r\n    overflow-wrap:break-word;\r\n  }\r\n\r\n  .document-meta{\r\n    font-size:clamp(9px, 1.4vw, 11px);\r\n    color:var(--text-muted);\r\n    font-weight:500;\r\n  }\r\n  \r\n  a{ color:var(--accent); text-decoration:none }\r\n  a:hover{ color:var(--accent-hover) }\r\n\r\n  .troubleshooting-item{\r\n    margin-bottom:16px;\r\n    padding:16px;\r\n    background:var(--card);\r\n    border-left:4px solid var(--primary);\r\n    border-radius:4px;\r\n    box-shadow:var(--elevation-1);\r\n  }\r\n\r\n  .troubleshooting-question{\r\n    font-size:clamp(11px, 1.8vw, 13px);\r\n    font-weight:600;\r\n    color:var(--text);\r\n    margin-bottom:8px;\r\n  }\r\n\r\n  .troubleshooting-answer{\r\n    font-size:clamp(11px, 1.8vw, 13px);\r\n    line-height:1.6;\r\n    color:var(--text-secondary);\r\n  }\r\n\r\n  .backdrop{\r\n    position:fixed;\r\n    top:0;\r\n    left:0;\r\n    width:100%;\r\n    height:100%;\r\n    background:transparent;\r\n    opacity:0;\r\n    pointer-events:none;\r\n    transition:opacity 0.3s cubic-bezier(0.4, 0, 0.2, 1);\r\n    z-index:99;\r\n  }\r\n\r\n  .backdrop.active{\r\n    opacity:1;\r\n    /* Don't block pointer events - allow scrolling and interactions with content */\r\n    pointer-events:none;\r\n  }\r\n\r\n  /* Extra Large Screens (1600px+) */\r\n  @media (min-width: 1601px){\r\n    .section-grid{grid-template-columns:repeat(auto-fill,minmax(380px,1fr))}\r\n    .search-category-tiles{grid-template-columns:repeat(auto-fill,minmax(380px,1fr))}\r\n    .container{\r\n      max-width:1800px;\r\n      margin:0 auto;\r\n    }\r\n  }\r\n\r\n  /* Large Screens (1201px - 1600px) */\r\n  @media (min-width: 1201px) and (max-width: 1600px){\r\n    .backdrop{\r\n      display:none;\r\n    }\r\n    .section-grid{grid-template-columns:repeat(auto-fill,minmax(320px,1fr))}\r\n    .search-category-tiles{grid-template-columns:repeat(auto-fill,minmax(320px,1fr))}\r\n  }\r\n\r\n  /* Desktop Screens (1025px - 1200px) */\r\n  @media (min-width: 1025px) and (max-width: 1200px){\r\n    .main-content.pane-open{\r\n      margin-right:0;\r\n    }\r\n    .side-pane{\r\n      width:100%;\r\n      max-width:520px;\r\n      right:-100%;\r\n    }\r\n    .side-pane.open{\r\n      right:0;\r\n    }\r\n    .section-grid{grid-template-columns:repeat(auto-fill,minmax(300px,1fr))}\r\n    .search-category-tiles{grid-template-columns:repeat(auto-fill,minmax(300px,1fr))}\r\n  }\r\n\r\n  /* Tablet Landscape (769px - 1024px) */\r\n  @media (min-width: 769px) and (max-width: 1024px){\r\n    .main-content.pane-open{\r\n      margin-right:0;\r\n    }\r\n    .side-pane{\r\n      width:100%;\r\n      max-width:520px;\r\n      right:-100%;\r\n    }\r\n    .side-pane.open{\r\n      right:0;\r\n    }\r\n    .top-bar{\r\n      padding: 0 16px;\r\n      gap:12px;\r\n    }\r\n    .agent-count-display{\r\n      margin-left:auto;\r\n      margin-right:8px;\r\n      font-size:12px;\r\n    }\r\n    .agent-count-display::before{\r\n      margin-right:12px;\r\n    }\r\n    .search-wrapper{\r\n      max-width:400px;\r\n    }\r\n    .sections-container{\r\n      padding:24px 20px;\r\n    }\r\n    .section-grid, .search-category-tiles{\r\n      grid-template-columns:repeat(auto-fill,minmax(280px,1fr));\r\n      gap:16px;\r\n    }\r\n    .card{\r\n      padding:16px;\r\n    }\r\n    .section-header{\r\n      padding:16px 18px;\r\n    }\r\n    .action-buttons{\r\n      grid-template-columns:1fr;\r\n      gap:12px;\r\n    }\r\n    .detail-grid{\r\n      grid-template-columns:1fr;\r\n    }\r\n    .pane-header{\r\n      padding:20px;\r\n    }\r\n    .pane-content{\r\n      padding:20px;\r\n    }\r\n  }\r\n\r\n  /* Tablet Portrait (481px - 768px) */\r\n  @media (min-width: 481px) and (max-width: 768px){\r\n    .main-content.pane-open{\r\n      margin-right:0;\r\n    }\r\n    .side-pane{\r\n      width:100%;\r\n      max-width:520px;\r\n      right:-100%;\r\n    }\r\n    .side-pane.open{\r\n      right:0;\r\n    }\r\n    .top-bar{\r\n      flex-direction:column;\r\n      align-items:stretch;\r\n      padding: 0 16px;\r\n      gap:12px;\r\n      height:auto;\r\n      min-height:64px;\r\n    }\r\n    .search-wrapper{\r\n      max-width:100%;\r\n    }\r\n    .agent-count-display{\r\n      margin-left:auto;\r\n      margin-right:0;\r\n      align-self:flex-start;\r\n    }\r\n    .agent-count-display::before{\r\n      display:none;\r\n    }\r\n    .top-bar-actions{\r\n      margin-left:0;\r\n      justify-content:space-between;\r\n      flex-wrap:wrap;\r\n    }\r\n    .sort-select{\r\n      flex:1;\r\n      min-width:150px;\r\n    }\r\n    .sections-container{\r\n      padding:20px 16px;\r\n    }\r\n    .section-grid, .search-category-tiles{\r\n      grid-template-columns:repeat(auto-fill,minmax(250px,1fr));\r\n      gap:16px;\r\n    }\r\n    .card{\r\n      padding:16px;\r\n      min-height:180px;\r\n    }\r\n    .section-header{\r\n      padding:16px;\r\n    }\r\n    .action-buttons{\r\n      grid-template-columns:1fr;\r\n      gap:12px;\r\n    }\r\n    .detail-grid{\r\n      grid-template-columns:1fr;\r\n    }\r\n    .pane-header{\r\n      padding:16px;\r\n      gap:12px;\r\n    }\r\n    .pane-content{\r\n      padding:16px;\r\n    }\r\n    .pane-icon{\r\n      width:56px;\r\n      height:56px;\r\n    }\r\n    .pane-icon svg{\r\n      width:28px;\r\n      height:28px;\r\n    }\r\n    .pane-title-area h2{\r\n      font-size:18px;\r\n    }\r\n    .tab-btn{\r\n      padding:12px 16px;\r\n      font-size:12px;\r\n    }\r\n  }\r\n\r\n  /* Mobile Landscape (481px - 600px) */\r\n  @media (min-width: 481px) and (max-width: 600px){\r\n    .side-pane{\r\n      width:100%;\r\n      max-width:100%;\r\n    }\r\n    .pane-header{\r\n      padding:16px;\r\n    }\r\n    .pane-content{\r\n      padding:16px;\r\n    }\r\n  }\r\n\r\n  /* Mobile Portrait (320px - 480px) */\r\n  @media (max-width: 480px){\r\n    .main-content.pane-open{\r\n      margin-right:0;\r\n    }\r\n    .side-pane{\r\n      width:100%;\r\n      max-width:100%;\r\n      right:-100%;\r\n    }\r\n    .side-pane.open{\r\n      right:0;\r\n    }\r\n    .top-bar{\r\n      padding: 0 12px;\r\n      gap:8px;\r\n      height:auto;\r\n      min-height:56px;\r\n    }\r\n    .search-wrapper{\r\n      max-width:100%;\r\n    }\r\n    .agent-count-display{\r\n      margin-left:auto;\r\n      margin-right:0;\r\n      align-self:flex-start;\r\n      font-size:11px;\r\n    }\r\n    .agent-count-display::before{\r\n      display:none;\r\n    }\r\n    .top-bar-actions{\r\n      margin-left:0;\r\n      justify-content:space-between;\r\n      flex-wrap:wrap;\r\n      gap:8px;\r\n    }\r\n    .sort-select{\r\n      flex:1;\r\n      min-width:120px;\r\n      font-size:14px;\r\n    }\r\n    .theme-toggle{\r\n      width:40px;\r\n      height:40px;\r\n    }\r\n    .sections-container{\r\n      padding:16px 12px;\r\n    }\r\n    .section-grid, .search-category-tiles{\r\n      grid-template-columns:1fr;\r\n      gap:12px;\r\n    }\r\n    .card{\r\n      padding:16px;\r\n      min-height:160px;\r\n    }\r\n    .section-header{\r\n      padding:12px 16px;\r\n    }\r\n    .section-title h2{\r\n      font-size:14px;\r\n    }\r\n    .section-count{\r\n      font-size:11px;\r\n      padding:4px 8px;\r\n    }\r\n    .head{\r\n      gap:12px;\r\n      margin-bottom:12px;\r\n      padding-right:40px;\r\n    }\r\n    .icon-wrap{\r\n      width:40px;\r\n      height:40px;\r\n    }\r\n    .icon-wrap svg{\r\n      width:20px;\r\n      height:20px;\r\n    }\r\n    .name{\r\n      font-size:14px;\r\n    }\r\n    .badge{\r\n      font-size:10px;\r\n      padding:4px 8px;\r\n    }\r\n    .meta{\r\n      gap:12px;\r\n      padding-top:12px;\r\n      font-size:12px;\r\n    }\r\n    .action-buttons{\r\n      grid-template-columns:1fr;\r\n      gap:8px;\r\n    }\r\n    .action-btn{\r\n      padding:12px 16px;\r\n      font-size:12px;\r\n    }\r\n    .image-gallery{\r\n      grid-template-columns:1fr;\r\n    }\r\n    .detail-grid{\r\n      grid-template-columns:1fr;\r\n    }\r\n    .pane-header{\r\n      padding:12px;\r\n      gap:12px;\r\n    }\r\n    .pane-content{\r\n      padding:12px;\r\n    }\r\n    .pane-icon{\r\n      width:48px;\r\n      height:48px;\r\n    }\r\n    .pane-icon svg{\r\n      width:24px;\r\n      height:24px;\r\n    }\r\n    .pane-title-area h2{\r\n      font-size:16px;\r\n    }\r\n    .pane-tag{\r\n      font-size:10px;\r\n      padding:4px 8px;\r\n    }\r\n    .tab-btn{\r\n      padding:12px 16px;\r\n      font-size:12px;\r\n    }\r\n    .tab-nav-btn{\r\n      width:32px;\r\n      height:32px;\r\n    }\r\n    .tab-nav-btn svg{\r\n      width:16px;\r\n      height:16px;\r\n    }\r\n    .contact-item, .document-item, .video-item{\r\n      padding:12px;\r\n      gap:12px;\r\n    }\r\n    .contact-icon, .document-icon{\r\n      width:36px;\r\n      height:36px;\r\n    }\r\n    .contact-icon svg, .document-icon svg{\r\n      width:18px;\r\n      height:18px;\r\n    }\r\n    .video-thumb{\r\n      width:80px;\r\n      height:48px;\r\n    }\r\n    .video-thumb svg{\r\n      width:20px;\r\n      height:20px;\r\n    }\r\n    .video-title{\r\n      font-size:12px;\r\n    }\r\n    .video-duration{\r\n      font-size:10px;\r\n    }\r\n    .document-name{\r\n      font-size:12px;\r\n    }\r\n    .document-meta{\r\n      font-size:10px;\r\n    }\r\n    .contact-label{\r\n      font-size:9px;\r\n    }\r\n    .contact-value{\r\n      font-size:12px;\r\n    }\r\n  }\r\n\r\n  /* Extra Small Mobile (max-width: 320px) */\r\n  @media (max-width: 320px){\r\n    .top-bar{\r\n      padding: 0 8px;\r\n    }\r\n    .sections-container{\r\n      padding:12px 8px;\r\n    }\r\n    .card{\r\n      padding:12px;\r\n      min-height:140px;\r\n    }\r\n    .section-header{\r\n      padding:12px;\r\n    }\r\n    .head{\r\n      gap:8px;\r\n      margin-bottom:10px;\r\n      padding-right:36px;\r\n    }\r\n    .icon-wrap{\r\n      width:36px;\r\n      height:36px;\r\n    }\r\n    .icon-wrap svg{\r\n      width:18px;\r\n      height:18px;\r\n    }\r\n    .name{\r\n      font-size:13px;\r\n    }\r\n    .meta{\r\n      gap:8px;\r\n      padding-top:10px;\r\n      font-size:11px;\r\n    }\r\n    .pane-header{\r\n      padding:8px;\r\n    }\r\n    .pane-content{\r\n      padding:8px;\r\n    }\r\n    .pane-icon{\r\n      width:44px;\r\n      height:44px;\r\n    }\r\n    .pane-icon svg{\r\n      width:22px;\r\n      height:22px;\r\n    }\r\n    .pane-title-area h2{\r\n      font-size:15px;\r\n    }\r\n    .tab-btn{\r\n      padding:8px 12px;\r\n      font-size:10px;\r\n    }\r\n  }\r\n\r\n  ::-webkit-scrollbar{width:8px;height:8px}\r\n  ::-webkit-scrollbar-track{background:var(--bg)}\r\n  ::-webkit-scrollbar-thumb{background:var(--border);border-radius:4px}\r\n  ::-webkit-scrollbar-thumb:hover{background:var(--text-muted)}\r\n\r\n  .gallery-item img{width:100%;height:100%;object-fit:cover;display:block}\r\n\r\n  /* Image Pagination Styles */\r\n  .image-pagination {\r\n    display: flex;\r\n    align-items: center;\r\n    justify-content: center;\r\n    gap: 12px;\r\n    margin-top: 24px;\r\n    padding: 16px 0;\r\n  }\r\n\r\n  .pagination-btn {\r\n    display: flex;\r\n    align-items: center;\r\n    gap: 8px;\r\n    padding: 8px 16px;\r\n    background: var(--card);\r\n    border: 1px solid var(--border);\r\n    border-radius: 4px;\r\n    color: var(--text);\r\n    font-size: 14px;\r\n    font-weight: 500;\r\n    cursor: pointer;\r\n    transition: all 0.2s cubic-bezier(0.4, 0, 0.2, 1);\r\n  }\r\n\r\n  .pagination-btn:hover:not(:disabled) {\r\n    background: var(--accent-light);\r\n    border-color: var(--accent);\r\n  }\r\n\r\n  .pagination-btn:disabled {\r\n    opacity: 0.5;\r\n    cursor: not-allowed;\r\n  }\r\n\r\n  .pagination-btn svg {\r\n    width: 16px;\r\n    height: 16px;\r\n  }\r\n\r\n  .page-numbers {\r\n    display: flex;\r\n    gap: 4px;\r\n    align-items: center;\r\n  }\r\n\r\n  .page-btn {\r\n    min-width: 36px;\r\n    height: 36px;\r\n    padding: 0 8px;\r\n    background: var(--card);\r\n    border: 1px solid var(--border);\r\n    border-radius: 4px;\r\n    color: var(--text);\r\n    font-size: 14px;\r\n    font-weight: 500;\r\n    cursor: pointer;\r\n    transition: all 0.2s cubic-bezier(0.4, 0, 0.2, 1);\r\n  }\r\n\r\n  .page-btn:hover {\r\n    background: var(--accent-light);\r\n    border-color: var(--accent);\r\n  }\r\n\r\n  .page-btn.active {\r\n    background: var(--accent);\r\n    color: white;\r\n    border-color: var(--accent);\r\n  }\r\n\r\n  .project-filter{\r\n    display:flex;\r\n    align-items:center;\r\n    gap:8px;\r\n    color:var(--text-secondary);\r\n    font-size:14px;\r\n    user-select:none;\r\n  }\r\n  .project-filter label{\r\n    font-weight: 500;\r\n    white-space: nowrap;\r\n  }\r\n  .project-filter-container{\r\n    position:relative;\r\n    min-width: 200px;\r\n  }\r\n  .project-filter-button{\r\n    padding: 8px 32px 8px 12px;\r\n    border-radius: 4px;\r\n    border: 1px solid var(--border);\r\n    background: var(--cfx3rdlevel);\r\n    color: var(--text);\r\n    font-size: 14px;\r\n    cursor: pointer;\r\n    transition: all 0.2s cubic-bezier(0.4, 0, 0.2, 1);\r\n    width: 100%;\r\n    text-align: left;\r\n    position: relative;\r\n    min-height: 36px;\r\n    display: flex;\r\n    align-items: center;\r\n    font-family: inherit;\r\n  }\r\n  .project-filter-button:hover{\r\n    border-color: var(--accent);\r\n    background: var(--cfx3rdlevel);\r\n  }\r\n  .project-filter-button:focus{\r\n    outline: none;\r\n    border-color: var(--primary);\r\n    box-shadow: 0 0 0 2px rgba(25,118,210,0.2);\r\n  }\r\n  .project-filter-button::after{\r\n    content: '\u25bc';\r\n    position: absolute;\r\n    right: 12px;\r\n    font-size: 10px;\r\n    color: var(--text-muted);\r\n    transition: transform 0.2s;\r\n    pointer-events: none;\r\n  }\r\n  .project-filter-button.open::after{\r\n    transform: rotate(180deg);\r\n  }\r\n  .project-filter-dropdown{\r\n    position: absolute;\r\n    top: calc(100% + 4px);\r\n    left: 0;\r\n    right: 0;\r\n    background: var(--card);\r\n    border: 1px solid var(--border);\r\n    border-radius: 4px;\r\n    box-shadow: var(--elevation-8);\r\n    max-height: 300px;\r\n    overflow-y: auto;\r\n    z-index: 1000;\r\n    display: none;\r\n    padding: 4px;\r\n    min-width: 250px;\r\n  }\r\n  .project-filter-dropdown.show{\r\n    display: block;\r\n  }\r\n  .project-filter-item{\r\n    display: flex;\r\n    align-items: center;\r\n    gap: 8px;\r\n    padding: 8px 12px;\r\n    cursor: pointer;\r\n    border-radius: 4px;\r\n    transition: background 0.2s;\r\n  }\r\n  .project-filter-item:hover{\r\n    background: var(--accent-light);\r\n  }\r\n  .project-filter-item input[type=\"checkbox\"]{\r\n    cursor: pointer;\r\n    width: 16px;\r\n    height: 16px;\r\n    accent-color: var(--accent);\r\n    flex-shrink: 0;\r\n  }\r\n  .project-filter-item label{\r\n    cursor: pointer;\r\n    margin: 0;\r\n    flex: 1;\r\n    font-weight: normal;\r\n    font-size: 14px;\r\n    color: var(--text);\r\n    display: flex;\r\n    align-items: center;\r\n    gap: 8px;\r\n  }\r\n  \r\n  .project-filter-item .project-count-badge{\r\n    display: inline-flex;\r\n    align-items: center;\r\n    justify-content: center;\r\n    min-width: 24px;\r\n    height: 20px;\r\n    padding: 0 6px;\r\n    background: var(--accent-light);\r\n    color: var(--accent);\r\n    border-radius: 10px;\r\n    font-size: 11px;\r\n    font-weight: 600;\r\n    line-height: 1;\r\n    flex-shrink: 0;\r\n    margin-left: auto;\r\n  }\r\n  .project-filter-divider{\r\n    height: 1px;\r\n    background: var(--border);\r\n    margin: 4px 0;\r\n  }\r\n\r\n  /* === Selection Modal (Persona/Workflow Selection) === */\r\n  .selection-modal {\r\n    position: fixed;\r\n    top: 0;\r\n    left: 0;\r\n    width: 100%;\r\n    height: 100%;\r\n    z-index: 10000;\r\n    display: flex;\r\n    align-items: center;\r\n    justify-content: center;\r\n  }\r\n\r\n  .selection-modal-backdrop {\r\n    position: absolute;\r\n    top: 0;\r\n    left: 0;\r\n    width: 100%;\r\n    height: 100%;\r\n    background-color: rgba(0, 0, 0, 0.7);\r\n    backdrop-filter: blur(2px);\r\n  }\r\n\r\n  .selection-modal-content {\r\n    position: relative;\r\n    background: var(--card);\r\n    border-radius: 8px;\r\n    box-shadow: var(--elevation-24);\r\n    max-width: 500px;\r\n    width: 90%;\r\n    max-height: 80vh;\r\n    display: flex;\r\n    flex-direction: column;\r\n    z-index: 1;\r\n    border: 1px solid var(--border);\r\n  }\r\n\r\n  .selection-modal-header {\r\n    display: flex;\r\n    align-items: center;\r\n    justify-content: space-between;\r\n    padding: 20px 24px;\r\n    border-bottom: 1px solid var(--border);\r\n  }\r\n\r\n  .selection-modal-header h3 {\r\n    margin: 0;\r\n    font-size: 18px;\r\n    font-weight: 600;\r\n    color: var(--text);\r\n  }\r\n\r\n  .selection-modal-close {\r\n    background: transparent;\r\n    border: none;\r\n    color: var(--text-secondary);\r\n    font-size: 28px;\r\n    cursor: pointer;\r\n    padding: 0;\r\n    width: 32px;\r\n    height: 32px;\r\n    display: flex;\r\n    align-items: center;\r\n    justify-content: center;\r\n    border-radius: 4px;\r\n    transition: all 0.2s;\r\n  }\r\n\r\n  .selection-modal-close:hover {\r\n    background: var(--accent-light);\r\n    color: var(--text);\r\n  }\r\n\r\n  .selection-modal-body {\r\n    padding: 24px;\r\n    overflow-y: auto;\r\n    flex: 1;\r\n  }\r\n\r\n  .selection-list {\r\n    display: flex;\r\n    flex-direction: column;\r\n    gap: 8px;\r\n  }\r\n\r\n  .selection-item {\r\n    display: flex;\r\n    align-items: center;\r\n    gap: 12px;\r\n    padding: 12px 16px;\r\n    background: var(--cfx3rdlevel);\r\n    border: 1px solid var(--border);\r\n    border-radius: 6px;\r\n    cursor: pointer;\r\n    transition: all 0.2s cubic-bezier(0.4, 0, 0.2, 1);\r\n  }\r\n\r\n  .selection-item:hover {\r\n    background: var(--accent-light);\r\n    border-color: var(--accent);\r\n    box-shadow: var(--elevation-2);\r\n    transform: translateY(-1px);\r\n  }\r\n\r\n  .selection-item:active {\r\n    transform: translateY(0);\r\n    box-shadow: var(--elevation-1);\r\n  }\r\n\r\n  .selection-item-icon {\r\n    width: 40px;\r\n    height: 40px;\r\n    display: flex;\r\n    align-items: center;\r\n    justify-content: center;\r\n    background: var(--accent-light);\r\n    border-radius: 6px;\r\n    flex-shrink: 0;\r\n    color: var(--accent);\r\n  }\r\n\r\n  .selection-item-text {\r\n    flex: 1;\r\n    min-width: 0;\r\n  }\r\n\r\n  .selection-item-title {\r\n    font-size: 14px;\r\n    font-weight: 600;\r\n    color: var(--text);\r\n    margin-bottom: 2px;\r\n  }\r\n\r\n  .selection-item-subtitle {\r\n    font-size: 12px;\r\n    color: var(--text-muted);\r\n    font-weight: 400;\r\n  }\r\n\r\n  /* === Image Modal (Lightbox) === */\r\n#imgModal {\r\n  display: none;\r\n  position: fixed;\r\n  z-index: 9999;\r\n  padding-top: 60px;\r\n  left: 0;\r\n  top: 0;\r\n  width: 100%;\r\n  height: 100%;\r\n  overflow: auto;\r\n  background-color: rgba(0, 0, 0, 0.9);\r\n}\r\n\r\n#imgModal img {\r\n  margin: auto;\r\n  display: block;\r\n  max-width: 90%;\r\n  max-height: 85%;\r\n  border-radius: 10px;\r\n  box-shadow: 0 0 15px #000;\r\n}\r\n\r\n#imgModalClose {\r\n  position: absolute;\r\n  top: 15px;\r\n  right: 35px;\r\n  color: #fff;\r\n  font-size: 35px;\r\n  font-weight: bold;\r\n  cursor: pointer;\r\n}\r\n/* === Agent Icon Styling === */\r\n  .icon-wrap img {\r\n    display: block;\r\n    width: 32px;\r\n    height: 32px;\r\n    border-radius: 6px;\r\n  }\r\n\r\n  .icon-wrap.default-icon {\r\n    background-color: #000000 !important;\r\n  }\r\n</style>\r\n</head>\r\n<body>\r\n  <div class=\"container\">\r\n    <div class=\"main-content\">\r\n      <div class=\"top-bar\">\r\n        <div class=\"search-wrapper\">\r\n          <svg class=\"search-icon\" width=\"18\" height=\"18\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\">\r\n            <circle cx=\"11\" cy=\"11\" r=\"8\"></circle><path d=\"m21 21-4.35-4.35\"></path>\r\n          </svg>\r\n          <input type=\"text\" class=\"search-input\" id=\"searchInput\" placeholder=\"Search agents...\">\r\n        </div>\r\n        <div class=\"top-bar-actions\">\r\n          <div class=\"project-filter\">\r\n            <label for=\"projectFilterButton\">Filter by Project:</label>\r\n            <div class=\"project-filter-container\">\r\n              <button class=\"project-filter-button\" id=\"projectFilterButton\" type=\"button\">\r\n                All Projects\r\n              </button>\r\n              <div class=\"project-filter-dropdown\" id=\"projectFilterDropdown\">\r\n                <div class=\"project-filter-item\">\r\n                  <input type=\"checkbox\" id=\"projectFilterSelectAll\" checked>\r\n                  <label for=\"projectFilterSelectAll\">\r\n                    Select All\r\n                    <span class=\"project-count-badge\" id=\"selectAllCountBadge\">0</span>\r\n                  </label>\r\n                </div>\r\n                <div class=\"project-filter-divider\"></div>\r\n                <div id=\"projectFilterOptions\"></div>\r\n              </div>\r\n            </div>\r\n          </div>\r\n          <button class=\"theme-toggle\" id=\"themeToggle\" title=\"Toggle theme\">\r\n            <svg class=\"sun-icon\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\" style=\"display: block;\">\r\n              <circle cx=\"12\" cy=\"12\" r=\"5\"></circle><line x1=\"12\" y1=\"1\" x2=\"12\" y2=\"3\"></line><line x1=\"12\" y1=\"21\" x2=\"12\" y2=\"23\"></line><line x1=\"4.22\" y1=\"4.22\" x2=\"5.64\" y2=\"5.64\"></line><line x1=\"18.36\" y1=\"18.36\" x2=\"19.78\" y2=\"19.78\"></line><line x1=\"1\" y1=\"12\" x2=\"3\" y2=\"12\"></line><line x1=\"21\" y1=\"12\" x2=\"23\" y2=\"12\"></line><line x1=\"4.22\" y1=\"19.78\" x2=\"5.64\" y2=\"18.36\"></line><line x1=\"18.36\" y1=\"5.64\" x2=\"19.78\" y2=\"4.22\"></line>\r\n            </svg>\r\n            <svg class=\"moon-icon\" style=\"display: none;\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\">\r\n              <path d=\"M21 12.79A9 9 0 1 1 11.21 3 7 7 0 0 0 21 12.79z\"></path>\r\n            </svg>\r\n          </button>\r\n        </div>\r\n      </div>\r\n\r\n      <div class=\"sections-container\" id=\"sectionsContainer\">\r\n        <div class=\"search-results-container\" id=\"searchResults\">\r\n          <div class=\"search-header\">\r\n            <h2>Search Results</h2>\r\n            <div class=\"count\" id=\"searchCount\"></div>\r\n          </div>\r\n          <div class=\"search-results-grid\" id=\"searchResultsGrid\"></div>\r\n        </div>\r\n\r\n        <div class=\"category-section\" id=\"favorites-section\">\r\n          <div class=\"section-header\" data-section=\"favorites\">\r\n            <div class=\"section-title\">\r\n              <h2>FAVORITES</h2>\r\n              <span class=\"section-count\" id=\"fav-count\">0</span>\r\n            </div>\r\n            <span class=\"toggle-icon\">\u25bc</span>\r\n          </div>\r\n          <div class=\"section-grid\" id=\"favorites-grid\"><div class=\"empty-section\">No favorites yet. Star some agents to see them here!</div></div>\r\n        </div>\r\n\r\n        <div id=\"categoriesContainer\"></div>\r\n      </div>\r\n    </div>\r\n\r\n    <div class=\"backdrop\" id=\"backdrop\"></div>\r\n\r\n    <div class=\"side-pane\" id=\"sidePane\">\r\n      <div class=\"pane-header\">\r\n        <div class=\"pane-icon\" id=\"paneIcon\"></div>\r\n        <div class=\"pane-title-area\">\r\n          <h2 id=\"paneTitle\"></h2>\r\n          <div class=\"pane-tags\" id=\"paneTags\"></div>\r\n        </div>\r\n        <button class=\"close-btn\" id=\"closePane\">\r\n          <svg viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\">\r\n            <path d=\"M18 6L6 18M6 6l12 12\"></path>\r\n          </svg>\r\n        </button>\r\n      </div>\r\n      <div class=\"pane-content\" id=\"paneContent\"></div>\r\n    </div>\r\n  </div>\r\n\r\n<script>\r\n  const icons = {\r\n    bot: '<path d=\"M12 2a2 2 0 0 1 2 2c0 .74-.4 1.39-1 1.73V7h1a7 7 0 0 1 7 7h1a1 1 0 0 1 1 1v3a1 1 0 0 1-1 1h-1a7 7 0 0 1-7 7H9a7 7 0 0 1-7-7H1a1 1 0 0 1-1-1v-3a1 1 0 0 1 1-1h1a7 7 0 0 1 7-7h1V5.73c-.6-.34-1-.99-1-1.73a2 2 0 0 1 2-2z\"/><circle cx=\"9\" cy=\"13\" r=\"1\"/><circle cx=\"15\" cy=\"13\" r=\"1\"/>',\r\n    bell: '<path d=\"M18 8A6 6 0 0 0 6 8c0 7-3 9-3 9h18s-3-2-3-9M13.73 21a2 2 0 0 1-3.46 0\"/>',\r\n    puzzle: '<path d=\"M19.439 15.439v4.122A1.439 1.439 0 0 1 18 21H4a2 2 0 0 1-2-2v-6.439A1.439 1.439 0 0 1 3.439 11H6a2 2 0 0 0 0-4H3.439A1.439 1.439 0 0 1 2 5.561V4a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2v1.561A1.439 1.439 0 0 1 18.561 7H16a2 2 0 0 0 0 4h2.561a1.439 1.439 0 0 1 .878 2.439z\"/>',\r\n    tool: '<path d=\"M14.7 6.3a1 1 0 0 0 0 1.4l1.6 1.6a1 1 0 0 0 1.4 0l3.77-3.77a6 6 0 0 1-7.94 7.94l-6.91 6.91a2.12 2.12 0 0 1-3-3l6.91-6.91a6 6 0 0 1 7.94-7.94l-3.76 3.76z\"/>',\r\n    box: '<path d=\"M21 16V8a2 2 0 0 0-1-1.73l-7-4a2 2 0 0 0-2 0l-7 4A2 2 0 0 0 3 8v8a2 2 0 0 0 1 1.73l7 4a2 2 0 0 0 2 0l7-4A2 2 0 0 0 21 16z\"/><polyline points=\"3.27 6.96 12 12.01 20.73 6.96\"/><line x1=\"12\" y1=\"22.08\" x2=\"12\" y2=\"12\"/>' ,\r\n    pen: '<path d=\"M17 3a2.83 2.83 0 1 1 4 4L7.5 20.5 2 22l1.5-5.5L17 3z\"/>',\r\n    globe: '<circle cx=\"12\" cy=\"12\" r=\"10\"/><line x1=\"2\" y1=\"12\" x2=\"22\" y2=\"12\"/><path d=\"M12 2a15.3 15.3 0 0 1 4 10 15.3 15.3 0 0 1-4 10 15.3 15.3 0 0 1-4-10 15.3 15.3 0 0 1 4-10z\"/>',\r\n    toolbox: '<path d=\"M5 4h14a2 2 0 0 1 2 2v5a1 1 0 0 1-1 1H4a1 1 0 0 1-1-1V6a2 2 0 0 1 2-2z\"/><path d=\"M3 12h18v7a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-7z\"/><path d=\"M10 6V4a1 1 0 0 1 1-1h2a1 1 0 0 1 1 1v2\"/>',\r\n    user: '<path d=\"M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2\"/><circle cx=\"12\" cy=\"7\" r=\"4\"/>',\r\n    satellite: '<path d=\"M13 7 9 3 5 7l4 4 4-4z\"/><path d=\"m17 11 4 4-4 4-4-4 4-4z\"/><path d=\"m8 12 4 4 6-6-4-4-6 6z\"/><path d=\"M3 21a9 9 0 0 0 9-9\"/>',\r\n    chart: '<line x1=\"18\" y1=\"20\" x2=\"18\" y2=\"10\"/><line x1=\"12\" y1=\"20\" x2=\"12\" y2=\"4\"/><line x1=\"6\" y1=\"20\" x2=\"6\" y2=\"14\"/>',\r\n    shield: '<path d=\"M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z\"/>'\r\n  };\r\n\r\n  // @ts-ignore - Jinja2 template syntax (processed server-side before JS execution)\r\n  {# Extract USER_AI_PROJECTS from context using Jinja2 template syntax #}\r\n  {%- set user_ai_projects = [] -%}\r\n  {# Debug: Log what's available in context #}\r\n  {# Priority 1: Get USER_AI_PROJECTS from all_variables.context #}\r\n  {%- if all_variables and all_variables.get(\"context\") -%}\r\n    {%- if all_variables.context.get(\"USER_AI_PROJECTS\") -%}\r\n      {%- set user_ai_projects = all_variables.context.get(\"USER_AI_PROJECTS\") -%}\r\n    {%- endif -%}\r\n  {# Priority 2: Get USER_AI_PROJECTS from direct context variable #}\r\n  {%- elif context and context.get(\"USER_AI_PROJECTS\") -%}\r\n    {%- set user_ai_projects = context.get(\"USER_AI_PROJECTS\") -%}\r\n  {# Priority 3: Get USER_AI_PROJECTS from request_args (query parameters) #}\r\n  {%- elif all_variables and all_variables.get(\"request_args\") and all_variables.request_args.get(\"USER_AI_PROJECTS\") -%}\r\n    {%- set user_ai_projects = all_variables.request_args.get(\"USER_AI_PROJECTS\") -%}\r\n  {%- endif -%}\r\n\r\n  // Initialize USER_AI_PROJECTS from Jinja2 template variable\r\n  const userAIProjects = {{ user_ai_projects | tojson }};\r\n  console.log('\ud83d\udd0d Debug - User AI Projects from context:', userAIProjects);\r\n  console.log('\ud83d\udd0d Debug - userAIProjects type:', typeof userAIProjects);\r\n  console.log('\ud83d\udd0d Debug - userAIProjects length:', userAIProjects ? userAIProjects.length : 0);\r\n\r\n  let agents = [];\r\n  let categoryMap = {};\r\n\r\n  async function loadData() {\r\n  try {\r\n    console.log(\"\ud83d\udd04 Loading agent data...\");\r\n    \r\n    // Build CFXQL query to filter by user's assigned projects\r\n    let cfxqlQuery = '';\r\n    if (userAIProjects && userAIProjects.length > 0) {\r\n      // Build OR condition: ai_project_name is 'project1' OR ai_project_name is 'project2' ...\r\n      const projectConditions = userAIProjects.map(project => `ai_project_name is '${project}'`).join(' OR ');\r\n      cfxqlQuery = `(${projectConditions})`;\r\n      console.log(\"\ud83d\udd0d Filtering agents by user projects:\", userAIProjects);\r\n      console.log(\"\ud83d\udd0d CFXQL Query:\", cfxqlQuery);\r\n    } else {\r\n      console.warn(\"\u26a0\ufe0f No user projects found - loading all agents\");\r\n    }\r\n    \r\n    // Build API URL with CFXQL query if available\r\n    let apiUrl = `/api/v2/pstreams/pstream/agent_catalogue_data/data?limit=1000`;\r\n    if (cfxqlQuery) {\r\n      apiUrl += `&cfxql_query=${encodeURIComponent(cfxqlQuery)}`;\r\n    }\r\n    \r\n    const [agentsRes, categoriesRes] = await Promise.all([\r\n      fetch(apiUrl, {\r\n        method: \"GET\",\r\n        headers: { \"Accept\": \"application/json\" }\r\n      }),\r\n      fetch(\"categories.json\")\r\n    ]);\r\n\r\n    console.log(\"Agents API response:\", agentsRes.status, agentsRes.ok);\r\n    console.log(\"Categories response:\", categoriesRes.status, categoriesRes.ok);\r\n\r\n    if (!agentsRes.ok) {\r\n      const errorText = await agentsRes.text();\r\n      console.error(\"\u274c Agents API failed:\", agentsRes.status, errorText);\r\n      throw new Error(`Failed to load agents: ${agentsRes.status} ${agentsRes.statusText}`);\r\n    }\r\n    \r\n    if (!categoriesRes.ok) {\r\n      console.error(\"\u274c Categories.json failed:\", categoriesRes.status);\r\n      throw new Error(`Failed to load categories.json: ${categoriesRes.status} ${categoriesRes.statusText}`);\r\n    }\r\n\r\n    const agentsData = await agentsRes.json();\r\n    let rawAgents = agentsData?.pstream_data || [];\r\n    console.log(\"\u2705 Fetched agents:\", rawAgents.length);\r\n    \r\n    // Client-side filtering by user projects (backup in case API filtering doesn't work)\r\n    if (userAIProjects && userAIProjects.length > 0) {\r\n      const beforeFilter = rawAgents.length;\r\n      rawAgents = rawAgents.filter(agent => {\r\n        const agentProject = agent.ai_project_name;\r\n        const hasAccess = agentProject && userAIProjects.includes(agentProject);\r\n        if (!hasAccess) {\r\n          console.log(`\ud83d\udeab Filtered out agent ${agent.agent_id || agent.agent_name}: project ${agentProject} not in user projects`);\r\n        }\r\n        return hasAccess;\r\n      });\r\n      console.log(`\ud83d\udd0d Client-side filtered: ${rawAgents.length} agents (from ${beforeFilter})`);\r\n    } else {\r\n      console.warn(\"\u26a0\ufe0f No user projects found - showing all agents\");\r\n    }\r\n    \r\n    if (rawAgents.length > 0) {\r\n      console.log(\"Sample agent:\", rawAgents[0]);\r\n    }\r\n    \r\n    categoryMap = await categoriesRes.json();\r\n    console.log(\"\u2705 Loaded categories:\", Object.keys(categoryMap).length);\r\n\r\n    // Parse pstream documents\r\n    agents = rawAgents.map(a => {\r\n      // remove unnecessary keys\r\n      const { _id, _index, _score, sort, _RDA_Id, ...agent } = a;\r\n\r\n      // Map new column names to expected field names\r\n      if (agent.agent_id && !agent.id) agent.id = agent.agent_id;\r\n      if (agent.agent_name && !agent.name) agent.name = agent.agent_name;\r\n\r\n      // Helper to safely parse JSON-like strings\r\n      const parseField = (field) => {\r\n      try {\r\n        if (typeof agent[field] === \"string\") {\r\n          const value = agent[field].trim();\r\n          \r\n          // If it's already a valid JSON array/object string, parse it directly\r\n          if ((value.startsWith('[') && value.endsWith(']')) || \r\n              (value.startsWith('{') && value.endsWith('}'))) {\r\n            try {\r\n              return JSON.parse(value);\r\n            } catch (e) {\r\n              // If direct parse fails, try cleaning\r\n            }\r\n          }\r\n          \r\n          // Fix escaped backslashes and single quotes for non-standard formats\r\n          const cleaned = value\r\n            .replaceAll(\"\\\\'\", \"'\")\r\n            .replace(/'/g, '\"')\r\n            .replaceAll('\"{', '{')\r\n            .replaceAll('}\"', '}')\r\n            .replaceAll('\\\\\"', '\"');\r\n\r\n          return JSON.parse(cleaned);\r\n        }\r\n      } catch (err) {\r\n        console.warn(`Could not parse field ${field} for agent ${agent.id}:`, err, agent[field]);\r\n      }\r\n      return agent[field];\r\n      };\r\n\r\n      // Helper to convert path to minio API URL\r\n      const getMinioUrl = (path) => {\r\n        if (!path) return null;\r\n        if (path.startsWith('http://') || path.startsWith('https://')) return path;\r\n        // New API: path is a query parameter\r\n        if (path.startsWith('/api/v2/object_storage/file?path=')) return path;\r\n        // Ensure path starts with / if it doesn't already\r\n        let filePath = path.trim();\r\n        if (!filePath.startsWith('/')) {\r\n          filePath = '/' + filePath;\r\n        }\r\n        return `/api/v2/object_storage/file?path=${encodeURIComponent(filePath)}`;\r\n      };\r\n\r\n      // Convert escaped string fields\r\n      agent.tags = parseField(\"tags\") || [];\r\n      agent.contact = parseField(\"contact\");\r\n      agent.documents = parseField(\"documents\") || [];\r\n      agent.images = parseField(\"images\") || [];\r\n      agent.videos = parseField(\"videos\") || [];\r\n      \r\n      // Parse personas and workflows (stored as arrays in stream)\r\n      // IMPORTANT: DO NOT TRIM - preserve spaces in persona/workflow names\r\n      // Helper to ensure we get an array from various formats\r\n      const ensurePersonaArray = (value) => {\r\n        if (!value) return [];\r\n        if (Array.isArray(value)) return value;\r\n        if (typeof value === 'string') {\r\n          const str = value;\r\n          // Try parsing as JSON array first (format: '[\"Service Desk\", \"DataOps\"]')\r\n          if (str.trim().startsWith('[') && str.trim().endsWith(']')) {\r\n            try {\r\n              const parsed = JSON.parse(str);\r\n              // DO NOT TRIM - preserve spaces\r\n              return Array.isArray(parsed) ? parsed : [];\r\n            } catch (e) {\r\n              console.warn('Failed to parse personas JSON:', e, str);\r\n            }\r\n          }\r\n          // If it's a comma-separated string (but NOT a JSON array), split it\r\n          if (!str.trim().startsWith('[') && str.includes(',')) {\r\n            // DO NOT TRIM - only remove surrounding quotes, preserve spaces\r\n            return str.split(',').map(p => p.replace(/^[\"']|[\"']$/g, '')).filter(p => p && p.length > 0);\r\n          }\r\n          // Single string value - preserve as-is (no trim)\r\n          return str.length > 0 ? [str] : [];\r\n        }\r\n        return [];\r\n      };\r\n      \r\n      let parsedPersonas = parseField(\"personas\");\r\n      let parsedAiPersonas = parseField(\"ai_personas\");\r\n      \r\n      // Ensure arrays and deduplicate (filter out empty/null values)\r\n      // DO NOT TRIM - preserve spaces in persona names\r\n      const personasArray = ensurePersonaArray(parsedPersonas);\r\n      const aiPersonasArray = ensurePersonaArray(parsedAiPersonas);\r\n      \r\n      // DO NOT TRIM - preserve spaces in persona names\r\n      agent.personas = [...new Set(personasArray.map(p => String(p)).filter(p => p && p.length > 0))];\r\n      agent.ai_personas = [...new Set(aiPersonasArray.map(p => String(p)).filter(p => p && p.length > 0))];\r\n      \r\n      console.log('\ud83d\udd0d Parsed personas:', agent.personas, 'from:', parsedPersonas);\r\n      console.log('\ud83d\udd0d Parsed ai_personas:', agent.ai_personas, 'from:', parsedAiPersonas);\r\n      \r\n      // Parse workflows similarly - use same helper as personas\r\n      // IMPORTANT: DO NOT TRIM - preserve spaces in workflow names\r\n      const ensureWorkflowArray = (value) => {\r\n        if (!value) return [];\r\n        if (Array.isArray(value)) return value;\r\n        if (typeof value === 'string') {\r\n          const str = value;\r\n          // Try parsing as JSON array first (format: '[\"workflow1\", \"workflow2\"]')\r\n          if (str.trim().startsWith('[') && str.trim().endsWith(']')) {\r\n            try {\r\n              const parsed = JSON.parse(str);\r\n              // DO NOT TRIM - preserve spaces\r\n              return Array.isArray(parsed) ? parsed : [];\r\n            } catch (e) {\r\n              console.warn('Failed to parse workflows JSON:', e, str);\r\n            }\r\n          }\r\n          // If it's a comma-separated string (but NOT a JSON array), split it\r\n          if (!str.trim().startsWith('[') && str.includes(',')) {\r\n            // DO NOT TRIM - only remove surrounding quotes, preserve spaces\r\n            return str.split(',').map(p => p.replace(/^[\"']|[\"']$/g, '')).filter(p => p && p.length > 0);\r\n          }\r\n          // Single string value - preserve as-is (no trim)\r\n          return str.length > 0 ? [str] : [];\r\n        }\r\n        return [];\r\n      };\r\n      \r\n      let parsedWorkflows = parseField(\"workflows\");\r\n      let parsedAiWorkflows = parseField(\"ai_workflows\");\r\n      \r\n      // Ensure arrays and deduplicate (filter out empty/null values)\r\n      // DO NOT TRIM - preserve spaces in workflow names\r\n      const workflowsArray = ensureWorkflowArray(parsedWorkflows);\r\n      const aiWorkflowsArray = ensureWorkflowArray(parsedAiWorkflows);\r\n      \r\n      // DO NOT TRIM - preserve spaces in workflow names\r\n      agent.workflows = [...new Set(workflowsArray.map(w => String(w)).filter(w => w && w.length > 0))];\r\n      agent.ai_workflows = [...new Set(aiWorkflowsArray.map(w => String(w)).filter(w => w && w.length > 0))];\r\n      \r\n      console.log('\ud83d\udd0d Parsed workflows:', agent.workflows, 'from:', parsedWorkflows);\r\n      console.log('\ud83d\udd0d Parsed ai_workflows:', agent.ai_workflows, 'from:', parsedAiWorkflows);\r\n\r\n      // Initialize MinIO file arrays (will be populated by loadMinioFiles)\r\n      agent.images = [];\r\n      agent.documents = [];\r\n      agent.videos = [];\r\n      agent.icons = [];\r\n\r\n      // Process documents with minio URL conversion\r\n      if (Array.isArray(agent.documents) && agent.documents.length > 0) {\r\n        agent.documents = agent.documents.map(doc => {\r\n          const docObj = typeof doc === 'string' ? { url: doc } : doc;\r\n          const docPath = docObj.path || docObj.url || '';\r\n          return {\r\n            ...docObj,\r\n            url: getMinioUrl(docPath) || docObj.url,\r\n            path: docPath,\r\n            name: docObj.name || docPath.split('/').pop() || 'Document',\r\n            type: docObj.type || docPath.split('.').pop() || 'file'\r\n          };\r\n        });\r\n      }\r\n\r\n      // Process videos with minio URL conversion\r\n      if (Array.isArray(agent.videos) && agent.videos.length > 0) {\r\n        agent.videos = agent.videos.map(video => {\r\n          const videoObj = typeof video === 'string' ? { url: video } : video;\r\n          const videoPath = videoObj.path || videoObj.url || '';\r\n          return {\r\n            ...videoObj,\r\n            url: getMinioUrl(videoPath) || videoObj.url,\r\n            path: videoPath,\r\n            title: videoObj.title || videoPath.split('/').pop() || 'Video'\r\n          };\r\n        });\r\n      }\r\n\r\n      // Fix booleans\r\n      agent.hasInteractiveMode = agent.hasInteractiveMode === \"true\" || agent.hasInteractiveMode === true;\r\n      agent.hasWorkflowMode = agent.hasWorkflowMode === \"true\" || agent.hasWorkflowMode === true;\r\n\r\n      return agent;\r\n    });\r\n\r\n    // Assign globally so renderAll() still works\r\n    window.agents = agents;\r\n    console.log(\"\u2705 Final parsed agents count:\", agents.length);\r\n    console.log(\"\ud83d\udccb Final parsed agent sample:\", agents[0]);\r\n    \r\n    // Debug: Check agent projects\r\n    if (agents.length > 0) {\r\n      const agentProjects = [...new Set(agents.map(a => a.ai_project_name).filter(Boolean))];\r\n      console.log(\"\ud83d\udd0d Unique agent projects found:\", agentProjects);\r\n      console.log(\"\ud83d\udd0d User AI Projects:\", userAIProjects);\r\n      console.log(\"\ud83d\udd0d Matching projects:\", agentProjects.filter(p => userAIProjects && userAIProjects.includes(p)));\r\n    }\r\n    \r\n    // Render agents immediately (don't wait for icons)\r\n    window.renderAll?.();\r\n    \r\n    // Load MinIO icons in background (non-blocking) - will update UI as they load\r\n    loadMinioFilesForAgents(agents).catch(err => {\r\n      console.warn('Error loading agent icons:', err);\r\n    });\r\n\r\n  } catch (err) {\r\n    console.error(\"\u274c Data load error:\", err);\r\n    const container = document.getElementById(\"sectionsContainer\");\r\n    if (container) {\r\n      container.innerHTML = `\r\n        <div class=\"empty-section\" style=\"padding: 40px 20px;\">\r\n          <h3 style=\"color: var(--error); margin-bottom: 16px;\">\u26a0\ufe0f Failed to load data</h3>\r\n          <p style=\"margin-bottom: 12px;\"><strong>Error:</strong> ${err.message}</p>\r\n          <p style=\"color: var(--text-muted); font-size: 13px; margin-bottom: 8px;\">Please check:</p>\r\n          <ul style=\"text-align: left; display: inline-block; color: var(--text-muted); font-size: 13px;\">\r\n            <li>Pstream API: <code>/api/v2/pstreams/pstream/agent_catalogue_data/data</code></li>\r\n            <li>Categories file: <code>categories.json</code></li>\r\n            <li>Network connectivity</li>\r\n            <li>Browser console for detailed errors</li>\r\n          </ul>\r\n        </div>`;\r\n    }\r\n  }\r\n}\r\n\r\n\r\n  const favKey = 'agentFavs';\r\n  const themeKey = 'agentTheme';\r\n  const favSet = new Set(JSON.parse(localStorage.getItem(favKey) || '[]'));\r\n  const $ = (sel, el=document)=>el.querySelector(sel);\r\n  const $$ = (sel, el=document)=>el.querySelectorAll(sel);\r\n\r\n  // Helper to fetch file list from MinIO.\r\n  // path must be the full folder path: agent_catalog/<agent_id>/<folder_type> (e.g. agent_catalog/test-dd270d32/images).\r\n  // Do NOT pass only the base folder; the API requires the complete path.\r\n  async function fetchFileList(path) {\r\n    if (!path || path.trim() === '') return [];\r\n    \r\n    try {\r\n                // Use the complete folder path for the API (do NOT truncate to base folder).\r\n                // Expected format: agent_catalog/<agent_id>/<folder_type> (e.g. agent_catalog/test-dd270d32/images).\r\n                // Remove leading slash if present.\r\n      let folderPath = path.trim();\r\n      if (folderPath.startsWith('/')) {\r\n        folderPath = folderPath.substring(1);\r\n      }\r\n      \r\n                // cfxql_query: filter by full folder path\r\n      const cfxqlQuery = `folder is '${folderPath}'`;\r\n      \r\n      // Pass the complete folder path to the API (do NOT truncate). Format: agent_catalog/<agent_id>/<folder_type>\r\n      const url = `/api/v2/object_storage/folder?folder_path=${encodeURIComponent(folderPath)}&cfxql_query=${encodeURIComponent(cfxqlQuery)}&limit=100`;\r\n      const response = await fetch(url, {\r\n        method: 'GET',\r\n        headers: { 'Accept': 'application/json' }\r\n      });\r\n      \r\n      if (!response.ok) {\r\n        console.warn(`Failed to fetch file list for path ${path}:`, response.status);\r\n        return [];\r\n      }\r\n      \r\n      const data = await response.json();\r\n      \r\n      // Debug: Log the response structure to understand the API format\r\n      if (!data.objects && !data.data && !data.items && !data.results && !data.rda_objects) {\r\n        console.warn(`\u26a0\ufe0f Unexpected API response structure for path ${path}:`, Object.keys(data));\r\n        console.log('Full response:', data);\r\n      }\r\n      \r\n      // Try different possible response field names\r\n      // The API returns data in: rda_objects, objects, data, items, results, or directly as an array\r\n      const fileList = data.rda_objects || data.objects || data.data || data.items || data.results || \r\n                       (Array.isArray(data) ? data : []);\r\n      \r\n      if (fileList.length === 0 && data.num_items > 0) {\r\n        console.warn(`\u26a0\ufe0f API reports ${data.num_items} items but fileList is empty for path ${path}`);\r\n        console.log(`\ud83d\udd0d Available fields in response:`, Object.keys(data));\r\n        console.log(`\ud83d\udd0d Checking rda_objects:`, data.rda_objects ? `${data.rda_objects.length} items` : 'not found');\r\n      }\r\n      \r\n      return fileList;\r\n    } catch (err) {\r\n      console.warn(`Error fetching file list for path ${path}:`, err);\r\n      return [];\r\n    }\r\n  }\r\n\r\n  // Helper to fetch file content and convert to data URL\r\n  // fileType: Optional file type from API response (e.g., 'png', 'pdf') - more reliable than parsing filename\r\n  async function fetchFileAsDataUrl(filePath, fileType = null) {\r\n    try {\r\n      // New API: path is a query parameter (data_path from API response)\r\n      // Path should NOT have leading slash - use as-is from data_path\r\n      // Note: .data files contain the actual binary content (images, PDFs, etc.)\r\n      // When fetched, they return the binary content, which is correct behavior\r\n      const path = filePath.trim();\r\n      \r\n      const url = `/api/v2/object_storage/file?path=${encodeURIComponent(path)}`;\r\n      const response = await fetch(url, {\r\n        method: 'GET',\r\n        headers: { 'Accept': '*/*' }\r\n      });\r\n      \r\n      if (!response.ok) {\r\n        const errorText = await response.text().catch(() => 'Unable to read error response');\r\n        console.error(`\u274c Failed to fetch file ${filePath}:`, {\r\n          status: response.status,\r\n          statusText: response.statusText,\r\n          error: errorText.substring(0, 200) // First 200 chars of error\r\n        });\r\n        return null;\r\n      }\r\n      \r\n      // Get content type from response header, fileType parameter, or infer from filename\r\n      let contentType = response.headers.get('content-type');\r\n      const fileName = filePath.split('/').pop() || '';\r\n      \r\n      // Map file types to MIME types\r\n      const mimeTypes = {\r\n        'jpg': 'image/jpeg',\r\n        'jpeg': 'image/jpeg',\r\n        'png': 'image/png',\r\n        'gif': 'image/gif',\r\n        'webp': 'image/webp',\r\n        'pdf': 'application/pdf',\r\n        'mp4': 'video/mp4',\r\n        'webm': 'video/webm',\r\n        'mp3': 'audio/mpeg',\r\n        'wav': 'audio/wav',\r\n        'txt': 'text/plain',\r\n        'json': 'application/json',\r\n        'xml': 'application/xml',\r\n        'csv': 'text/csv'\r\n      };\r\n      \r\n      // Prefer fileType from API response (most reliable)\r\n      if (fileType && mimeTypes[fileType.toLowerCase()]) {\r\n        contentType = mimeTypes[fileType.toLowerCase()];\r\n      }\r\n      // Handle .data files - determine actual file type from filename if fileType not provided\r\n      else if (fileName.endsWith('.data') && !contentType) {\r\n        // Extract extension from hash-prefixed filename (e.g., \"520bf2ea-asset_health_6_png.data\" -> \"png\")\r\n        const nameWithoutData = fileName.replace(/\\.data$/, '');\r\n        // Remove hash prefix if present (e.g., \"520bf2ea-asset_health_6_png\" -> \"asset_health_6_png\")\r\n        const nameWithoutHash = nameWithoutData.includes('-') ? nameWithoutData.split('-').slice(1).join('-') : nameWithoutData;\r\n        const extension = nameWithoutHash.split('.').pop()?.toLowerCase();\r\n        \r\n        if (extension && mimeTypes[extension]) {\r\n          contentType = mimeTypes[extension];\r\n        } else {\r\n          // Default to image/jpeg if we can't determine\r\n          contentType = 'image/jpeg';\r\n        }\r\n      }\r\n      \r\n      const blob = await response.blob();\r\n      \r\n      // If we determined a content type and blob doesn't have it, create a new blob with correct type\r\n      if (contentType && blob.type !== contentType) {\r\n        const correctedBlob = new Blob([blob], { type: contentType });\r\n        return new Promise((resolve, reject) => {\r\n          const reader = new FileReader();\r\n          reader.onloadend = () => resolve(reader.result);\r\n          reader.onerror = () => reject(new Error('Failed to convert blob to data URL'));\r\n          reader.readAsDataURL(correctedBlob);\r\n        });\r\n      }\r\n      \r\n      return new Promise((resolve, reject) => {\r\n        const reader = new FileReader();\r\n        reader.onloadend = () => resolve(reader.result);\r\n        reader.onerror = () => reject(new Error('Failed to convert blob to data URL'));\r\n        reader.readAsDataURL(blob);\r\n      });\r\n    } catch (err) {\r\n      console.warn(`Error fetching file ${filePath}:`, err);\r\n      return null;\r\n    }\r\n  }\r\n\r\n  // Function to load only icons for an agent (called on initial load)\r\n  async function loadMinioIconsForAgent(agent) {\r\n    const iconsPath = agent.icons_path;\r\n    \r\n    if (!iconsPath) return;\r\n    \r\n    // Load icons (use first icon as agent icon)\r\n    const iconFiles = await fetchFileList(iconsPath);\r\n    for (const file of iconFiles) {\r\n      // Check if it's an image file (file_type from API response)\r\n      const fileType = (file.file_type || '').toLowerCase();\r\n      if (fileType === 'png' || fileType === 'jpg' || fileType === 'jpeg' || fileType === 'gif' || fileType === 'webp') {\r\n        // Use data_path from API response to fetch the file\r\n        // Pass file_type from API response for accurate content type detection\r\n        const dataUrl = await fetchFileAsDataUrl(file.data_path, fileType);\r\n        if (dataUrl) {\r\n          agent.icons.push({\r\n            url: dataUrl,\r\n            path: file.data_path,\r\n            name: file.name // Use name from API response (display name without .data)\r\n          });\r\n        }\r\n      }\r\n    }\r\n    // Use first icon as the main agent icon\r\n    if (agent.icons.length > 0) {\r\n      agent.iconUrl = agent.icons[0].url;\r\n      // Update card icon immediately if card is already rendered\r\n      updateCardIcon(agent);\r\n    }\r\n  }\r\n\r\n  // Function to load only documents for an agent (called when card is opened)\r\n  async function loadMinioDocsForAgent(agent) {\r\n    // Check if already loaded\r\n    if (agent._docsLoaded) {\r\n      return;\r\n    }\r\n    \r\n    const docsPath = agent.docs_path;\r\n    if (!docsPath) {\r\n      agent._docsLoaded = true;\r\n      return;\r\n    }\r\n\r\n    // Load documents\r\n    const docFiles = await fetchFileList(docsPath);\r\n    for (const file of docFiles) {\r\n      // Use data_path from API response (no leading slash needed)\r\n      const fileUrl = `/api/v2/object_storage/file?path=${encodeURIComponent(file.data_path)}`;\r\n      const fileExt = (file.file_type || '').toLowerCase() || 'file';\r\n      \r\n      agent.documents.push({\r\n        url: fileUrl,\r\n        path: file.data_path,\r\n        name: file.name, // Use name from API response (display name)\r\n        type: fileExt,\r\n        size: file.file_size,\r\n        contentType: file.content_type || `application/${fileExt}`\r\n      });\r\n    }\r\n    \r\n    // Mark as loaded\r\n    agent._docsLoaded = true;\r\n    \r\n    // Update tabs if no docs found (tabs will be hidden)\r\n    if (agent.documents.length === 0 && window.updateTabsAfterLoad) {\r\n      window.updateTabsAfterLoad(agent);\r\n    }\r\n  }\r\n\r\n  // Function to get image file list (fast - just metadata)\r\n  async function getImageFileList(agent) {\r\n    const imagesPath = agent.images_path;\r\n    if (!imagesPath) return [];\r\n    \r\n    const imageFiles = await fetchFileList(imagesPath);\r\n    \r\n    // Debug: Log first file structure to understand API response format\r\n    if (imageFiles.length > 0) {\r\n      console.log(`\ud83d\udccb Sample file object structure for agent ${agent.id}:`, Object.keys(imageFiles[0]));\r\n      console.log(`\ud83d\udccb Sample file object:`, imageFiles[0]);\r\n    } else {\r\n      console.warn(`\u26a0\ufe0f No files returned from API for images_path: ${imagesPath}`);\r\n    }\r\n    \r\n    // Filter for image files based on file_type from API response\r\n    const imageTypes = ['png', 'jpg', 'jpeg', 'gif', 'webp'];\r\n    const filteredFiles = imageFiles.filter(file => {\r\n      const fileType = (file.file_type || '').toLowerCase();\r\n      const isImage = imageTypes.includes(fileType);\r\n      \r\n      // Also check if data_path exists (required for fetching the file)\r\n      if (isImage && !file.data_path) {\r\n        console.warn(`\u26a0\ufe0f Image file missing data_path:`, file);\r\n      }\r\n      \r\n      return isImage;\r\n    });\r\n    \r\n    console.log(`\ud83d\uddbc\ufe0f Filtered ${filteredFiles.length} image files from ${imageFiles.length} total files for agent ${agent.id}`);\r\n    \r\n    return filteredFiles;\r\n  }\r\n\r\n  // Function to load a single image and update the DOM\r\n  async function loadSingleImage(agent, file, index) {\r\n    try {\r\n      // Check if data_path exists\r\n      if (!file.data_path) {\r\n        console.error(`\u274c File object missing data_path at index ${index} for agent ${agent.id}:`, file);\r\n        return;\r\n      }\r\n      \r\n      // Use data_path from API response to fetch the file\r\n      // Pass file_type from API response for accurate content type detection\r\n      const fileType = (file.file_type || '').toLowerCase();\r\n      const dataUrl = await fetchFileAsDataUrl(file.data_path, fileType);\r\n      if (dataUrl) {\r\n        const imageData = {\r\n          url: dataUrl,\r\n          path: file.data_path,\r\n          name: file.name, // Use name from API response (display name)\r\n          imageName: agent.name || 'Agent Image',\r\n          alt: agent.name || 'Agent Image',\r\n          size: file.file_size,\r\n          contentType: file.content_type || `image/${fileType || 'jpeg'}`\r\n        };\r\n        \r\n        // Add to agent's images array\r\n        agent.images[index] = imageData;\r\n        \r\n        // Update the DOM immediately for this specific image\r\n        let imgElement = document.getElementById(`img-${agent.id}-${index}`);\r\n        const spinnerElement = document.getElementById(`spinner-${agent.id}-${index}`);\r\n        const galleryItem = spinnerElement ? spinnerElement.closest('.gallery-item') : null;\r\n        \r\n        // Hide spinner\r\n        if (spinnerElement) {\r\n          spinnerElement.style.display = 'none';\r\n          spinnerElement.style.pointerEvents = 'none';\r\n        }\r\n        \r\n        // Create img element if it doesn't exist\r\n        if (!imgElement && galleryItem) {\r\n          imgElement = document.createElement('img');\r\n          imgElement.id = `img-${agent.id}-${index}`;\r\n          imgElement.alt = imageData.name;\r\n          galleryItem.appendChild(imgElement);\r\n        }\r\n        \r\n        if (imgElement) {\r\n          // Update image source with smooth transition\r\n          imgElement.src = dataUrl;\r\n          imgElement.alt = imageData.name;\r\n          \r\n          // Ensure proper styling for loaded image\r\n          imgElement.style.width = '100%';\r\n          imgElement.style.height = '100%';\r\n          imgElement.style.objectFit = 'cover';\r\n          imgElement.style.borderRadius = '8px';\r\n          imgElement.style.cursor = 'pointer';\r\n          imgElement.style.pointerEvents = 'auto';\r\n          \r\n          // Set up click handler for modal (use imageData.name instead of undefined fileName)\r\n          imgElement.onclick = function(e) {\r\n            e.stopPropagation();\r\n            openImageModal(dataUrl, imageData.name);\r\n          };\r\n          \r\n          // Also set onclick attribute for compatibility\r\n          imgElement.setAttribute('onclick', `openImageModal('${dataUrl.replace(/'/g, \"\\\\'\")}', '${imageData.name.replace(/'/g, \"\\\\'\")}')`);\r\n          \r\n          // Show image and fade in\r\n          imgElement.style.display = 'block';\r\n          imgElement.style.opacity = '0';\r\n          setTimeout(() => {\r\n            imgElement.style.transition = 'opacity 0.3s ease';\r\n            imgElement.style.opacity = '1';\r\n            // Re-attach onclick handler after transition to ensure it persists\r\n            imgElement.onclick = function(e) {\r\n              e.stopPropagation();\r\n              openImageModal(dataUrl, imageData.name);\r\n            };\r\n            imgElement.setAttribute('onclick', `openImageModal('${dataUrl.replace(/'/g, \"\\\\'\")}', '${imageData.name.replace(/'/g, \"\\\\'\")}')`);\r\n          }, 10);\r\n        }\r\n        \r\n        return imageData;\r\n      }\r\n    } catch (err) {\r\n      console.warn(`Failed to load image ${file.name}:`, err);\r\n      // Keep placeholder but mark as failed\r\n      const imgElement = document.getElementById(`img-${agent.id}-${index}`);\r\n      if (imgElement) {\r\n        imgElement.style.opacity = '0.5';\r\n        imgElement.title = 'Failed to load image';\r\n      }\r\n    }\r\n    return null;\r\n  }\r\n\r\n  // Helper function to load images for a specific page range\r\n  async function loadImagesForPageRange(agent, startIdx, endIdx) {\r\n    const imageFiles = agent._imageFiles || [];\r\n    if (imageFiles.length === 0) return;\r\n    \r\n    // Initialize images array if needed\r\n    if (!agent.images || agent.images.length !== imageFiles.length) {\r\n      agent.images = new Array(imageFiles.length);\r\n    }\r\n    \r\n    // Track which images are being loaded for this page\r\n    const pageFiles = imageFiles.slice(startIdx, endIdx);\r\n    const loadPromises = pageFiles.map((file, idx) => {\r\n      const globalIdx = startIdx + idx;\r\n      // Only load if not already loaded\r\n      if (!agent.images[globalIdx] || !agent.images[globalIdx].url) {\r\n        return loadSingleImage(agent, file, globalIdx);\r\n      }\r\n      return Promise.resolve();\r\n    });\r\n    \r\n    await Promise.all(loadPromises);\r\n    console.log(`\u2705 Loaded images ${startIdx} to ${endIdx - 1} for agent ${agent.id}`);\r\n  }\r\n\r\n  // Function to load images progressively (called when Images tab is clicked)\r\n  // Now only loads the first page (6 images) initially\r\n  async function loadMinioImagesForAgent(agent) {\r\n    // Check if already initialized\r\n    if (agent._imagesInitialized) {\r\n      return;\r\n    }\r\n    \r\n    const imagesPath = agent.images_path;\r\n    if (!imagesPath) {\r\n      agent._imagesInitialized = true;\r\n      return;\r\n    }\r\n\r\n    if (agent._imagesLoading) {\r\n      return;\r\n    }\r\n\r\n    agent._imagesLoading = true;\r\n    \r\n    try {\r\n      // Step 1: Get file list quickly (just metadata)\r\n      const imageFiles = await getImageFileList(agent);\r\n      \r\n      if (imageFiles.length === 0) {\r\n        agent._imagesInitialized = true;\r\n        agent._imagesLoading = false;\r\n        // Update tabs to hide images tab since there are no images\r\n        if (window.updateTabsAfterLoad) {\r\n          window.updateTabsAfterLoad(agent);\r\n        }\r\n        return;\r\n      }\r\n      \r\n      // Step 2: Store image file list metadata for pagination\r\n      agent._imageFiles = imageFiles; // Store metadata for pagination\r\n      \r\n      // Step 3: Initialize images array\r\n      if (!agent.images || agent.images.length !== imageFiles.length) {\r\n        agent.images = new Array(imageFiles.length);\r\n      }\r\n      \r\n      // Step 4: Load ONLY the first 6 images (first page)\r\n      const imagesPerPage = 6;\r\n      await loadImagesForPageRange(agent, 0, Math.min(imagesPerPage, imageFiles.length));\r\n      \r\n      // Mark as initialized (but not fully loaded - more pages will load on demand)\r\n      agent._imagesInitialized = true;\r\n      \r\n    } catch (err) {\r\n      console.error(`Failed to load images for agent ${agent.name}:`, err);\r\n    } finally {\r\n      agent._imagesLoading = false;\r\n    }\r\n  }\r\n\r\n  // Function to load videos for an agent (if needed in future)\r\n  async function loadMinioVideosForAgent(agent) {\r\n    if (agent._videosLoaded) {\r\n      return;\r\n    }\r\n    \r\n    const videosPath = agent.videos_path;\r\n    if (!videosPath) {\r\n      agent._videosLoaded = true;\r\n      return;\r\n    }\r\n\r\n    const videoFiles = await fetchFileList(videosPath);\r\n    for (const file of videoFiles) {\r\n      // Check if it's a video file (file_type from API response)\r\n      const fileType = (file.file_type || '').toLowerCase();\r\n      if (fileType === 'mp4' || fileType === 'webm' || fileType === 'mov' || fileType === 'avi') {\r\n        // Use data_path from API response (no leading slash needed)\r\n        const fileUrl = `/api/v2/object_storage/file?path=${encodeURIComponent(file.data_path)}`;\r\n        \r\n        agent.videos.push({\r\n          url: fileUrl,\r\n          path: file.data_path,\r\n          title: file.name, // Use name from API response (display name)\r\n          contentType: file.content_type || `video/${fileType}`,\r\n          size: file.file_size\r\n        });\r\n      }\r\n    }\r\n    \r\n    agent._videosLoaded = true;\r\n    \r\n    // Update tabs if no videos found (tabs will be hidden)\r\n    if (agent.videos.length === 0 && window.updateTabsAfterLoad) {\r\n      window.updateTabsAfterLoad(agent);\r\n    }\r\n  }\r\n\r\n  // Function to update card icon after it loads\r\n  function updateCardIcon(agent) {\r\n    if (!agent.iconUrl) return;\r\n    \r\n    const card = document.querySelector(`.card[data-id=\"${agent.id}\"]`);\r\n    if (!card) return;\r\n    \r\n    const iconWrap = card.querySelector('.icon-wrap');\r\n    if (!iconWrap) return;\r\n    \r\n    const img = iconWrap.querySelector('img');\r\n    if (img && img.src !== agent.iconUrl) {\r\n      img.src = agent.iconUrl;\r\n      iconWrap.classList.remove('default-icon');\r\n      iconWrap.style.backgroundColor = 'transparent';\r\n    }\r\n  }\r\n\r\n  // Function to load only icons for all agents (initial load)\r\n  async function loadMinioFilesForAgents(agents) {\r\n    console.log(`\ud83d\udd04 Loading MinIO icons for ${agents.length} agents...`);\r\n    \r\n    // Process agents in batches of 5 to avoid overwhelming the API\r\n    const batchSize = 5;\r\n    for (let i = 0; i < agents.length; i += batchSize) {\r\n      const batch = agents.slice(i, i + batchSize);\r\n      await Promise.all(batch.map(agent => loadMinioIconsForAgent(agent)));\r\n      \r\n      // Update card icons for this batch immediately after loading\r\n      batch.forEach(agent => {\r\n        if (agent.iconUrl) {\r\n          updateCardIcon(agent);\r\n        }\r\n      });\r\n      \r\n      console.log(`\u2705 Loaded icons for ${Math.min(i + batchSize, agents.length)}/${agents.length} agents`);\r\n    }\r\n    \r\n    console.log(`\u2705 Completed loading MinIO icons for all agents`);\r\n  }\r\n\r\n  // Function to get RDA object contents - tries multiple approaches\r\n  async function getRDAObjectImage(folder, name) {\r\n    // Silently try to load via service API (only method that might work)\r\n    // If it fails, we'll just keep using the placeholder (no errors logged)\r\n    try {\r\n      const serviceEndpoint = `/api/v2/service/invoke`;\r\n      const requestPayload = {\r\n        \"service-name\": \"saas-reports\",\r\n        \"request\": {\r\n          \"request-type\": \"get_rda_object_contents\",\r\n          \"params\": {\r\n            \"folder\": folder,\r\n            \"name\": name\r\n          }\r\n        }\r\n      };\r\n      \r\n      const response = await fetch(serviceEndpoint, {\r\n        method: 'POST',\r\n        headers: { \r\n          'Content-Type': 'application/json',\r\n          'Accept': '*/*'\r\n        },\r\n        body: JSON.stringify(requestPayload)\r\n      });\r\n      \r\n      if (response.ok) {\r\n        const blob = await response.blob();\r\n        return new Promise((resolve, reject) => {\r\n          const reader = new FileReader();\r\n          reader.onloadend = () => {\r\n            console.log(`\u2705 [getRDAObjectImage] Loaded via service API`);\r\n            resolve(reader.result);\r\n          };\r\n          reader.onerror = () => reject(new Error('Failed to convert blob'));\r\n          reader.readAsDataURL(blob);\r\n        });\r\n      }\r\n    } catch (err) {\r\n      // Silently fail - placeholder will be used instead\r\n      // No console logs to avoid cluttering\r\n    }\r\n    \r\n    // Return null to indicate we should keep using placeholder\r\n    return null;\r\n  }\r\n\r\n  let searchQuery = '';\r\n  let sortBy = 'name';\r\n  let activeTab = 'documents';\r\n  let tabScrollPosition = 0;\r\n  let selectedProjects = []; // Array of selected project names. Empty array = all projects selected\r\n  // userAIProjects is already declared above from Jinja2 template\r\n\r\n  // Theme toggle\r\n  const themeToggle = $('#themeToggle');\r\n  const sunIcon = $('.sun-icon');\r\n  const moonIcon = $('.moon-icon');\r\n  \r\n  const savedTheme = localStorage.getItem(themeKey) || 'dark';\r\n  if(savedTheme === 'light'){\r\n    document.documentElement.setAttribute('data-theme', 'light');\r\n    sunIcon.style.display = 'none';\r\n    moonIcon.style.display = 'block';\r\n  }\r\n\r\n  themeToggle.addEventListener('click', () => {\r\n    const currentTheme = document.documentElement.getAttribute('data-theme');\r\n    const newTheme = currentTheme === 'light' ? 'dark' : 'light';\r\n    \r\n    document.documentElement.setAttribute('data-theme', newTheme);\r\n    localStorage.setItem(themeKey, newTheme);\r\n    \r\n    if(newTheme === 'light'){\r\n      sunIcon.style.display = 'none';\r\n      moonIcon.style.display = 'block';\r\n    } else {\r\n      sunIcon.style.display = 'block';\r\n      moonIcon.style.display = 'none';\r\n    }\r\n  });\r\n\r\n  // Adopt light/dark mode from top-level app automatically\r\n  (function setupParentThemeSync(){\r\n    const htmlEl = document.documentElement;\r\n    const applyTheme = (mode) => {\r\n      const t = mode === 'light' ? 'light' : 'dark';\r\n      htmlEl.setAttribute('data-theme', t);\r\n    };\r\n\r\n    function tryParentDomTheme(){\r\n      try{\r\n        if(window.top && window.top !== window && window.top.document){\r\n          const t = window.top.document.documentElement.getAttribute('data-theme');\r\n          if(t === 'light' || t === 'dark'){\r\n            applyTheme(t);\r\n            return true;\r\n          }\r\n        }\r\n      }catch(e){ /* cross-origin */ }\r\n      return false;\r\n    }\r\n\r\n    window.addEventListener('message', (ev) => {\r\n      const d = ev && ev.data;\r\n      if(!d) return;\r\n      const t = (d.theme || (d.payload && d.payload.theme) || '').toLowerCase();\r\n      if(d.type === 'THEME_CHANGE' || d.type === 'SET_THEME'){\r\n        if(t === 'light' || t === 'dark') applyTheme(t);\r\n      }\r\n    });\r\n\r\n    if(!tryParentDomTheme()){\r\n      // Request theme from parent if embedded\r\n      try{ if(window.top && window.top !== window){ window.top.postMessage({ type:'REQUEST_THEME', from:'agent_catalogue' }, '*'); } }catch(e){}\r\n      // Fallback to system preference\r\n      const mq = window.matchMedia && window.matchMedia('(prefers-color-scheme: dark)');\r\n      if(mq){\r\n        applyTheme(mq.matches ? 'dark' : 'light');\r\n        if(mq.addEventListener){\r\n          mq.addEventListener('change', (ev) => applyTheme(ev.matches ? 'dark' : 'light'));\r\n        }\r\n      }\r\n    }\r\n  })();\r\n\r\n  function favButton(id){\r\n    const btn = document.createElement('button');\r\n    btn.className = 'fav' + (favSet.has(id) ? ' active' : '');\r\n    btn.innerHTML = '<svg viewBox=\"0 0 24 24\"><path d=\"M12 17.3l-6.2 3.3 1.2-6.9L1 8.9l6.9-1 3.1-6.3 3.1 6.3 6.9 1-5 4.8 1.2 6.9z\"/></svg>';\r\n    btn.addEventListener('click', (e)=>{\r\n      e.stopPropagation();\r\n      if(favSet.has(id)){\r\n        favSet.delete(id);\r\n        btn.classList.remove('active');\r\n      } else {\r\n        favSet.add(id);\r\n        btn.classList.add('active');\r\n      }\r\n      localStorage.setItem(favKey, JSON.stringify([...favSet]));\r\n      renderAll();\r\n    });\r\n    return btn;\r\n  }\r\n\r\n  function tile(agent){\r\n    const card = document.createElement('div');\r\n    card.className = 'card tile';\r\n    card.dataset.id = agent.id || '';\r\n    card.dataset.name = (agent.name || '').toLowerCase();\r\n    card.dataset.category = agent.category || '';\r\n\r\n    card.appendChild(favButton(agent.id));\r\n\r\n    const head = document.createElement('div');\r\n    head.className = 'head';\r\n    \r\n    // Use agent icon if available, otherwise use default icon\r\n    const placeholderIcon = 'data:image/svg+xml;base64,';\r\n    const defaultIconUrl = \"https://www.fabrix.ai/images/icons/ai_agent_icon.svg\";\r\n    const iconUrl = agent.icon || agent.iconUrl || defaultIconUrl;\r\n    const isDefaultIcon = !agent.icon && !agent.iconUrl;\r\n    \r\n    head.innerHTML = `\r\n      <div class=\"icon-wrap ${isDefaultIcon ? 'default-icon' : ''}\"\r\n     style=\"width:48px;height:48px;display:flex;align-items:center;justify-content:center;border-radius:8px;overflow:hidden;background-color:${isDefaultIcon ? '#000' : '#fff'};padding:4px;box-sizing:border-box;\">\r\n  <img \r\n      src=\"${iconUrl}\"\r\n      alt=\"${agent.name}\"\r\n      style=\"width:100%;height:100%;object-fit:contain;display:block;\"\r\n      onerror=\"this.src='${defaultIconUrl}'; this.parentElement.classList.add('default-icon'); this.parentElement.style.backgroundColor='#000';\"\r\n    />\r\n</div>\r\n      <div class=\"title-area\">\r\n        <h3 class=\"name\">${agent.name}</h3>\r\n        <span class=\"badge\">${agent.category}</span>\r\n      </div>\r\n    `;\r\n\r\n    card.appendChild(head);\r\n\r\n    // Check for multiple personas/workflows\r\n    const personas = getAgentPersonas(agent);\r\n    const workflows = getAgentWorkflows(agent);\r\n    const hasMultiplePersonas = personas.length > 1;\r\n    const hasMultipleWorkflows = workflows.length > 1;\r\n\r\n    const meta = document.createElement('div');\r\n    meta.className = 'meta';\r\n    \r\n    // Build meta HTML with optional indicators\r\n    let metaHTML = `\r\n      <div class=\"kv\"><div class=\"k\">Version</div><div class=\"v\">${agent.version}</div></div>\r\n      <div class=\"kv\"><div class=\"k\">Publisher</div><div class=\"v\">${agent.publisher}</div></div>\r\n    `;\r\n    \r\n    // Add persona/workflow indicators if multiple exist\r\n    // OPTIONS section removed as per user request\r\n    \r\n    metaHTML += `\r\n      <div class=\"kv desc\"><div class=\"k\">Description</div><div class=\"v desc-truncate\">${(agent.description || '').replace(/</g,'&lt;').replace(/>/g,'&gt;')}</div></div>\r\n      <div class=\"state-indicator ${agent.state === 'Disabled' ? 'disabled' : ''}\">\r\n        <span class=\"dot\"></span>${agent.state}\r\n      </div>\r\n    `;\r\n    \r\n    meta.innerHTML = metaHTML;\r\n    card.appendChild(meta);\r\n\r\n    card.addEventListener('click', () => openPane(agent));\r\n\r\n    return card;\r\n  }\r\n\r\n  function updateTabNavigation(){\r\n    const container = document.querySelector('.tabs-nav-container');\r\n    if(!container) return;\r\n    \r\n    const scroll = container.querySelector('.tabs-scroll');\r\n    const leftBtn = container.querySelector('.tab-nav-btn.left');\r\n    const rightBtn = container.querySelector('.tab-nav-btn.right');\r\n    \r\n    if(!scroll || !leftBtn || !rightBtn) return;\r\n    \r\n    const maxScroll = scroll.scrollWidth - scroll.parentElement.clientWidth;\r\n    \r\n    leftBtn.classList.toggle('hidden', tabScrollPosition <= 0);\r\n    rightBtn.classList.toggle('hidden', tabScrollPosition >= maxScroll);\r\n  }\r\n\r\n  function scrollTabs(direction){\r\n    const container = document.querySelector('.tabs-nav-container');\r\n    if(!container) return;\r\n    \r\n    const scroll = container.querySelector('.tabs-scroll');\r\n    if(!scroll) return;\r\n    \r\n    const scrollAmount = 200;\r\n    const maxScroll = scroll.scrollWidth - scroll.parentElement.clientWidth;\r\n    \r\n    if(direction === 'left'){\r\n      tabScrollPosition = Math.max(0, tabScrollPosition - scrollAmount);\r\n    } else {\r\n      tabScrollPosition = Math.min(maxScroll, tabScrollPosition + scrollAmount);\r\n    }\r\n    \r\n    scroll.style.transform = `translateX(-${tabScrollPosition}px)`;\r\n    updateTabNavigation();\r\n  }\r\n\r\n  async function switchTab(tabName){\r\n    activeTab = tabName;\r\n    \r\n    // If Images tab is clicked, load images if not already loaded\r\n    if (tabName === 'images') {\r\n      const paneTitle = $('#paneTitle')?.textContent;\r\n      if (paneTitle) {\r\n        const currentAgent = agents.find(a => a.name === paneTitle);\r\n        if (currentAgent) {\r\n          // Always clear images tab content first to prevent showing wrong images\r\n          const imagesTabContent = $(`.tab-content[data-tab=\"images\"]`);\r\n          \r\n          // If images are already loaded, re-render with pagination\r\n          if (currentAgent.images && currentAgent.images.length > 0) {\r\n            if (imagesTabContent) {\r\n              renderImagesWithPagination(currentAgent, imagesTabContent).catch(err => {\r\n                console.error('Error rendering images:', err);\r\n              });\r\n            }\r\n          } else if (!currentAgent._imagesInitialized && !currentAgent._imagesLoading) {\r\n            // Load images if not initialized yet\r\n          const imagesPath = currentAgent.images_path;\r\n          if (imagesPath) {\r\n            // Step 1: Get file list quickly (just metadata - very fast)\r\n            const imageFiles = await getImageFileList(currentAgent);\r\n            // Store image file list metadata for pagination\r\n            currentAgent._imageFiles = imageFiles;\r\n            \r\n            if (imageFiles.length > 0) {\r\n                // Step 2: Immediately show placeholders with spinners for first 6 images\r\n              if (imagesTabContent) {\r\n                const placeholdersToShow = imageFiles.slice(0, 6);\r\n                  const placeholders = placeholdersToShow.map((file, idx) => {\r\n                  const fileName = file.name.split('/').pop();\r\n                  return `\r\n                    <div class=\"gallery-item\" style=\"position:relative;min-height:120px;background:var(--cfx3rdlevel);display:flex;align-items:center;justify-content:center;\">\r\n                      <div class=\"image-loading-spinner\" id=\"spinner-${currentAgent.id}-${idx}\" style=\"display:flex;align-items:center;justify-content:center;\">\r\n                        <svg width=\"32\" height=\"32\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"var(--primary, #7dcfff)\" stroke-width=\"2\">\r\n                          <circle cx=\"12\" cy=\"12\" r=\"10\" stroke-opacity=\"0.25\"/>\r\n                          <path d=\"M12 2 A10 10 0 0 1 22 12\" stroke-linecap=\"round\"/>\r\n                        </svg>\r\n                      </div>\r\n                    </div>\r\n                  `;\r\n                }).join('');\r\n                \r\n                imagesTabContent.innerHTML = `\r\n                  <div class=\"tab-section\">\r\n                      <div class=\"tab-section-title\">Screenshots & Diagrams ${imageFiles.length > 6 ? `(${imageFiles.length} total)` : ''}</div>\r\n                    <div class=\"image-gallery\">${placeholders}</div>\r\n                      ${imageFiles.length > 6 ? '<div class=\"image-pagination\"><div style=\"text-align:center;padding:16px;color:var(--text-muted);\">Loading images... Pagination will appear once loaded.</div></div>' : ''}\r\n                  </div>`;\r\n              }\r\n              \r\n              // Step 3: Start progressive loading in the background (non-blocking)\r\n              loadMinioImagesForAgent(currentAgent).then(() => {\r\n                  // Update tabs if images were loaded\r\n                  if (currentAgent.images && currentAgent.images.length > 0) {\r\n                    if (window.updateTabsAfterLoad) {\r\n                      window.updateTabsAfterLoad(currentAgent);\r\n                    }\r\n                    // Re-render with pagination once all images are loaded\r\n                    const imagesTabContent = $(`.tab-content[data-tab=\"images\"]`);\r\n                    if (imagesTabContent) {\r\n                      renderImagesWithPagination(currentAgent, imagesTabContent).catch(err => {\r\n                        console.error('Error rendering images:', err);\r\n                      });\r\n                    }\r\n                  }\r\n              });\r\n            } else {\r\n              // No images found - mark as initialized and hide the tab\r\n              currentAgent._imagesInitialized = true;\r\n              // Update tabs to hide images tab since there are no images\r\n              if (window.updateTabsAfterLoad) {\r\n                window.updateTabsAfterLoad(currentAgent);\r\n              }\r\n              }\r\n            }\r\n          }\r\n        }\r\n      }\r\n    }\r\n    \r\n    // If Videos tab is clicked, load videos if not already loaded\r\n    if (tabName === 'videos') {\r\n      const paneTitle = $('#paneTitle')?.textContent;\r\n      if (paneTitle) {\r\n        const currentAgent = agents.find(a => a.name === paneTitle);\r\n        if (currentAgent && !currentAgent._videosLoaded && !currentAgent._videosLoading) {\r\n          const videosPath = currentAgent.videos_path;\r\n          if (videosPath) {\r\n            // Show loading placeholder\r\n            const videosTabContent = $(`.tab-content[data-tab=\"videos\"]`);\r\n            if (videosTabContent) {\r\n              videosTabContent.innerHTML = `\r\n                <div class=\"tab-section\">\r\n                  <div class=\"tab-section-title\">Tutorial Videos</div>\r\n                  <div class=\"video-list\" style=\"text-align:center;padding:40px;color:var(--text-muted);\">\r\n                    <div style=\"display:flex;align-items:center;justify-content:center;gap:12px;\">\r\n                      <svg width=\"24\" height=\"24\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" style=\"animation:spin 1s linear infinite;\">\r\n                        <circle cx=\"12\" cy=\"12\" r=\"10\" stroke-opacity=\"0.25\"/>\r\n                        <path d=\"M12 2 A10 10 0 0 1 22 12\" stroke-linecap=\"round\"/>\r\n                      </svg>\r\n                      <span>Loading videos...</span>\r\n                    </div>\r\n                  </div>\r\n                </div>`;\r\n            }\r\n            \r\n            // Load videos\r\n            loadMinioVideosForAgent(currentAgent).then(() => {\r\n              // Always update pane content after loading (even if no videos found)\r\n              updatePaneContent(currentAgent);\r\n              // Always update tabs after loading (even if no videos found)\r\n              if (window.updateTabsAfterLoad) {\r\n                window.updateTabsAfterLoad(currentAgent);\r\n              }\r\n            });\r\n          } else {\r\n            // No videos path\r\n            const videosTabContent = $(`.tab-content[data-tab=\"videos\"]`);\r\n            if (videosTabContent) {\r\n              videosTabContent.innerHTML = '<div class=\"tab-section\"><div class=\"tab-section-title\">Tutorial Videos</div><div class=\"video-list\" style=\"text-align:center;padding:40px;color:var(--text-muted);\">No videos found</div></div>';\r\n            }\r\n            currentAgent._videosLoaded = true;\r\n          }\r\n        }\r\n      }\r\n    }\r\n    \r\n    $$('.tab-btn').forEach(btn => {\r\n      btn.classList.toggle('active', btn.dataset.tab === tabName);\r\n    });\r\n    \r\n    $$('.tab-content').forEach(content => {\r\n      content.classList.toggle('active', content.dataset.tab === tabName);\r\n    });\r\n  }\r\n  \r\n  // Helper function to get personas from agent (arrays from stream)\r\n  // IMPORTANT: Preserves spaces in persona names (never trim)\r\n  function getAgentPersonas(agent) {\r\n    const personas = [];\r\n    \r\n    // Helper to ensure we have an array\r\n    // IMPORTANT: Do NOT trim persona names - preserve spaces before/after\r\n    const ensureArray = (value) => {\r\n      if (!value) return [];\r\n      if (Array.isArray(value)) {\r\n        // Ensure all items are strings - DO NOT TRIM (preserve spaces)\r\n        return value.map(v => String(v)).filter(v => v && v.length > 0);\r\n      }\r\n      if (typeof value === 'string') {\r\n        const str = value;\r\n        // Try to parse as JSON array first (format: '[\"Service Desk\", \"DataOps\"]')\r\n        if (str.trim().startsWith('[') && str.trim().endsWith(']')) {\r\n          try {\r\n            const parsed = JSON.parse(str);\r\n            if (Array.isArray(parsed)) {\r\n              // DO NOT TRIM - preserve spaces in persona names\r\n              return parsed.map(v => String(v)).filter(v => v && v.length > 0);\r\n            }\r\n          } catch (e) {\r\n            console.warn('Failed to parse personas JSON:', e, str);\r\n            // If JSON parse fails, try to extract values manually\r\n            try {\r\n              // Try to extract values from string like [\"a\", \"b\", \"c\"]\r\n              const matches = str.match(/\"([^\"]+)\"/g);\r\n              if (matches) {\r\n                // DO NOT TRIM - preserve spaces\r\n                return matches.map(m => m.replace(/\"/g, '')).filter(m => m && m.length > 0);\r\n              }\r\n            } catch (e2) {\r\n              console.warn('Failed to extract personas from string:', e2);\r\n            }\r\n          }\r\n        }\r\n        // If it's a comma-separated string (but NOT a JSON array), split it\r\n        if (!str.trim().startsWith('[') && str.includes(',')) {\r\n          // Only remove surrounding quotes, DO NOT TRIM spaces\r\n          return str.split(',').map(p => p.replace(/^[\"']|[\"']$/g, '')).filter(p => p && p.length > 0);\r\n        }\r\n        // Single string value - preserve as-is (no trim)\r\n        return str.length > 0 ? [str] : [];\r\n      }\r\n      return [];\r\n    };\r\n    \r\n    // Check for array fields from stream (parse if needed)\r\n    const parsedPersonas = ensureArray(agent.personas);\r\n    const parsedAiPersonas = ensureArray(agent.ai_personas);\r\n    \r\n    if (parsedPersonas.length > 0) {\r\n      personas.push(...parsedPersonas);\r\n    }\r\n    if (parsedAiPersonas.length > 0) {\r\n      personas.push(...parsedAiPersonas);\r\n    }\r\n    \r\n    // Fallback to single value for backward compatibility\r\n    // Handle case where ai_persona/persona might be a JSON array string like '[\"DataOps\"]'\r\n    const addPersonaFallback = (value) => {\r\n      if (!value) return;\r\n      let val = value;\r\n      // If it's a JSON array string, parse it\r\n      if (typeof val === 'string' && val.trim().startsWith('[') && val.trim().endsWith(']')) {\r\n        try {\r\n          const parsed = JSON.parse(val);\r\n          if (Array.isArray(parsed)) {\r\n            parsed.forEach(p => {\r\n              const pStr = String(p);\r\n              if (pStr && pStr.length > 0 && !personas.includes(pStr)) {\r\n                personas.push(pStr);\r\n              }\r\n            });\r\n            return;\r\n          }\r\n        } catch (e) {\r\n          // Not valid JSON, use as-is\r\n        }\r\n      }\r\n      // If it's an array\r\n      if (Array.isArray(val)) {\r\n        val.forEach(p => {\r\n          const pStr = String(p);\r\n          if (pStr && pStr.length > 0 && !personas.includes(pStr)) {\r\n            personas.push(pStr);\r\n          }\r\n        });\r\n        return;\r\n      }\r\n      // Single string value\r\n      const valStr = String(val);\r\n      if (valStr && valStr.length > 0 && !personas.includes(valStr)) {\r\n        personas.push(valStr);\r\n      }\r\n    };\r\n    \r\n    addPersonaFallback(agent.ai_persona);\r\n    addPersonaFallback(agent.persona);\r\n    \r\n    // Return unique personas (deduplicate and filter out empty values)\r\n    // DO NOT TRIM - preserve spaces in persona names\r\n    const uniquePersonas = [...new Set(personas.map(p => String(p)).filter(p => p && p.length > 0))];\r\n    console.log('\ud83d\udd0d getAgentPersonas - Input:', { personas: agent.personas, ai_personas: agent.ai_personas });\r\n    console.log('\ud83d\udd0d getAgentPersonas - Output:', uniquePersonas);\r\n    return uniquePersonas;\r\n  }\r\n\r\n  // Helper function to get workflows from agent (arrays from stream)\r\n  // IMPORTANT: Preserves spaces in workflow names (never trim)\r\n  function getAgentWorkflows(agent) {\r\n    console.log('\ud83d\udd0d getAgentWorkflows - Agent object:', {\r\n      workflows: agent.workflows,\r\n      ai_workflows: agent.ai_workflows,\r\n      workflow_name: agent.workflow_name,\r\n      ai_workflow_name: agent.ai_workflow_name,\r\n      allKeys: Object.keys(agent).filter(k => k.toLowerCase().includes('workflow'))\r\n    });\r\n    \r\n    const workflows = [];\r\n    \r\n    // Helper to ensure we have an array (similar to personas)\r\n    // IMPORTANT: DO NOT TRIM - preserve spaces in workflow names\r\n    const ensureArray = (value) => {\r\n      if (!value) return [];\r\n      if (Array.isArray(value)) {\r\n        // First, convert all items to strings\r\n        let result = value.map(v => String(v)).filter(v => v && v.length > 0);\r\n        \r\n        // Special case: if array has a single element that looks like a JSON array, parse it\r\n        if (result.length === 1 && result[0].trim().startsWith('[') && result[0].trim().endsWith(']')) {\r\n          try {\r\n            const parsed = JSON.parse(result[0]);\r\n            if (Array.isArray(parsed) && parsed.length > 0) {\r\n              // DO NOT TRIM - preserve spaces\r\n              result = parsed.map(v => String(v)).filter(v => v && v.length > 0);\r\n              console.log('\u2705 Parsed single JSON array element into multiple workflows:', result);\r\n            }\r\n          } catch (e) {\r\n            // Try regex extraction\r\n            const matches = result[0].match(/\"([^\"]+)\"/g);\r\n            if (matches && matches.length > 0) {\r\n              result = matches.map(m => m.replace(/\"/g, '')).filter(m => m && m.length > 0);\r\n              console.log('\u2705 Extracted workflows from single array element using regex:', result);\r\n            }\r\n          }\r\n        }\r\n        \r\n        return result;\r\n      }\r\n      if (typeof value === 'string') {\r\n        const str = value;\r\n        // Try to parse as JSON array first (format: '[\"workflow1\", \"workflow2\"]')\r\n        if (str.trim().startsWith('[') && str.trim().endsWith(']')) {\r\n          try {\r\n            const parsed = JSON.parse(str);\r\n            if (Array.isArray(parsed)) {\r\n              // DO NOT TRIM - preserve spaces\r\n              return parsed.map(v => String(v)).filter(v => v && v.length > 0);\r\n            }\r\n          } catch (e) {\r\n            console.warn('Failed to parse workflows JSON:', e, str);\r\n            // If JSON parse fails, try to extract values manually\r\n            try {\r\n              // Try to extract values from string like [\"a\", \"b\", \"c\"]\r\n              const matches = str.match(/\"([^\"]+)\"/g);\r\n              if (matches) {\r\n                // DO NOT TRIM - preserve spaces\r\n                return matches.map(m => m.replace(/\"/g, '')).filter(m => m && m.length > 0);\r\n              }\r\n            } catch (e2) {\r\n              console.warn('Failed to extract workflows from string:', e2);\r\n            }\r\n          }\r\n        }\r\n        // If it's a comma-separated string (but NOT a JSON array), split it\r\n        if (!str.trim().startsWith('[') && str.includes(',')) {\r\n          // DO NOT TRIM - preserve spaces\r\n          return str.split(',').map(p => p.replace(/^[\"']|[\"']$/g, '')).filter(p => p && p.length > 0);\r\n        }\r\n        // Single string value - preserve as-is (no trim)\r\n        return str.length > 0 ? [str] : [];\r\n      }\r\n      return [];\r\n    };\r\n    \r\n    // Check for array fields from stream (parse if needed)\r\n    // Check multiple possible field names\r\n    const workflowFields = [\r\n      agent.workflows,\r\n      agent.ai_workflows,\r\n      agent.workflow,\r\n      agent.ai_workflow,\r\n      agent.workflow_list,\r\n      agent.ai_workflow_list\r\n    ];\r\n    \r\n    for (const fieldValue of workflowFields) {\r\n      if (fieldValue) {\r\n        const parsed = ensureArray(fieldValue);\r\n        if (parsed.length > 0) {\r\n          workflows.push(...parsed);\r\n          console.log('\u2705 Found workflows in field:', fieldValue, 'parsed:', parsed);\r\n        }\r\n      }\r\n    }\r\n    \r\n    // Fallback to single value for backward compatibility\r\n    const singleWorkflowFields = [\r\n      agent.ai_workflow_name,\r\n      agent.workflow_name,\r\n      agent.workflowName,\r\n      agent.ai_workflowName\r\n    ];\r\n    \r\n    for (const fieldValue of singleWorkflowFields) {\r\n      // DO NOT TRIM - preserve spaces\r\n      const workflowStr = String(fieldValue);\r\n      if (fieldValue && workflowStr && workflowStr.length > 0 && !workflows.some(w => String(w) === workflowStr)) {\r\n        workflows.push(workflowStr);\r\n        console.log('\u2705 Found single workflow:', fieldValue);\r\n      }\r\n    }\r\n    \r\n    // Deduplicate and convert to objects with name/persona\r\n    // DO NOT TRIM - preserve spaces in workflow names\r\n    const uniqueWorkflows = [...new Set(workflows.map(w => String(w)).filter(w => w && w.length > 0))];\r\n    const defaultPersona = agent.ai_persona || agent.persona || null;\r\n    \r\n    // Convert to array of objects\r\n    const workflowObjects = uniqueWorkflows.map(w => {\r\n      if (typeof w === 'object' && w.name) {\r\n        return w; // Already an object\r\n      }\r\n      return { name: String(w), persona: defaultPersona };\r\n    });\r\n    \r\n    console.log('\ud83d\udd0d getAgentWorkflows - Input:', { workflows: agent.workflows, ai_workflows: agent.ai_workflows });\r\n    console.log('\ud83d\udd0d getAgentWorkflows - Output:', workflowObjects);\r\n    return workflowObjects;\r\n  }\r\n\r\n  function launchfabaio(agent, selectedPersona = null) {\r\n    const today = new Date();\r\n    const day = String(today.getDate()).padStart(2, '0');\r\n    const month = String(today.getMonth() + 1).padStart(2, '0'); // Month is 0-indexed\r\n    const year = today.getFullYear();\r\n    const formattedDate = `${month}/${day}/${year}`;\r\n    \r\n    // Use selected persona or fallback to single persona\r\n    let persona = selectedPersona || agent.ai_persona || agent.persona || \"Lab Admin\";\r\n    \r\n    // If persona is an array, take the first element\r\n    if (Array.isArray(persona) && persona.length > 0) {\r\n      persona = persona[0];\r\n    }\r\n    \r\n    // If persona is still a string that looks like a JSON array, parse it\r\n    if (typeof persona === 'string' && persona.trim().startsWith('[') && persona.trim().endsWith(']')) {\r\n      try {\r\n        const parsed = JSON.parse(persona);\r\n        if (Array.isArray(parsed) && parsed.length > 0) {\r\n          persona = parsed[0];\r\n        }\r\n      } catch (e) {\r\n        // Not valid JSON, use as-is\r\n      }\r\n    }\r\n    \r\n    // Ensure persona is a string\r\n    persona = String(persona);\r\n    \r\n    // Get the AI project name from the agent\r\n    const aiProjectName = agent.ai_project_name || \"Legacy\";\r\n    \r\n    // Create fully qualified persona name (project/persona format)\r\n    const personaFqName = `${aiProjectName}/${persona}`;\r\n    \r\n    console.log('\ud83d\ude80 launchfabaio - persona:', persona, 'ai_project_name:', aiProjectName, 'persona_fq_name:', personaFqName);\r\n   \r\n    const cfxAction = {\r\n      \"type\": \"GO_TO_APP_STATE\", \r\n      \"label\": \"Fabaio\", \r\n      \"appName\": \"user-dashboard/fabrix.ai-copilot\", \r\n      \"pageName\": \"user-dashboard-fabrix.ai-copilot\", \r\n      \"contexts\": {\r\n        \"agent\": {},\r\n        \"persona\": persona,\r\n        \"persona_fq_name\": personaFqName,\r\n        \"ai_project_name\": aiProjectName\r\n      }, \r\n      \"postLaunchParams\": {\r\n        \"startConversation\": {\r\n          \"title\": `${persona} ${formattedDate}`,\r\n          \"persona\": persona,\r\n          \"persona_fq_name\": personaFqName,\r\n          \"ai_project_name\": aiProjectName,\r\n          \"llm\": \"claude-sonnet-4\"\r\n        }\r\n      } \r\n    };\r\n    console.log('\ud83d\ude80 launchfabaio - cfxAction:', JSON.stringify(cfxAction, null, 2));\r\n    window.top.postMessage(cfxAction);\r\n  }\r\n\r\n  function launchfabaioOriginal(agent) {\r\n    console.log(\"Launching Copilor with agent persona\")\r\n    console.log(agent);\r\n    const cfxAction = {\r\n      type: \"GO_TO_APP_STATE\",\r\n      label: \"Fabaio\",\r\n      appName: \"user-dashboard/fabrix.ai-copilot\",\r\n      pageName: \"user-dashboard-fabrix.ai-copilot\",\r\n      contexts: { agent }\r\n    };\r\n    window.top.postMessage(cfxAction);\r\n  }\r\n\r\n  function launchAIPersonaObserveDashboard(agent, selectedPersona = null) {\r\n    // Use selected persona or fallback to single persona\r\n    let persona = selectedPersona || agent.ai_persona || agent.persona || \"Lab Admin\";\r\n    \r\n    // If persona is an array, take the first element\r\n    if (Array.isArray(persona) && persona.length > 0) {\r\n      persona = persona[0];\r\n    }\r\n    \r\n    // If persona is still a string that looks like a JSON array, parse it\r\n    if (typeof persona === 'string' && persona.trim().startsWith('[') && persona.trim().endsWith(']')) {\r\n      try {\r\n        const parsed = JSON.parse(persona);\r\n        if (Array.isArray(parsed) && parsed.length > 0) {\r\n          persona = parsed[0];\r\n        }\r\n      } catch (e) {\r\n        // Not valid JSON, use as-is\r\n      }\r\n    }\r\n    \r\n    // Ensure persona is a string\r\n    persona = String(persona);\r\n    \r\n    // Get the AI project name from the agent\r\n    const aiProjectName = agent.ai_project_name || \"Legacy\";\r\n    \r\n    // Build the fully qualified persona name for precise identification\r\n    const personaFqName = `${aiProjectName}/${persona}`;\r\n    \r\n    console.log('\ud83d\udd0d launchAIPersonaObserveDashboard - persona:', persona, 'ai_project_name:', aiProjectName);\r\n    \r\n    const cfxAction = {\r\n      type: \"GO_TO_APP_STATE\",\r\n      label: \"Observe Dashboard\",\r\n      appName: \"user-dashboard/ai_administration_observe\",\r\n      pageName: \"user-dashboard-ai_catalog_chat_sessions\",\r\n      contexts: { \r\n        persona: persona,\r\n        persona_fq_name: personaFqName,\r\n        ai_project_name: aiProjectName\r\n      }\r\n    };\r\n    console.log('\ud83d\udd0d launchAIPersonaObserveDashboard - cfxAction:', JSON.stringify(cfxAction, null, 2));\r\n    window.top.postMessage(cfxAction);\r\n  }\r\n\r\n  function launchAgenticWorkflows(agent, selectedWorkflow = null, selectedPersona = null) {\r\n    // Use selected workflow/persona or fallback to single values\r\n    // selectedWorkflow can be a string (workflow name) or null\r\n    // selectedPersona can be a string (persona name) or null\r\n    let workflowName = selectedWorkflow;\r\n    let personaName = selectedPersona;\r\n    \r\n    // If no workflow selected, try to get from agent's workflows\r\n    if (!workflowName) {\r\n      const workflows = getAgentWorkflows(agent);\r\n      if (workflows.length > 0) {\r\n        const workflow = workflows[0];\r\n        workflowName = typeof workflow === 'object' ? workflow.name : workflow;\r\n        if (!personaName && typeof workflow === 'object') {\r\n          personaName = workflow.persona;\r\n        }\r\n      }\r\n    }\r\n    \r\n    // Fallback to agent's workflow_name if still no workflow\r\n    if (!workflowName) {\r\n      workflowName = agent.ai_workflow_name || agent.workflow_name;\r\n    }\r\n    \r\n    // Handle workflowName being an array - extract first element\r\n    if (Array.isArray(workflowName)) {\r\n      if (workflowName.length > 0) {\r\n        workflowName = workflowName[0];\r\n      } else {\r\n        workflowName = null;\r\n      }\r\n    }\r\n    \r\n    // If workflowName is still a string that looks like a JSON array, parse it\r\n    if (typeof workflowName === 'string' && workflowName.trim().startsWith('[') && workflowName.trim().endsWith(']')) {\r\n      try {\r\n        const parsed = JSON.parse(workflowName);\r\n        if (Array.isArray(parsed) && parsed.length > 0) {\r\n          workflowName = parsed[0];\r\n        } else {\r\n          workflowName = null;\r\n        }\r\n      } catch (e) {\r\n        // Not valid JSON, use as-is\r\n        console.log('\u26a0\ufe0f Could not parse workflow name as JSON:', workflowName);\r\n      }\r\n    }\r\n    \r\n    // Ensure workflowName is a string (or null if empty)\r\n    if (workflowName) {\r\n      workflowName = String(workflowName).trim();\r\n      if (workflowName.length === 0) {\r\n        workflowName = null;\r\n      }\r\n    } else {\r\n      workflowName = null;\r\n    }\r\n    \r\n    // Final validation - ensure we have a valid workflow name\r\n    if (!workflowName) {\r\n      console.error('\u274c No valid workflow name found for agent:', agent.name || agent.id);\r\n      console.error('\u274c Agent workflow data:', {\r\n        ai_workflow_name: agent.ai_workflow_name,\r\n        workflow_name: agent.workflow_name,\r\n        workflows: agent.workflows\r\n      });\r\n      alert('No workflow found for this agent. Please check the agent configuration.');\r\n      return;\r\n    }\r\n    \r\n    console.log('\u2705 Final workflow name to use:', workflowName);\r\n    \r\n    // Fallback to agent's persona if still no persona\r\n    if (!personaName) {\r\n      personaName = agent.ai_persona || agent.persona || \"Lab Admin\";\r\n    }\r\n    \r\n    // Handle personaName being an array\r\n    if (Array.isArray(personaName) && personaName.length > 0) {\r\n      personaName = personaName[0];\r\n    }\r\n    \r\n    // If personaName is still a string that looks like a JSON array, parse it\r\n    if (typeof personaName === 'string' && personaName.trim().startsWith('[') && personaName.trim().endsWith(']')) {\r\n      try {\r\n        const parsed = JSON.parse(personaName);\r\n        if (Array.isArray(parsed) && parsed.length > 0) {\r\n          personaName = parsed[0];\r\n        }\r\n      } catch (e) {\r\n        // Not valid JSON, use as-is\r\n      }\r\n    }\r\n    \r\n    // Ensure personaName is a string\r\n    personaName = String(personaName);\r\n    \r\n    const aiProjectName = agent.ai_project_name || \"Legacy\";\r\n    const flowType = agent.flow_type || \"agent\";\r\n    \r\n    // Build the fully qualified persona name for precise identification\r\n    const personaFqName = `${aiProjectName}/${personaName}`;\r\n    \r\n    console.log('\ud83d\udd0d launchAgenticWorkflows - workflowName:', workflowName, 'personaName:', personaName, 'ai_project_name:', aiProjectName);\r\n\r\n    const cfxAction = {\r\n      type: \"GO_TO_APP_STATE\",\r\n      label: \"Agentic Workflows\",\r\n      appName: \"user-dashboard/workflow_details\",\r\n      pageName: \"user-dashboard/workflow_details\",\r\n      contexts: {\r\n        persona: personaName,\r\n        persona_fq_name: personaFqName,\r\n        agentId: agent.id,\r\n        agentName: agent.name,\r\n        flow_name: workflowName,\r\n        ai_project_name: aiProjectName,\r\n        flow_type: flowType\r\n      }\r\n    };\r\n\r\n    console.log('\ud83d\udd0d launchAgenticWorkflows - cfxAction:', JSON.stringify(cfxAction, null, 2));\r\n    window.top.postMessage(cfxAction);\r\n  }\r\n\r\n  // Function to show persona selection modal\r\n  // options.message - custom message to display (default: \"Choose a persona to interact with:\")\r\n  function showPersonaSelectionModal(agent, callback, options = {}) {\r\n    try {\r\n      const message = options.message || \"Choose a persona to interact with:\";\r\n      console.log('\ud83d\udd0d showPersonaSelectionModal called for agent:', agent?.name || 'unknown');\r\n      let personas = getAgentPersonas(agent);\r\n      console.log('\ud83d\udd0d showPersonaSelectionModal - Agent:', agent.name, 'Personas:', personas);\r\n    \r\n    if (personas.length === 0) {\r\n      // No personas found, use default\r\n      console.log('\u26a0\ufe0f No personas found, using default');\r\n      callback(null);\r\n      return;\r\n    }\r\n    \r\n    // Always show selection modal if there are personas available\r\n    // This allows users to see and select which persona to use\r\n    console.log('\ud83d\udccb Showing persona selection modal with', personas.length, 'persona(s)');\r\n    console.log('\ud83d\udccb Personas array:', personas, 'Type:', typeof personas, 'IsArray:', Array.isArray(personas));\r\n    \r\n    // Ensure personas is an array\r\n    // IMPORTANT: DO NOT TRIM - preserve spaces in persona names\r\n    if (!Array.isArray(personas)) {\r\n      console.error('\u274c Personas is not an array!', personas, 'Type:', typeof personas);\r\n      // Try to convert to array\r\n      if (typeof personas === 'string') {\r\n        const str = personas;\r\n        // Try parsing as JSON array\r\n        if (str.trim().startsWith('[') && str.trim().endsWith(']')) {\r\n          try {\r\n            const parsed = JSON.parse(str);\r\n            if (Array.isArray(parsed)) {\r\n              // DO NOT TRIM - preserve spaces\r\n              personas = parsed.map(p => String(p)).filter(p => p && p.length > 0);\r\n              console.log('\u2705 Parsed personas from JSON string:', personas);\r\n            } else {\r\n              personas = [str];\r\n            }\r\n          } catch (e) {\r\n            console.warn('Failed to parse JSON, trying regex extraction:', e);\r\n            // Try regex extraction for cases like [\"a\", \"b\", \"c\"]\r\n            const matches = str.match(/\"([^\"]+)\"/g);\r\n            if (matches && matches.length > 0) {\r\n              // DO NOT TRIM - preserve spaces\r\n              personas = matches.map(m => m.replace(/\"/g, '')).filter(m => m && m.length > 0);\r\n              console.log('\u2705 Extracted personas using regex:', personas);\r\n            } else if (!str.trim().startsWith('[') && str.includes(',')) {\r\n              // Fallback: split by comma (only if not a JSON array)\r\n              // DO NOT TRIM - preserve spaces\r\n              personas = str.split(',').map(p => p.replace(/^[\"']|[\"']$/g, '')).filter(p => p && p.length > 0);\r\n              console.log('\u2705 Split personas by comma:', personas);\r\n            } else {\r\n              personas = [str];\r\n            }\r\n          }\r\n        } else if (str.includes(',')) {\r\n          // Comma-separated string\r\n          // DO NOT TRIM - preserve spaces\r\n          personas = str.split(',').map(p => p.replace(/^[\"']|[\"']$/g, '')).filter(p => p && p.length > 0);\r\n          console.log('\u2705 Split personas by comma:', personas);\r\n        } else {\r\n          personas = [str];\r\n        }\r\n      } else {\r\n        personas = [String(personas)];\r\n      }\r\n    }\r\n    \r\n    // Final check: ensure all items are strings and filter out empty values\r\n    // DO NOT TRIM - preserve spaces in persona names\r\n    personas = personas.map(p => String(p)).filter(p => p && p.length > 0);\r\n    \r\n    // Special case: if we have a single item that looks like a JSON array string, parse it\r\n    // This handles cases like '[\"DataOps\"]' or '[\"Service Desk\", \"DataOps\"]'\r\n    if (personas.length === 1 && personas[0].trim().startsWith('[') && personas[0].trim().endsWith(']')) {\r\n      try {\r\n        const parsed = JSON.parse(personas[0]);\r\n        // Parse even single-element arrays like [\"DataOps\"] -> [\"DataOps\"]\r\n        if (Array.isArray(parsed) && parsed.length >= 1) {\r\n          // DO NOT TRIM - preserve spaces\r\n          personas = parsed.map(p => String(p)).filter(p => p && p.length > 0);\r\n          console.log('\u2705 Parsed JSON array string into personas:', personas);\r\n        }\r\n      } catch (e) {\r\n        // Try regex extraction\r\n        const matches = personas[0].match(/\"([^\"]+)\"/g);\r\n        if (matches && matches.length > 0) {\r\n          // DO NOT TRIM - preserve spaces\r\n          personas = matches.map(m => m.replace(/\"/g, '')).filter(m => m && m.length > 0);\r\n          console.log('\u2705 Extracted personas from single string using regex:', personas);\r\n        }\r\n      }\r\n    }\r\n    \r\n    console.log('\ud83d\udccb Final personas array for modal:', personas, 'Length:', personas.length);\r\n    \r\n    // If there's only one persona, use it directly without showing modal\r\n    if (personas.length === 1) {\r\n      console.log('\u2705 Only one persona found, using it directly:', personas[0]);\r\n      callback(personas[0]);\r\n      return;\r\n    }\r\n    \r\n    // Multiple personas - show selection modal\r\n    const modal = document.createElement('div');\r\n    modal.className = 'selection-modal';\r\n    \r\n    // Define escape handler function (will be used later)\r\n    let escapeHandler = null;\r\n    \r\n    const closeModal = () => {\r\n      if (modal && modal.parentNode) {\r\n        modal.remove();\r\n      }\r\n      delete window._pendingPersonaCallback;\r\n      if (escapeHandler) {\r\n        document.removeEventListener('keydown', escapeHandler);\r\n      }\r\n    };\r\n    \r\n    // Create selection items with proper event handlers\r\n    // IMPORTANT: DO NOT TRIM persona names - preserve spaces\r\n    const selectionItems = personas.map((persona, idx) => {\r\n      // Ensure persona is a string - DO NOT TRIM (preserve spaces)\r\n      const personaStr = String(persona);\r\n      if (!personaStr || personaStr.length === 0) return null;\r\n      \r\n      const item = document.createElement('div');\r\n      item.className = 'selection-item';\r\n      item.style.cursor = 'pointer';\r\n      \r\n      const icon = document.createElement('div');\r\n      icon.className = 'selection-item-icon';\r\n      icon.innerHTML = `\r\n        <svg width=\"20\" height=\"20\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\">\r\n          <path d=\"M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2\"/>\r\n          <circle cx=\"12\" cy=\"7\" r=\"4\"/>\r\n        </svg>\r\n      `;\r\n      \r\n      const text = document.createElement('div');\r\n      text.className = 'selection-item-text';\r\n      const title = document.createElement('div');\r\n      title.className = 'selection-item-title';\r\n      // Display trimmed for visual purposes, but pass original to callback\r\n      title.textContent = personaStr.trim() || personaStr;\r\n      text.appendChild(title);\r\n      \r\n      item.appendChild(icon);\r\n      item.appendChild(text);\r\n      \r\n      // Add click handler - pass original personaStr (with spaces preserved)\r\n      item.addEventListener('click', () => {\r\n        closeModal();\r\n        // Pass original persona string with spaces preserved\r\n        if (callback) callback(personaStr);\r\n      });\r\n      \r\n      return item;\r\n    }).filter(item => item !== null); // Filter out null items\r\n    \r\n    modal.innerHTML = `\r\n      <div class=\"selection-modal-content\">\r\n        <div class=\"selection-modal-header\">\r\n          <h3>Select Persona</h3>\r\n          <button class=\"selection-modal-close\">&times;</button>\r\n        </div>\r\n        <div class=\"selection-modal-body\">\r\n          <p style=\"margin-bottom: 16px; color: var(--text-secondary);\">${message}</p>\r\n          <div class=\"selection-list\"></div>\r\n        </div>\r\n      </div>\r\n      <div class=\"selection-modal-backdrop\"></div>\r\n    `;\r\n    \r\n    // Append selection items\r\n    const selectionList = modal.querySelector('.selection-list');\r\n    if (!selectionList) {\r\n      console.error('\u274c selection-list element not found!');\r\n      return;\r\n    }\r\n    \r\n    if (selectionItems.length === 0) {\r\n      console.error('\u274c No selection items to display!');\r\n      return;\r\n    }\r\n    \r\n    selectionItems.forEach(item => {\r\n      if (item) selectionList.appendChild(item);\r\n    });\r\n    \r\n    // Define escape handler\r\n    escapeHandler = (e) => {\r\n      if (e.key === 'Escape') {\r\n        closeModal();\r\n      }\r\n    };\r\n    \r\n    // Close button handler\r\n    const closeBtn = modal.querySelector('.selection-modal-close');\r\n    const backdrop = modal.querySelector('.selection-modal-backdrop');\r\n    \r\n    if (closeBtn) {\r\n      closeBtn.addEventListener('click', closeModal);\r\n    }\r\n    if (backdrop) {\r\n      backdrop.addEventListener('click', closeModal);\r\n    }\r\n    \r\n    window._pendingPersonaCallback = callback;\r\n    document.body.appendChild(modal);\r\n    document.addEventListener('keydown', escapeHandler);\r\n    console.log('\u2705 Modal added to DOM');\r\n    } catch (error) {\r\n      console.error('\u274c Error in showPersonaSelectionModal:', error);\r\n      console.error('Error stack:', error.stack);\r\n      alert('Error showing persona selection: ' + error.message);\r\n      // Fallback: call callback with null to proceed without selection\r\n      if (callback) callback(null);\r\n    }\r\n  }\r\n\r\n  // Function to show workflow selection modal\r\n  function showWorkflowSelectionModal(agent, callback) {\r\n    try {\r\n      console.log('\ud83d\udd0d showWorkflowSelectionModal called for agent:', agent?.name || 'unknown');\r\n      let workflows = getAgentWorkflows(agent);\r\n      console.log('\ud83d\udd0d showWorkflowSelectionModal - Agent:', agent.name, 'Workflows (raw):', workflows);\r\n      \r\n      // Ensure workflows is an array (similar to personas) - parse if needed\r\n      // IMPORTANT: DO NOT TRIM - preserve spaces in workflow names\r\n      if (!Array.isArray(workflows)) {\r\n        console.warn('\u26a0\ufe0f Workflows is not an array, attempting to parse:', workflows, 'Type:', typeof workflows);\r\n        // Try to convert to array\r\n        if (typeof workflows === 'string') {\r\n          const str = workflows;\r\n          // Try parsing as JSON array\r\n          if (str.trim().startsWith('[') && str.trim().endsWith(']')) {\r\n            try {\r\n              const parsed = JSON.parse(str);\r\n              if (Array.isArray(parsed)) {\r\n                // DO NOT TRIM - preserve spaces\r\n                workflows = parsed.map(w => String(w)).filter(w => w && w.length > 0);\r\n                console.log('\u2705 Parsed workflows from JSON string:', workflows);\r\n              } else {\r\n                workflows = [str];\r\n              }\r\n            } catch (e) {\r\n              console.warn('Failed to parse JSON, trying regex extraction:', e);\r\n              // Try regex extraction for cases like [\"a\", \"b\", \"c\"]\r\n              const matches = str.match(/\"([^\"]+)\"/g);\r\n              if (matches && matches.length > 0) {\r\n                // DO NOT TRIM - preserve spaces\r\n                workflows = matches.map(m => m.replace(/\"/g, '')).filter(m => m && m.length > 0);\r\n                console.log('\u2705 Extracted workflows using regex:', workflows);\r\n              } else if (!str.trim().startsWith('[') && str.includes(',')) {\r\n                // Fallback: split by comma (only if not a JSON array)\r\n                // DO NOT TRIM - preserve spaces\r\n                workflows = str.split(',').map(w => w.replace(/^[\"']|[\"']$/g, '')).filter(w => w && w.length > 0);\r\n                console.log('\u2705 Split workflows by comma:', workflows);\r\n              } else {\r\n                workflows = [str];\r\n              }\r\n            }\r\n          } else if (str.includes(',')) {\r\n            // Comma-separated string\r\n            // DO NOT TRIM - preserve spaces\r\n            workflows = str.split(',').map(w => w.replace(/^[\"']|[\"']$/g, '')).filter(w => w && w.length > 0);\r\n            console.log('\u2705 Split workflows by comma:', workflows);\r\n          } else {\r\n            workflows = [str];\r\n          }\r\n        } else {\r\n          workflows = [String(workflows)];\r\n        }\r\n      }\r\n      \r\n      // Special case: if we have a single workflow object whose name looks like a JSON array, parse it\r\n      // This handles cases where getAgentWorkflows returns [{name: '[\"workflow1\", \"workflow2\"]', persona: ...}]\r\n      if (workflows.length === 1) {\r\n        const firstWorkflow = workflows[0];\r\n        const workflowName = typeof firstWorkflow === 'object' ? firstWorkflow.name : String(firstWorkflow);\r\n        \r\n        if (workflowName && workflowName.trim().startsWith('[') && workflowName.trim().endsWith(']')) {\r\n          try {\r\n            const parsed = JSON.parse(workflowName);\r\n            if (Array.isArray(parsed) && parsed.length >= 1) {\r\n              // DO NOT TRIM - preserve spaces\r\n              // Convert parsed array to workflow objects\r\n              const defaultPersona = (typeof firstWorkflow === 'object' ? firstWorkflow.persona : null) || agent.ai_persona || agent.persona || null;\r\n              workflows = parsed.map(w => ({ name: String(w), persona: defaultPersona })).filter(w => w.name && w.name.length > 0);\r\n              console.log('\u2705 Parsed single JSON array workflow name into multiple workflows:', workflows);\r\n            }\r\n          } catch (e) {\r\n            // Try regex extraction as fallback\r\n            const matches = workflowName.match(/\"([^\"]+)\"/g);\r\n            if (matches && matches.length > 0) {\r\n              // DO NOT TRIM - preserve spaces\r\n              const defaultPersona = (typeof firstWorkflow === 'object' ? firstWorkflow.persona : null) || agent.ai_persona || agent.persona || null;\r\n              workflows = matches.map(m => ({ name: m.replace(/\"/g, ''), persona: defaultPersona })).filter(w => w.name && w.name.length > 0);\r\n              console.log('\u2705 Extracted workflows from single workflow name using regex:', workflows);\r\n            }\r\n          }\r\n        }\r\n      }\r\n      \r\n      // Ensure all items are workflow objects with name/persona\r\n      // DO NOT TRIM - preserve spaces in workflow names\r\n      workflows = workflows.map(w => {\r\n        if (typeof w === 'object' && w.name) {\r\n          return w; // Already an object\r\n        }\r\n        return { name: String(w), persona: agent.ai_persona || agent.persona || null };\r\n      }).filter(w => w && w.name && w.name.length > 0);\r\n      \r\n      console.log('\ud83d\udd0d showWorkflowSelectionModal - Workflows (final parsed):', workflows);\r\n      \r\n      if (workflows.length === 0) {\r\n        // No workflows found, use default\r\n        console.log('\u26a0\ufe0f No workflows found, using default');\r\n        callback(null, null);\r\n        return;\r\n      }\r\n      \r\n      if (workflows.length === 1) {\r\n        // Only one workflow, use it directly without showing modal\r\n        const workflow = workflows[0];\r\n        const workflowName = typeof workflow === 'object' ? workflow.name : workflow;\r\n        const persona = typeof workflow === 'object' ? workflow.persona : null;\r\n        console.log('\u2705 Only one workflow found, using it directly:', workflowName, 'with persona:', persona);\r\n        callback(workflowName, persona);\r\n        return;\r\n      }\r\n      \r\n      console.log('\ud83d\udccb Showing workflow selection modal with', workflows.length, 'workflow(s)');\r\n    \r\n    // Multiple workflows - show selection modal\r\n    const modal = document.createElement('div');\r\n    modal.className = 'selection-modal';\r\n    \r\n    // Define escape handler function (will be used later)\r\n    let escapeHandler = null;\r\n    \r\n    const closeModal = () => {\r\n      if (modal && modal.parentNode) {\r\n        modal.remove();\r\n      }\r\n      delete window._pendingWorkflowCallback;\r\n      if (escapeHandler) {\r\n        document.removeEventListener('keydown', escapeHandler);\r\n      }\r\n    };\r\n    \r\n    // Create selection items with proper event handlers\r\n    // IMPORTANT: DO NOT TRIM - preserve spaces in workflow/persona names\r\n    const selectionItems = workflows.map((workflow, idx) => {\r\n      const workflowName = typeof workflow === 'object' ? workflow.name : String(workflow);\r\n      const persona = typeof workflow === 'object' ? workflow.persona : null;\r\n      // DO NOT TRIM - preserve spaces\r\n      const workflowNameStr = String(workflowName);\r\n      const personaStr = persona ? String(persona) : null;\r\n      \r\n      if (!workflowNameStr || workflowNameStr.length === 0) return null;\r\n      \r\n      const item = document.createElement('div');\r\n      item.className = 'selection-item';\r\n      item.style.cursor = 'pointer';\r\n      \r\n      const icon = document.createElement('div');\r\n      icon.className = 'selection-item-icon';\r\n      // Robot/AI icon - represents agentic workflows\r\n      icon.innerHTML = `\r\n        <svg width=\"20\" height=\"20\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\">\r\n          <rect x=\"3\" y=\"11\" width=\"18\" height=\"10\" rx=\"2\"></rect>\r\n          <circle cx=\"12\" cy=\"5\" r=\"2\"></circle>\r\n          <path d=\"M12 7v4\"></path>\r\n          <line x1=\"8\" y1=\"16\" x2=\"8\" y2=\"16\"></line>\r\n          <line x1=\"16\" y1=\"16\" x2=\"16\" y2=\"16\"></line>\r\n        </svg>\r\n      `;\r\n      \r\n      const text = document.createElement('div');\r\n      text.className = 'selection-item-text';\r\n      const title = document.createElement('div');\r\n      title.className = 'selection-item-title';\r\n      // Display trimmed for visual purposes, but pass original to callback\r\n      title.textContent = workflowNameStr.trim() || workflowNameStr;\r\n      text.appendChild(title);\r\n      \r\n      item.appendChild(icon);\r\n      item.appendChild(text);\r\n      \r\n      // Add click handler - pass original strings with spaces preserved\r\n      item.addEventListener('click', () => {\r\n        closeModal();\r\n        // Pass original workflow and persona strings with spaces preserved\r\n        if (callback) callback(workflowNameStr, personaStr);\r\n      });\r\n      \r\n      return item;\r\n    }).filter(item => item !== null); // Filter out null items\r\n    \r\n    modal.innerHTML = `\r\n      <div class=\"selection-modal-content\">\r\n        <div class=\"selection-modal-header\">\r\n          <h3>Select Workflow</h3>\r\n          <button class=\"selection-modal-close\">&times;</button>\r\n        </div>\r\n        <div class=\"selection-modal-body\">\r\n          <p style=\"margin-bottom: 16px; color: var(--text-secondary);\">Choose a workflow to view:</p>\r\n          <div class=\"selection-list\"></div>\r\n        </div>\r\n      </div>\r\n      <div class=\"selection-modal-backdrop\"></div>\r\n    `;\r\n    \r\n    // Append selection items\r\n    const selectionList = modal.querySelector('.selection-list');\r\n    if (!selectionList) {\r\n      console.error('\u274c selection-list element not found!');\r\n      return;\r\n    }\r\n    \r\n    if (selectionItems.length === 0) {\r\n      console.error('\u274c No selection items to display!');\r\n      return;\r\n    }\r\n    \r\n    selectionItems.forEach(item => {\r\n      if (item) selectionList.appendChild(item);\r\n    });\r\n    \r\n    // Define escape handler\r\n    escapeHandler = (e) => {\r\n      if (e.key === 'Escape') {\r\n        closeModal();\r\n      }\r\n    };\r\n    \r\n    // Close button handler\r\n    const closeBtn = modal.querySelector('.selection-modal-close');\r\n    const backdrop = modal.querySelector('.selection-modal-backdrop');\r\n    \r\n    if (closeBtn) {\r\n      closeBtn.addEventListener('click', closeModal);\r\n    }\r\n    if (backdrop) {\r\n      backdrop.addEventListener('click', closeModal);\r\n    }\r\n    \r\n    window._pendingWorkflowCallback = callback;\r\n    document.body.appendChild(modal);\r\n    document.addEventListener('keydown', escapeHandler);\r\n    console.log('\u2705 Workflow modal added to DOM');\r\n    console.log('\u2705 Workflow modal added to DOM');\r\n    } catch (error) {\r\n      console.error('\u274c Error in showWorkflowSelectionModal:', error);\r\n      console.error('Error stack:', error.stack);\r\n      alert('Error showing workflow selection: ' + error.message);\r\n      // Fallback: call callback with null to proceed without selection\r\n      if (callback) callback(null, null);\r\n    }\r\n  }\r\n\r\n  window.launchAgenticWorkflowsById = function(agentId) {\r\n    try {\r\n      console.log('\ud83d\udd0d launchAgenticWorkflowsById called with agentId:', agentId);\r\n      if (!agentId) {\r\n        console.warn('\u26a0\ufe0f No agentId provided');\r\n        return;\r\n      }\r\n      const a = agents.find(x => x.id === agentId);\r\n      if (!a) {\r\n        console.error('\u274c Agent not found with id:', agentId);\r\n        console.log('Available agents:', agents.map(ag => ({ id: ag.id, name: ag.name })));\r\n        return;\r\n      }\r\n      console.log('\ud83d\udd0d Launching workflows for agent:', a.name);\r\n      const availableWorkflows = getAgentWorkflows(a);\r\n      const availablePersonas = getAgentPersonas(a);\r\n      console.log('\ud83d\udd0d Available workflows:', availableWorkflows);\r\n      console.log('\ud83d\udd0d Available personas:', availablePersonas);\r\n      \r\n      // Show workflow selection modal first\r\n      showWorkflowSelectionModal(a, (workflowName, workflowPersona) => {\r\n        console.log('\u2705 Selected workflow:', workflowName, 'with persona from workflow:', workflowPersona);\r\n        \r\n        // If there are multiple personas and no specific persona from workflow,\r\n        // show persona selection modal after workflow selection\r\n        if (availablePersonas.length > 1 && !workflowPersona) {\r\n          console.log('\ud83d\udccb Multiple personas available, showing persona selection...');\r\n          showPersonaSelectionModal(a, (selectedPersona) => {\r\n            console.log('\u2705 Selected persona for workflow:', selectedPersona);\r\n            launchAgenticWorkflows(a, workflowName, selectedPersona);\r\n          }, { message: \"Choose a persona for this workflow:\" });\r\n        } else {\r\n          // Use the workflow's persona or let launchAgenticWorkflows handle the fallback\r\n          launchAgenticWorkflows(a, workflowName, workflowPersona);\r\n        }\r\n      });\r\n    } catch (error) {\r\n      console.error('\u274c Error in launchAgenticWorkflowsById:', error);\r\n      alert('Error launching workflows: ' + error.message);\r\n    }\r\n  };\r\n\r\n  window.launchfabaioById = function(agentId){\r\n    try {\r\n      console.log('\ud83d\udd0d launchfabaioById called with agentId:', agentId);\r\n      if(!agentId) {\r\n        console.warn('\u26a0\ufe0f No agentId provided');\r\n        return;\r\n      }\r\n      const a = agents.find(x => x.id === agentId);\r\n      if(!a) {\r\n        console.error('\u274c Agent not found with id:', agentId);\r\n        console.log('Available agents:', agents.map(ag => ({ id: ag.id, name: ag.name })));\r\n        return;\r\n      }\r\n      console.log('\ud83d\udd0d Launching fabaio for agent:', a.name);\r\n      console.log('\ud83d\udd0d Agent details:', {\r\n        id: a.id,\r\n        name: a.name,\r\n        ai_project_name: a.ai_project_name,\r\n        ai_persona: a.ai_persona,\r\n        ai_personas: a.ai_personas,\r\n        personas: a.personas,\r\n        persona: a.persona\r\n      });\r\n      const availablePersonas = getAgentPersonas(a);\r\n      console.log('\ud83d\udd0d Available personas from getAgentPersonas:', availablePersonas);\r\n      showPersonaSelectionModal(a, (persona) => {\r\n        console.log('\u2705 Selected persona from modal:', persona);\r\n        console.log('\u2705 Agent ai_project_name:', a.ai_project_name);\r\n        launchfabaio(a, persona);\r\n      });\r\n    } catch (error) {\r\n      console.error('\u274c Error in launchfabaioById:', error);\r\n      alert('Error launching agent: ' + error.message);\r\n    }\r\n  };\r\n\r\n  window.launchAIPersonaObserveDashboardById = function(agentId){\r\n    try {\r\n      console.log('\ud83d\udd0d launchAIPersonaObserveDashboardById called with agentId:', agentId);\r\n      if(!agentId) {\r\n        console.warn('\u26a0\ufe0f No agentId provided');\r\n        return;\r\n      }\r\n      const a = agents.find(x => x.id === agentId);\r\n      if(!a) {\r\n        console.error('\u274c Agent not found with id:', agentId);\r\n        return;\r\n      }\r\n      console.log('\ud83d\udd0d Launching observe dashboard for agent:', a.name);\r\n      const availablePersonas = getAgentPersonas(a);\r\n      console.log('\ud83d\udd0d Available personas:', availablePersonas);\r\n      \r\n      showPersonaSelectionModal(a, (persona) => {\r\n        console.log('\u2705 Selected persona for observe:', persona);\r\n        launchAIPersonaObserveDashboard(a, persona);\r\n      }, { message: \"Choose a persona to observe:\" });\r\n    } catch (error) {\r\n      console.error('\u274c Error in launchAIPersonaObserveDashboardById:', error);\r\n      alert('Error launching observe dashboard: ' + error.message);\r\n    }\r\n  };\r\n\r\n  async function openPane(agent){\r\n    // Open the pane immediately\r\n    $('#paneIcon').innerHTML = '';\r\n    const iconContainer = document.getElementById('paneIcon');\r\n    const placeholderIcon = 'data:image/svg+xml;base64,';\r\n    const defaultIconUrl = \"https://www.fabrix.ai/images/icons/ai_agent_icon.svg\";\r\n    const iconUrl = agent.icon || agent.iconUrl || defaultIconUrl;\r\n    const isDefaultIcon = !agent.icon && !agent.iconUrl;\r\n    const bgColor = isDefaultIcon ? '#000' : 'transparent';\r\n    \r\n    iconContainer.innerHTML = `<div class=\"pane-icon-container\" style=\"width:54px;height:54px;border-radius:4px;background-color:${bgColor};display:flex;align-items:center;justify-content:center;padding:0;overflow:hidden;\"><img src=\"${iconUrl}\" alt=\"Agent Icon\" class=\"pane-icon-img\" style=\"width:100%;height:100%;object-fit:contain;object-position:center;display:block;\" onerror=\"this.src='${defaultIconUrl}'; this.parentElement.style.backgroundColor='#000';\"></div>`;\r\n    $('#paneTitle').textContent = agent.name;\r\n    \r\n    // Remove old badge if exists\r\n    const oldBadge = $('#paneBadge');\r\n    if(oldBadge) oldBadge.remove();\r\n    // Tags\r\n    const tagsContainer = $('#paneTags');\r\n    tagsContainer.innerHTML = '';\r\n    const categoryTag = document.createElement('span');\r\n    categoryTag.className = 'pane-tag';\r\n    categoryTag.textContent = agent.category;\r\n    tagsContainer.appendChild(categoryTag);\r\n    agent.tags.forEach(tag => {\r\n      const tagEl = document.createElement('span');\r\n      tagEl.className = 'pane-tag';\r\n      tagEl.textContent = tag;\r\n      tagsContainer.appendChild(tagEl);\r\n    });\r\n\r\n    // Check file existence BEFORE rendering tabs to prevent flash\r\n    // This ensures tabs only show if files actually exist\r\n    const checks = [];\r\n    \r\n    if (agent.images_path && !agent._imagesInitialized && !agent._imagesLoading) {\r\n      checks.push(\r\n        getImageFileList(agent).then(imageFiles => {\r\n          agent._imageFiles = imageFiles;\r\n          if (imageFiles.length === 0) {\r\n            agent._imagesInitialized = true; // Mark as checked, no files\r\n          }\r\n          return { type: 'images', hasFiles: imageFiles.length > 0 };\r\n        }).catch(() => {\r\n          agent._imagesInitialized = true; // Mark as checked on error\r\n          return { type: 'images', hasFiles: false };\r\n        })\r\n      );\r\n    }\r\n    \r\n    if (agent.docs_path && !agent._docsLoaded) {\r\n      checks.push(\r\n        fetchFileList(agent.docs_path).then(docFiles => {\r\n          if (docFiles.length === 0) {\r\n            agent._docsLoaded = true; // Mark as checked, no files\r\n          }\r\n          return { type: 'docs', hasFiles: docFiles.length > 0 };\r\n        }).catch(() => {\r\n          agent._docsLoaded = true; // Mark as checked on error\r\n          return { type: 'docs', hasFiles: false };\r\n        })\r\n      );\r\n    }\r\n    \r\n    if (agent.videos_path && !agent._videosLoaded && !agent._videosLoading) {\r\n      checks.push(\r\n        fetchFileList(agent.videos_path).then(videoFiles => {\r\n          const videoFilesFiltered = videoFiles.filter(f => {\r\n            const fileType = (f.file_type || '').toLowerCase();\r\n            return fileType === 'mp4' || fileType === 'webm' || fileType === 'mov' || fileType === 'avi';\r\n          });\r\n          if (videoFilesFiltered.length === 0) {\r\n            agent._videosLoaded = true; // Mark as checked, no files\r\n          }\r\n          return { type: 'videos', hasFiles: videoFilesFiltered.length > 0 };\r\n        }).catch(() => {\r\n          agent._videosLoaded = true; // Mark as checked on error\r\n          return { type: 'videos', hasFiles: false };\r\n        })\r\n      );\r\n    }\r\n    \r\n    // Wait for all checks to complete\r\n    await Promise.allSettled(checks);\r\n    \r\n    // Now determine which tabs to show based on actual file existence\r\n    const hasDocs = Array.isArray(agent.documents) && agent.documents.length > 0;\r\n    const hasDocsPath = agent.docs_path && agent.docs_path.trim() !== '' && !agent._docsLoaded;\r\n    const shouldShowDocsTab = hasDocs || hasDocsPath;\r\n    \r\n    const hasImages = Array.isArray(agent.images) && agent.images.length > 0;\r\n    const hasImagesPath = agent.images_path && agent.images_path.trim() !== '' && !agent._imagesInitialized;\r\n    const shouldShowImagesTab = hasImages || hasImagesPath;\r\n    \r\n    const hasVideos = Array.isArray(agent.videos) && agent.videos.length > 0;\r\n    const hasVideosPath = agent.videos_path && agent.videos_path.trim() !== '' && !agent._videosLoaded;\r\n    const shouldShowVideosTab = hasVideos || hasVideosPath;\r\n\r\n    const contactItems = (() => {\r\n      const items = [];\r\n      if(Array.isArray(agent.contact)){\r\n        agent.contact.forEach(c => {\r\n          if(c.email) items.push({ type:'email', value:c.email, label:c.name || 'Email' });\r\n          if(c.phone) items.push({ type:'phone', value:c.phone, label:c.name || 'Phone' });\r\n          if(c.website) items.push({ type:'website', value:c.website, label:c.name || 'Website' });\r\n          if(c.chat) items.push({ type:'chat', value:c.chat, label:c.name || 'Live Chat' });\r\n        });\r\n      } else if(agent.contact && typeof agent.contact === 'object'){\r\n        const c = agent.contact;\r\n        if(c.email) items.push({ type:'email', value:c.email, label:'Email' });\r\n        if(c.phone) items.push({ type:'phone', value:c.phone, label:'Phone' });\r\n        if(c.website) items.push({ type:'website', value:c.website, label:'Website' });\r\n        if(c.chat) items.push({ type:'chat', value:c.chat, label:'Live Chat' });\r\n      }\r\n      return items;\r\n    })();\r\n    const hasContact = contactItems.length > 0;\r\n    const hasRelated = Array.isArray(agent.related) && agent.related.length > 0;\r\n\r\n    // Only add tabs if content exists (we've already checked file existence above)\r\n    const tabs = [];\r\n    if(shouldShowDocsTab) tabs.push('documents');\r\n    if(shouldShowImagesTab) tabs.push('images');\r\n    if(shouldShowVideosTab) tabs.push('videos');\r\n    if(hasContact) tabs.push('contact');\r\n    if(hasRelated) tabs.push('related');\r\n\r\n    // Pick first available as active, else none\r\n    const initialTab = tabs[0] || null;\r\n    activeTab = initialTab || 'documents';\r\n\r\n    // Render the static content (description, details, action buttons) immediately\r\n    const staticContent = `\r\n      <div class=\"detail-section\">\r\n        <div class=\"detail-title\">Description</div>\r\n        <div class=\"detail-value\">\r\n          ${agent.description || `This ${agent.category} agent provides automated monitoring and management capabilities for your infrastructure. It integrates seamlessly with your existing systems to deliver real-time insights and automated responses.`}\r\n        </div>\r\n      </div>\r\n\r\n      <div class=\"detail-grid\">\r\n        <div class=\"detail-item\">\r\n          <div class=\"detail-item-label\">Version</div>\r\n          <div class=\"detail-item-value\">${agent.version}</div>\r\n        </div>\r\n        <div class=\"detail-item\">\r\n          <div class=\"detail-item-label\">Publisher</div>\r\n          <div class=\"detail-item-value\">${agent.publisher}</div>\r\n        </div>\r\n        <div class=\"detail-item\">\r\n          <div class=\"detail-item-label\">Category</div>\r\n          <div class=\"detail-item-value\">${agent.category}</div>\r\n        </div>\r\n        <div class=\"detail-item\">\r\n          <div class=\"detail-item-label\">Status</div>\r\n          <div class=\"detail-item-value\" style=\"color:${agent.state === 'Enabled' ? 'var(--green)' : 'var(--red)'}\">${agent.state}</div>\r\n        </div>\r\n      </div>\r\n\r\n      <div class=\"action-buttons\">\r\n        ${agent.hasInteractiveMode ? `\r\n        <button class=\"action-btn\" onclick=\"launchfabaioById('${agent.id}')\">\r\n          Interact with Agent\r\n        </button>` : ''}\r\n        ${agent.hasWorkflowMode ? `\r\n        <button class=\"action-btn\" onclick=\"launchAgenticWorkflowsById('${agent.id}')\">\r\n          View Agentic Workflows\r\n        </button>` : ''}\r\n        <button class=\"action-btn\" onclick=\"launchAIPersonaObserveDashboardById('${agent.id}')\">Observe</button>\r\n      </div>\r\n\r\n      <div class=\"tabs-wrapper\"></div>\r\n    `;\r\n    \r\n    // Set the static content immediately\r\n    $('#paneContent').innerHTML = staticContent;\r\n    \r\n    // Open the pane immediately\r\n    $('#sidePane').classList.add('open');\r\n    $('.main-content').classList.add('pane-open');\r\n    $('#backdrop').classList.add('active');\r\n    \r\n    // Render tabs AFTER checking files (so tabs only show if files exist)\r\n    renderPaneContent(agent, initialTab, tabs, shouldShowDocsTab, shouldShowImagesTab, shouldShowVideosTab, hasContact, hasRelated, contactItems, hasImagesPath, hasDocsPath, hasVideosPath);\r\n    \r\n    // Update tab navigation after rendering\r\n    tabScrollPosition = 0;\r\n    setTimeout(updateTabNavigation, 100);\r\n    \r\n    // Now start loading files in the background (non-blocking)\r\n    // Load docs immediately in the background (non-blocking)\r\n    loadMinioDocsForAgent(agent).then(() => {\r\n      // Update the pane content when docs are loaded\r\n      updatePaneContent(agent);\r\n    });\r\n    \r\n    // Load images immediately in the background (non-blocking)\r\n    if (agent.images_path && !agent._imagesInitialized && !agent._imagesLoading && agent._imageFiles && agent._imageFiles.length > 0) {\r\n      agent._imagesLoading = true;\r\n      // Then load actual images\r\n      loadMinioImagesForAgent(agent).then(() => {\r\n        agent._imagesLoading = false;\r\n        // Always update tabs after loading\r\n        if (window.updateTabsAfterLoad) {\r\n          window.updateTabsAfterLoad(agent);\r\n        }\r\n        // Re-render with pagination once all images are loaded (if images exist)\r\n        if (agent.images && agent.images.length > 0) {\r\n          const imagesTabContent = $(`.tab-content[data-tab=\"images\"]`);\r\n          if (imagesTabContent) {\r\n            renderImagesWithPagination(agent, imagesTabContent).catch(err => {\r\n              console.error('Error rendering images:', err);\r\n            });\r\n          }\r\n        }\r\n      });\r\n    }\r\n    \r\n    // Load videos immediately when pane opens (similar to images and documents)\r\n    if (agent.videos_path && !agent._videosLoaded && !agent._videosLoading) {\r\n      agent._videosLoading = true;\r\n      loadMinioVideosForAgent(agent).then(() => {\r\n        agent._videosLoading = false;\r\n        // Always update tabs after loading\r\n        if (window.updateTabsAfterLoad) {\r\n          window.updateTabsAfterLoad(agent);\r\n        }\r\n      });\r\n    }\r\n  }\r\n  \r\n  // Helper function to update tabs dynamically after content is loaded\r\n  window.updateTabsAfterLoad = function(agent) {\r\n      // Check if content exists or is loading before showing tabs\r\n      const hasDocs = Array.isArray(agent.documents) && agent.documents.length > 0;\r\n      const hasDocsPath = agent.docs_path && agent.docs_path.trim() !== '' && !agent._docsLoaded;\r\n      const shouldShowDocsTab = hasDocs || hasDocsPath;\r\n      \r\n      const hasImages = Array.isArray(agent.images) && agent.images.length > 0;\r\n      const hasImagesPath = agent.images_path && agent.images_path.trim() !== '' && !agent._imagesInitialized;\r\n      const shouldShowImagesTab = hasImages || hasImagesPath;\r\n      \r\n      const hasVideos = Array.isArray(agent.videos) && agent.videos.length > 0;\r\n      const hasVideosPath = agent.videos_path && agent.videos_path.trim() !== '' && !agent._videosLoaded;\r\n      const shouldShowVideosTab = hasVideos || hasVideosPath;\r\n      \r\n      const contactItemsLocal = (() => {\r\n        const items = [];\r\n        if(Array.isArray(agent.contact)){\r\n          agent.contact.forEach(c => {\r\n            if(c.email) items.push({ type:'email', value:c.email, label:c.name || 'Email' });\r\n            if(c.phone) items.push({ type:'phone', value:c.phone, label:c.name || 'Phone' });\r\n            if(c.website) items.push({ type:'website', value:c.website, label:c.name || 'Website' });\r\n            if(c.chat) items.push({ type:'chat', value:c.chat, label:c.name || 'Live Chat' });\r\n          });\r\n        } else if(agent.contact && typeof agent.contact === 'object'){\r\n          const c = agent.contact;\r\n          if(c.email) items.push({ type:'email', value:c.email, label:'Email' });\r\n          if(c.phone) items.push({ type:'phone', value:c.phone, label:'Phone' });\r\n          if(c.website) items.push({ type:'website', value:c.website, label:'Website' });\r\n          if(c.chat) items.push({ type:'chat', value:c.chat, label:'Live Chat' });\r\n        }\r\n        return items;\r\n      })();\r\n      const hasContact = contactItemsLocal.length > 0;\r\n      const hasRelated = Array.isArray(agent.related) && agent.related.length > 0;\r\n      \r\n      // Only add tabs if content exists or is loading\r\n      const newTabs = [];\r\n      if(shouldShowDocsTab) newTabs.push('documents');\r\n      if(shouldShowImagesTab) newTabs.push('images');\r\n      if(shouldShowVideosTab) newTabs.push('videos');\r\n      if(hasContact) newTabs.push('contact');\r\n      if(hasRelated) newTabs.push('related');\r\n      \r\n      // Get current tabs from DOM (preserve order)\r\n      const currentTabs = Array.from(document.querySelectorAll('.tab-btn')).map(btn => btn.dataset.tab);\r\n      // Compare without sorting to preserve order\r\n      const tabsChanged = JSON.stringify(newTabs) !== JSON.stringify(currentTabs);\r\n      \r\n      // Always update tabs if they changed (not just for images/videos)\r\n      if (tabsChanged) {\r\n        // Re-render pane content with updated tabs\r\n        // Preserve the current active tab instead of resetting to first tab\r\n        const currentActiveTab = activeTab && newTabs.includes(activeTab) ? activeTab : (newTabs[0] || null);\r\n        activeTab = currentActiveTab || 'documents';\r\n        renderPaneContent(agent, currentActiveTab, newTabs, shouldShowDocsTab, shouldShowImagesTab, shouldShowVideosTab, hasContact, hasRelated, contactItemsLocal, hasImagesPath, hasDocsPath, hasVideosPath);\r\n        setTimeout(updateTabNavigation, 100);\r\n      }\r\n    };\r\n\r\n  // Helper function to format file size\r\n  function formatFileSize(bytes) {\r\n    if (!bytes || bytes === 0) return '';\r\n    const k = 1024;\r\n    const sizes = ['Bytes', 'KB', 'MB', 'GB'];\r\n    const i = Math.floor(Math.log(bytes) / Math.log(k));\r\n    return Math.round(bytes / Math.pow(k, i) * 100) / 100 + ' ' + sizes[i];\r\n  }\r\n\r\n  // Function to render/update pane content (called initially and after media loads)\r\n  function renderPaneContent(agent, initialTab, tabs, shouldShowDocsTab, shouldShowImagesTab, shouldShowVideosTab, hasContact, hasRelated, contactItems, hasImagesPath = false, hasDocsPath = false, hasVideosPath = false) {\r\n    function renderTabsNav(){\r\n      if(tabs.length === 0) return '';\r\n      const labelMap = { documents:'Documents', images:'Images', videos:'Videos', contact:'Contact', related:'Related' };\r\n      const buttons = tabs.map(tab => `<button class=\"tab-btn ${tab===initialTab?'active':''}\" data-tab=\"${tab}\" onclick=\"switchTabGlobal('${tab}')\">${labelMap[tab] || (tab.charAt(0).toUpperCase()+tab.slice(1))}</button>`).join('');\r\n      return `\r\n        <div class=\"tabs-nav-container\">\r\n          <button class=\"tab-nav-btn left\" onclick=\"scrollTabsGlobal('left')\">\r\n            <svg viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\"><polyline points=\"15 18 9 12 15 6\"/></svg>\r\n          </button>\r\n          <div class=\"tabs-nav\"><div class=\"tabs-scroll\">${buttons}</div></div>\r\n          <button class=\"tab-nav-btn right\" onclick=\"scrollTabsGlobal('right')\">\r\n            <svg viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\"><polyline points=\"9 18 15 12 9 6\"/></svg>\r\n          </button>\r\n        </div>`;\r\n    }\r\n\r\n    function renderDocs() {\r\n      // Render if there are docs OR if docs_path exists AND not finished loading (will show loading/empty state)\r\n      const currentHasDocs = Array.isArray(agent.documents) && agent.documents.length > 0;\r\n      const currentHasDocsPath = agent.docs_path && agent.docs_path.trim() !== '' && !agent._docsLoaded;\r\n      \r\n      // Don't render if no docs and either no path OR path was checked and found empty\r\n      if(!currentHasDocs && (!hasDocsPath || (agent._docsLoaded && !currentHasDocs))) return '';\r\n      \r\n      // If no docs loaded yet but path exists and not initialized, show loading state\r\n      if(!currentHasDocs && currentHasDocsPath && !agent._docsLoaded) {\r\n        // Docs not loaded yet - show loading state with placeholders\r\n        return `\r\n          <div class=\"tab-content ${initialTab === 'documents' ? 'active' : ''}\" data-tab=\"documents\">\r\n            <div class=\"tab-section\">\r\n              <div class=\"tab-section-title\">Product Documents</div>\r\n              <div class=\"document-list\" style=\"text-align:center;padding:40px;color:var(--text-muted);\">\r\n                <div style=\"display:flex;align-items:center;justify-content:center;gap:12px;\">\r\n                  <svg width=\"24\" height=\"24\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" style=\"animation:spin 1s linear infinite;\">\r\n                    <circle cx=\"12\" cy=\"12\" r=\"10\" stroke-opacity=\"0.25\"/>\r\n                    <path d=\"M12 2 A10 10 0 0 1 22 12\" stroke-linecap=\"round\"/>\r\n                  </svg>\r\n                  <span>Loading documents...</span>\r\n                </div>\r\n              </div>\r\n            </div>\r\n          </div>`;\r\n      }\r\n      \r\n      const items = agent.documents.map(doc => {\r\n        const isExternal = doc.url && doc.url.startsWith('http');\r\n        const fileType = (doc.type || '').toLowerCase();\r\n        const fileSize = doc.size ? formatFileSize(doc.size) : '';\r\n        if (isExternal) {\r\n          return `\r\n            <a class=\"document-item\" href=\"${doc.url}\" target=\"_blank\" rel=\"noopener noreferrer\">\r\n              <div class=\"document-icon\">\r\n                <svg viewBox=\"0 0 24 24\">\r\n                  <path d=\"M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z\"/>\r\n                  <polyline points=\"14 2 14 8 20 8\"/>\r\n                </svg>\r\n              </div>\r\n              <div class=\"document-info\">\r\n                <div class=\"document-name\">${doc.name || 'Document'}</div>\r\n                <div class=\"document-meta\">${[fileType, fileSize].filter(Boolean).join(' \u2022 ')}</div>\r\n              </div>\r\n            </a>`;\r\n        }\r\n        return `\r\n          <div class=\"document-item\" style=\"cursor:pointer\"\r\n               onclick=\"openDocModal('${doc.url || ''}', '${(doc.name || 'Document').replace(/'/g, \"\\\\'\")}', '${fileType}')\">\r\n            <div class=\"document-icon\">\r\n              <svg viewBox=\"0 0 24 24\">\r\n                <path d=\"M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z\"/>\r\n                <polyline points=\"14 2 14 8 20 8\"/>\r\n              </svg>\r\n            </div>\r\n            <div class=\"document-info\">\r\n              <div class=\"document-name\">${doc.name || 'Document'}</div>\r\n              <div class=\"document-meta\">${[fileType, fileSize].filter(Boolean).join(' \u2022 ') || 'File'}</div>\r\n            </div>\r\n          </div>`;\r\n      }).join('');\r\n      return `\r\n        <div class=\"tab-content ${initialTab === 'documents' ? 'active' : ''}\" data-tab=\"documents\">\r\n          <div class=\"tab-section\">\r\n            <div class=\"tab-section-title\">Product Documents</div>\r\n            <div class=\"document-list\">${items}</div>\r\n          </div>\r\n        </div>`;\r\n    }\r\n\r\n    function renderImages(){\r\n      // Render if there are images OR if images_path exists AND not finished loading (will show loading/empty state)\r\n      const currentHasImages = Array.isArray(agent.images) && agent.images.length > 0;\r\n      const currentHasImagesPath = agent.images_path && agent.images_path.trim() !== '' && !agent._imagesInitialized;\r\n      \r\n      // Don't render if no images and either no path OR path was checked and found empty\r\n      if(!currentHasImages && (!hasImagesPath || (agent._imagesInitialized && !currentHasImages))) return '';\r\n      \r\n      // If no images loaded yet but path exists and not initialized, show loading state with placeholders\r\n      if(!currentHasImages && currentHasImagesPath && !agent._imagesInitialized) {\r\n        // Show 6 placeholder cards with spinners\r\n        const placeholders = Array.from({ length: 6 }, (_, idx) => `\r\n          <div class=\"gallery-item\" style=\"position:relative;min-height:120px;background:var(--cfx3rdlevel);display:flex;align-items:center;justify-content:center;\">\r\n            <div class=\"image-loading-spinner\" id=\"spinner-${agent.id}-${idx}\">\r\n              <svg width=\"32\" height=\"32\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"var(--primary, #7dcfff)\" stroke-width=\"2\">\r\n                <circle cx=\"12\" cy=\"12\" r=\"10\" stroke-opacity=\"0.25\"/>\r\n                <path d=\"M12 2 A10 10 0 0 1 22 12\" stroke-linecap=\"round\"/>\r\n              </svg>\r\n            </div>\r\n          </div>\r\n        `).join('');\r\n        \r\n        return `\r\n          <div class=\"tab-content ${initialTab === 'images' ? 'active' : ''}\" data-tab=\"images\">\r\n            <div class=\"tab-section\">\r\n              <div class=\"tab-section-title\">Screenshots & Diagrams</div>\r\n              <div class=\"image-gallery\">${placeholders}</div>\r\n            </div>\r\n          </div>`;\r\n      }\r\n      \r\n      // Use agent.images - render with pagination synchronously\r\n      // renderImagesWithPagination will return HTML string when container is null\r\n      const imageFiles = agent._imageFiles || [];\r\n      const imagesToRender = agent.images || [];\r\n      const totalImageCount = imageFiles.length > 0 ? imageFiles.length : imagesToRender.length;\r\n      const imagesPerPage = 6;\r\n      const totalPages = Math.ceil(totalImageCount / imagesPerPage);\r\n      \r\n      // Initialize pagination state for this agent if not exists\r\n      if (!agent._imagePage) agent._imagePage = 1;\r\n      const currentPage = agent._imagePage;\r\n      const startIdx = (currentPage - 1) * imagesPerPage;\r\n      const endIdx = startIdx + imagesPerPage;\r\n      \r\n      const defaultPlaceholder = 'data:image/svg+xml;base64,';\r\n      \r\n      // Generate placeholders for current page\r\n      // If we have imageFiles metadata, use that; otherwise use loaded images\r\n      let pageImageFiles = [];\r\n      if (imageFiles.length > 0) {\r\n        pageImageFiles = imageFiles.slice(startIdx, endIdx);\r\n      } else if (imagesToRender.length > 0) {\r\n        pageImageFiles = imagesToRender.slice(startIdx, endIdx);\r\n      }\r\n      \r\n      const items = pageImageFiles.map((fileOrImg, idx) => {\r\n        const globalIdx = startIdx + idx;\r\n        // Check if it's a file object (has .name property that's a path) or an image object\r\n        const isFile = fileOrImg && typeof fileOrImg === 'object' && fileOrImg.name && fileOrImg.name.includes('/');\r\n        const fileName = isFile ? fileOrImg.name.split('/').pop() : (fileOrImg?.name || 'Image');\r\n        const loadedImage = imagesToRender[globalIdx];\r\n        const imageName = loadedImage?.imageName || loadedImage?.name || agent.name || 'Image';\r\n        const imageAlt = loadedImage?.alt || agent.name || 'Agent Image';\r\n        const imgId = `img-${agent.id}-${globalIdx}`;\r\n        const hasLoadedImage = loadedImage && loadedImage.url && loadedImage.url !== defaultPlaceholder;\r\n        \r\n        // Only set imageUrl if image is loaded, otherwise use empty string to avoid showing blue placeholder\r\n        const imageUrl = hasLoadedImage ? loadedImage.url : '';\r\n        \r\n        // If image hasn't loaded yet, show only spinner (no img tag - loadSingleImage will create it when ready)\r\n        if (!hasLoadedImage) {\r\n          return `\r\n          <div class=\"gallery-item\" style=\"position:relative;min-height:120px;background:var(--cfx3rdlevel);display:flex;align-items:center;justify-content:center;\">\r\n            <div class=\"image-loading-spinner\" id=\"spinner-${agent.id}-${globalIdx}\" style=\"display:flex;align-items:center;justify-content:center;\">\r\n              <svg width=\"32\" height=\"32\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"var(--primary, #7dcfff)\" stroke-width=\"2\">\r\n                <circle cx=\"12\" cy=\"12\" r=\"10\" stroke-opacity=\"0.25\"/>\r\n                <path d=\"M12 2 A10 10 0 0 1 22 12\" stroke-linecap=\"round\"/>\r\n              </svg>\r\n            </div>\r\n          </div>\r\n        `;\r\n        }\r\n        \r\n        // Image is loaded, render normally\r\n        return `\r\n        <div class=\"gallery-item\" style=\"position:relative;min-height:120px;\">\r\n          <img id=\"${imgId}\"\r\n              src=\"${imageUrl}\"\r\n              alt=\"${imageAlt}\" \r\n              style=\"width:100%;height:100%;object-fit:cover;border-radius:8px;transition:opacity 0.3s ease;\"\r\n              onerror=\"this.onerror=null; this.src='${defaultPlaceholder}'; this.style.display='block';\"\r\n              onclick=\"openImageModal(this.src, '${imageName.replace(/'/g, \"\\\\'\")}')\">\r\n        </div>\r\n      `;\r\n      }).join('');\r\n      \r\n      // Pagination controls\r\n      let paginationHTML = '';\r\n      if (totalPages > 1) {\r\n        const prevDisabled = currentPage === 1 ? 'disabled' : '';\r\n        const nextDisabled = currentPage === totalPages ? 'disabled' : '';\r\n        const pageNumbers = [];\r\n        for (let i = 1; i <= totalPages; i++) {\r\n          const active = i === currentPage ? 'active' : '';\r\n          pageNumbers.push(`<button class=\"page-btn ${active}\" onclick=\"changeImagePage('${agent.id}', ${i})\">${i}</button>`);\r\n        }\r\n        \r\n        paginationHTML = `\r\n          <div class=\"image-pagination\">\r\n            <button class=\"pagination-btn\" ${prevDisabled} onclick=\"changeImagePage('${agent.id}', ${currentPage - 1})\" ${prevDisabled ? 'disabled' : ''}>\r\n              <svg width=\"16\" height=\"16\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\">\r\n                <polyline points=\"15 18 9 12 15 6\"/>\r\n              </svg>\r\n              Previous\r\n            </button>\r\n            <div class=\"page-numbers\">${pageNumbers.join('')}</div>\r\n            <button class=\"pagination-btn\" ${nextDisabled} onclick=\"changeImagePage('${agent.id}', ${currentPage + 1})\" ${nextDisabled ? 'disabled' : ''}>\r\n              Next\r\n              <svg width=\"16\" height=\"16\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\">\r\n                <polyline points=\"9 18 15 12 9 6\"/>\r\n              </svg>\r\n            </button>\r\n          </div>`;\r\n      }\r\n      \r\n      return `\r\n        <div class=\"tab-content ${initialTab==='images'?'active':''}\" data-tab=\"images\">\r\n          <div class=\"tab-section\">\r\n            <div class=\"tab-section-title\">Screenshots & Diagrams ${totalImageCount > 0 ? `(${totalImageCount} total)` : ''}</div>\r\n            <div class=\"image-gallery\">${items}</div>\r\n            ${paginationHTML}\r\n          </div>\r\n        </div>`;\r\n    }\r\n    \r\n    // Helper function to render images with pagination\r\n    window.renderImagesWithPagination = async function(agent, container, initialTab = '') {\r\n      // Use image file list metadata if available, otherwise fetch it if images_path exists\r\n      let imageFiles = agent._imageFiles || [];\r\n      \r\n      // If _imageFiles is not populated but images_path exists, fetch the file list\r\n      if (imageFiles.length === 0 && agent.images_path && !agent._imagesLoading) {\r\n        // Show loading placeholders with spinners if container is provided\r\n        if (container && agent.images_path) {\r\n          // Show 6 placeholder cards with spinners\r\n          const placeholders = Array.from({ length: 6 }, (_, idx) => `\r\n            <div class=\"gallery-item\" style=\"position:relative;min-height:120px;background:var(--cfx3rdlevel);display:flex;align-items:center;justify-content:center;\">\r\n              <div class=\"image-loading-spinner\" id=\"spinner-${agent.id}-${idx}\" style=\"display:block;\">\r\n                <svg width=\"32\" height=\"32\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"var(--primary, #7dcfff)\" stroke-width=\"2\">\r\n                  <circle cx=\"12\" cy=\"12\" r=\"10\" stroke-opacity=\"0.25\"/>\r\n                  <path d=\"M12 2 A10 10 0 0 1 22 12\" stroke-linecap=\"round\"/>\r\n                </svg>\r\n              </div>\r\n              <img id=\"img-${agent.id}-${idx}\"\r\n                  src=\"\"\r\n                  alt=\"Loading...\" \r\n                  style=\"display:none;width:100%;height:100%;object-fit:cover;border-radius:8px;transition:opacity 0.3s ease;\"\r\n                  onload=\"const spinner=document.getElementById('spinner-${agent.id}-${idx}');const img=this;if(spinner && img.src && !img.src.includes('data:image/svg+xml') && img.src !== ''){spinner.style.display='none';img.style.display='block';img.style.opacity='0';setTimeout(()=>{img.style.opacity='1';},10);}\">\r\n            </div>\r\n          `).join('');\r\n          \r\n          container.innerHTML = `\r\n            <div class=\"tab-section\">\r\n              <div class=\"tab-section-title\">Screenshots & Diagrams</div>\r\n              <div class=\"image-gallery\">${placeholders}</div>\r\n            </div>`;\r\n        }\r\n        try {\r\n          imageFiles = await getImageFileList(agent);\r\n          agent._imageFiles = imageFiles; // Cache it for future use\r\n          \r\n          // If no images found, mark as initialized and hide the tab\r\n          if (imageFiles.length === 0) {\r\n            agent._imagesInitialized = true;\r\n            if (window.updateTabsAfterLoad) {\r\n              window.updateTabsAfterLoad(agent);\r\n            }\r\n            // Clear container if no images\r\n            if (container) {\r\n              container.innerHTML = '';\r\n            }\r\n            return; // Don't render anything\r\n          }\r\n        } catch (err) {\r\n          console.warn(`Failed to fetch image file list for agent ${agent.id}:`, err);\r\n          agent._imagesInitialized = true;\r\n          if (window.updateTabsAfterLoad) {\r\n            window.updateTabsAfterLoad(agent);\r\n          }\r\n          if (container) {\r\n            container.innerHTML = '';\r\n          }\r\n          return;\r\n        }\r\n      }\r\n      \r\n      const imagesToRender = agent.images || [];\r\n      const totalImageCount = imageFiles.length > 0 ? imageFiles.length : imagesToRender.length;\r\n      \r\n      // If no images at all, don't render\r\n      if (totalImageCount === 0) {\r\n        if (container) {\r\n          container.innerHTML = '';\r\n        }\r\n        return;\r\n      }\r\n      const imagesPerPage = 6;\r\n      const totalPages = Math.ceil(totalImageCount / imagesPerPage);\r\n      \r\n      // Initialize pagination state for this agent if not exists\r\n      if (!agent._imagePage) agent._imagePage = 1;\r\n      const currentPage = agent._imagePage;\r\n      const startIdx = (currentPage - 1) * imagesPerPage;\r\n      const endIdx = startIdx + imagesPerPage;\r\n      \r\n      console.log('\ud83d\uddbc\ufe0f Rendering images page', currentPage, 'for agent', agent.id, '- startIdx:', startIdx, 'endIdx:', endIdx, 'totalImages:', totalImageCount);\r\n      \r\n      // Generate placeholders for current page\r\n      // If we have imageFiles metadata, use that; otherwise use loaded images\r\n      let pageImageFiles = [];\r\n      if (imageFiles.length > 0) {\r\n        pageImageFiles = imageFiles.slice(startIdx, endIdx);\r\n      } else if (imagesToRender.length > 0) {\r\n        pageImageFiles = imagesToRender.slice(startIdx, endIdx);\r\n      }\r\n      \r\n      const items = pageImageFiles.map((fileOrImg, idx) => {\r\n        const globalIdx = startIdx + idx;\r\n        // Check if it's a file object (has .name property that's a path) or an image object\r\n        const isFile = fileOrImg && typeof fileOrImg === 'object' && fileOrImg.name && fileOrImg.name.includes('/');\r\n        const fileName = isFile ? fileOrImg.name.split('/').pop() : (fileOrImg?.name || 'Image');\r\n        const loadedImage = imagesToRender[globalIdx];\r\n        const imageName = loadedImage?.imageName || loadedImage?.name || agent.name || 'Image';\r\n        const imageAlt = loadedImage?.alt || agent.name || 'Agent Image';\r\n        const imageUrl = loadedImage?.url || '';\r\n        const imgId = `img-${agent.id}-${globalIdx}`;\r\n        const hasLoadedImage = loadedImage && loadedImage.url && loadedImage.url !== '';\r\n        \r\n        return `\r\n        <div class=\"gallery-item\" style=\"position:relative;min-height:120px;${!hasLoadedImage ? 'background:var(--cfx3rdlevel);display:flex;align-items:center;justify-content:center;' : ''}\">\r\n          ${!hasLoadedImage ? `\r\n          <div class=\"image-loading-spinner\" id=\"spinner-${agent.id}-${globalIdx}\" style=\"display:block;pointer-events:none;\">\r\n            <svg width=\"32\" height=\"32\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"var(--primary, #7dcfff)\" stroke-width=\"2\">\r\n              <circle cx=\"12\" cy=\"12\" r=\"10\" stroke-opacity=\"0.25\"/>\r\n              <path d=\"M12 2 A10 10 0 0 1 22 12\" stroke-linecap=\"round\"/>\r\n            </svg>\r\n          </div>` : ''}\r\n          <img id=\"${imgId}\"\r\n              src=\"${hasLoadedImage ? imageUrl : ''}\"\r\n              alt=\"${imageAlt}\" \r\n              style=\"${!hasLoadedImage ? 'display:none;' : ''}width:100%;height:100%;object-fit:cover;border-radius:8px;transition:opacity 0.3s ease;cursor:pointer;position:relative;z-index:2;\"\r\n              onerror=\"this.onerror=null;\"\r\n              onload=\"${!hasLoadedImage ? `const spinner=document.getElementById('spinner-${agent.id}-${globalIdx}');const img=this;if(spinner && img.src && !img.src.includes('data:image/svg+xml') && img.src !== ''){spinner.style.display='none';spinner.style.pointerEvents='none';img.style.display='block';img.style.opacity='0';setTimeout(()=>{img.style.opacity='1';},10);}` : ''}\"\r\n              onclick=\"openImageModal(this.src, '${imageName.replace(/'/g, \"\\\\'\")}')\">\r\n        </div>\r\n      `;\r\n      }).join('');\r\n      \r\n      // Pagination controls\r\n      let paginationHTML = '';\r\n      if (totalPages > 1) {\r\n        const prevDisabled = currentPage === 1 ? 'disabled' : '';\r\n        const nextDisabled = currentPage === totalPages ? 'disabled' : '';\r\n        const pageNumbers = [];\r\n        for (let i = 1; i <= totalPages; i++) {\r\n          const active = i === currentPage ? 'active' : '';\r\n          pageNumbers.push(`<button class=\"page-btn ${active}\" onclick=\"changeImagePage('${agent.id}', ${i})\">${i}</button>`);\r\n        }\r\n        \r\n        paginationHTML = `\r\n          <div class=\"image-pagination\">\r\n            <button class=\"pagination-btn\" ${prevDisabled} onclick=\"changeImagePage('${agent.id}', ${currentPage - 1})\" ${prevDisabled ? 'disabled' : ''}>\r\n              <svg width=\"16\" height=\"16\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\">\r\n                <polyline points=\"15 18 9 12 15 6\"/>\r\n              </svg>\r\n              Previous\r\n            </button>\r\n            <div class=\"page-numbers\">${pageNumbers.join('')}</div>\r\n            <button class=\"pagination-btn\" ${nextDisabled} onclick=\"changeImagePage('${agent.id}', ${currentPage + 1})\" ${nextDisabled ? 'disabled' : ''}>\r\n              Next\r\n              <svg width=\"16\" height=\"16\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\">\r\n                <polyline points=\"9 18 15 12 9 6\"/>\r\n              </svg>\r\n            </button>\r\n          </div>`;\r\n      }\r\n      \r\n      // Generate HTML - if container is provided, it's the tab-content element itself, so don't wrap\r\n      // If container is null, we're generating HTML for initial render, so include the wrapper\r\n      let html;\r\n      if (container) {\r\n        // Container IS the tab-content element, so just set inner content (no wrapper)\r\n        html = `\r\n          <div class=\"tab-section\">\r\n            <div class=\"tab-section-title\">Screenshots & Diagrams ${totalImageCount > 0 ? `(${totalImageCount} total)` : ''}</div>\r\n            <div class=\"image-gallery\">${items}</div>\r\n            ${paginationHTML}\r\n          </div>`;\r\n      } else {\r\n        // No container - return full HTML with wrapper for initial render\r\n        html = `\r\n          <div class=\"tab-content ${initialTab==='images'?'active':''}\" data-tab=\"images\">\r\n            <div class=\"tab-section\">\r\n              <div class=\"tab-section-title\">Screenshots & Diagrams ${totalImageCount > 0 ? `(${totalImageCount} total)` : ''}</div>\r\n              <div class=\"image-gallery\">${items}</div>\r\n              ${paginationHTML}\r\n            </div>\r\n          </div>`;\r\n      }\r\n      \r\n      if (container) {\r\n        // Set HTML immediately so placeholders show right away (synchronous operation)\r\n        // This will replace the content and show placeholders immediately\r\n        container.innerHTML = html;\r\n        console.log('\u2705 HTML set for images page', currentPage, '- showing', pageImageFiles.length, 'items');\r\n        \r\n        // After HTML is set, load images for the current page in the background\r\n        // This happens asynchronously and doesn't block the UI\r\n        if (imageFiles.length > 0) {\r\n          // Initialize images array if needed\r\n          if (!agent.images || agent.images.length !== imageFiles.length) {\r\n            agent.images = new Array(imageFiles.length);\r\n          }\r\n          \r\n          // Load images for current page range (if not already loaded)\r\n          // This is now handled by changeImagePage, but we check here too for initial render\r\n          const pageFiles = imageFiles.slice(startIdx, endIdx);\r\n          const needsLoading = pageFiles.some((file, idx) => {\r\n            const globalIdx = startIdx + idx;\r\n            return !imagesToRender[globalIdx] || !imagesToRender[globalIdx].url;\r\n          });\r\n          \r\n          if (needsLoading) {\r\n            console.log('\ud83d\udce5 Loading', pageFiles.length, 'images for page', currentPage, '(indices', startIdx, 'to', endIdx - 1, ')');\r\n            // Load images for this page (don't wait - let them load in background)\r\n            loadImagesForPageRange(agent, startIdx, endIdx).catch(err => {\r\n              console.warn('Failed to load images for page', currentPage, ':', err);\r\n            });\r\n          } else {\r\n            console.log('\u23ed\ufe0f Images for page', currentPage, 'already loaded, skipping');\r\n          }\r\n        } else if (imagesToRender.length > 0) {\r\n          // If we don't have file metadata but have loaded images, they should already be displayed\r\n          console.log('\u2139\ufe0f Using already loaded images (no file metadata)');\r\n        }\r\n      } else {\r\n        return html;\r\n      }\r\n    };\r\n    \r\n    // Function to change image page\r\n    window.changeImagePage = async function(agentId, page) {\r\n      const agent = agents.find(a => a.id === agentId);\r\n      if (!agent) {\r\n        console.error('Agent not found:', agentId);\r\n        return;\r\n      }\r\n      \r\n      // Use image file list if available, otherwise use loaded images\r\n      const imageFiles = agent._imageFiles || [];\r\n      const imagesToRender = agent.images || [];\r\n      const totalImageCount = imageFiles.length > 0 ? imageFiles.length : imagesToRender.length;\r\n      \r\n      if (totalImageCount === 0) {\r\n        console.warn('No images found for agent:', agentId);\r\n        return;\r\n      }\r\n      \r\n      const imagesPerPage = 6;\r\n      const totalPages = Math.ceil(totalImageCount / imagesPerPage);\r\n      if (page < 1 || page > totalPages) {\r\n        console.warn('Invalid page number:', page, 'out of', totalPages);\r\n        return;\r\n      }\r\n      \r\n      // Update page number FIRST\r\n      agent._imagePage = page;\r\n      \r\n      // Get the images tab content container\r\n      const imagesTabContent = $(`.tab-content[data-tab=\"images\"]`);\r\n      if (!imagesTabContent) {\r\n        console.error('Images tab content not found');\r\n        return;\r\n      }\r\n      \r\n      // Calculate the range for this page\r\n      const startIdx = (page - 1) * imagesPerPage;\r\n      const endIdx = Math.min(startIdx + imagesPerPage, totalImageCount);\r\n      \r\n      // Load images for this page if not already loaded\r\n      if (imageFiles.length > 0) {\r\n        // Check if images for this page are already loaded\r\n        const needsLoading = imageFiles.slice(startIdx, endIdx).some((file, idx) => {\r\n          const globalIdx = startIdx + idx;\r\n          return !agent.images || !agent.images[globalIdx] || !agent.images[globalIdx].url;\r\n        });\r\n        \r\n        if (needsLoading) {\r\n          console.log(`\ud83d\udce5 Loading images for page ${page} (indices ${startIdx} to ${endIdx - 1})`);\r\n          // Load images for this page\r\n          await loadImagesForPageRange(agent, startIdx, endIdx);\r\n        }\r\n      }\r\n      \r\n      // Immediately render the new page\r\n      try {\r\n        await renderImagesWithPagination(agent, imagesTabContent);\r\n        console.log('\u2705 Page changed to', page, 'for agent', agentId);\r\n      } catch (error) {\r\n        console.error('Error rendering images page:', error);\r\n      }\r\n    };\r\n\r\n    function renderVideos() {\r\n      // Render if there are videos OR if videos_path exists AND not finished loading (will show loading/empty state)\r\n      const currentHasVideos = Array.isArray(agent.videos) && agent.videos.length > 0;\r\n      const currentHasVideosPath = agent.videos_path && agent.videos_path.trim() !== '' && !agent._videosLoaded;\r\n      \r\n      // Don't render if no videos and either no path OR path was checked and found empty\r\n      if(!currentHasVideos && (!hasVideosPath || (agent._videosLoaded && !currentHasVideos))) return '';\r\n      \r\n      // If no videos loaded yet but path exists and not initialized, show loading state\r\n      if(!currentHasVideos && currentHasVideosPath && !agent._videosLoaded) {\r\n        // Videos not loaded yet - show loading state\r\n        return `\r\n          <div class=\"tab-content ${initialTab === 'videos' ? 'active' : ''}\" data-tab=\"videos\">\r\n            <div class=\"tab-section\">\r\n              <div class=\"tab-section-title\">Tutorial Videos</div>\r\n              <div class=\"video-list\" style=\"text-align:center;padding:40px;color:var(--text-muted);\">\r\n                <div style=\"display:flex;align-items:center;justify-content:center;gap:12px;\">\r\n                  <svg width=\"24\" height=\"24\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" style=\"animation:spin 1s linear infinite;\">\r\n                    <circle cx=\"12\" cy=\"12\" r=\"10\" stroke-opacity=\"0.25\"/>\r\n                    <path d=\"M12 2 A10 10 0 0 1 22 12\" stroke-linecap=\"round\"/>\r\n                  </svg>\r\n                  <span>Loading videos...</span>\r\n                </div>\r\n              </div>\r\n            </div>\r\n          </div>`;\r\n      }\r\n      \r\n      const items = agent.videos.map(v => {\r\n        const isExternal = v.url && v.url.startsWith('http');\r\n        const thumbHTML = v.thumb\r\n          ? `<img src=\"${v.thumb}\" alt=\"${v.title || 'Video'}\" style=\"width:100%;height:100%;object-fit:cover;border-radius:8px\"/>`\r\n          : `<svg viewBox=\"0 0 24 24\"><polygon points=\"5 3 19 12 5 21 5 3\"/></svg>`;\r\n        if (isExternal) {\r\n          return `\r\n            <a class=\"video-item\" href=\"${v.url}\" target=\"_blank\" rel=\"noopener noreferrer\">\r\n              <div class=\"video-thumb\">${thumbHTML}</div>\r\n              <div class=\"video-info\">\r\n                <div class=\"video-title\">${v.title || 'Video'}</div>\r\n                <div class=\"video-duration\">${v.duration || ''}</div>\r\n              </div>\r\n            </a>`;\r\n        }\r\n        return `\r\n          <div class=\"video-item\" style=\"cursor:pointer\"\r\n              onclick=\"openVideoModal('${v.url}', '${v.title || 'Video'}')\">\r\n            <div class=\"video-thumb\">${thumbHTML}</div>\r\n            <div class=\"video-info\">\r\n              <div class=\"video-title\">${v.title || 'Video'}</div>\r\n              <div class=\"video-duration\">${v.duration || ''}</div>\r\n            </div>\r\n          </div>`;\r\n      }).join('');\r\n      return `\r\n        <div class=\"tab-content ${initialTab === 'videos' ? 'active' : ''}\" data-tab=\"videos\">\r\n          <div class=\"tab-section\">\r\n            <div class=\"tab-section-title\">Tutorial Videos</div>\r\n            <div class=\"video-list\">${items}</div>\r\n          </div>\r\n        </div>`;\r\n    }\r\n\r\n    function renderContact(){\r\n      if(!hasContact) return '';\r\n      const iconEmail = '<svg viewBox=\"0 0 24 24\"><path d=\"M4 4h16c1.1 0 2 .9 2 2v12c0 1.1-.9 2-2 2H4c-1.1 0-2-.9-2-2V6c0-1.1.9-2 2-2z\"/><polyline points=\"22,6 12,13 2,6\"/></svg>';\r\n      const iconPhone = '<svg viewBox=\"0 0 24 24\"><path d=\"M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07 19.5 19.5 0 0 1-6-6 19.79 19.79 0 0 1-3.07-8.67A2 2 0 0 1 4.11 2h3a2 2 0 0 1 2 1.72 12.84 12.84 0 0 0 .7 2.81 2 2 0 0 1-.45 2.11L8.09 9.91a16 16 0 0 0 6 6l1.27-1.27a2 2 0 0 1 2.11-.45 12.84 12.84 0 0 0 2.81.7A2 2 0 0 1 22 16.92z\"/></svg>';\r\n      const iconWeb = '<svg viewBox=\"0 0 24 24\"><circle cx=\"12\" cy=\"12\" r=\"10\"/><line x1=\"2\" y1=\"12\" x2=\"22\" y2=\"12\"/><path d=\"M12 2a15.3 15.3 0 0 1 4 10 15.3 15.3 0 0 1-4 10 15.3 15.3 0 0 1-4-10 15.3 15.3 0 0 1 4-10z\"/></svg>';\r\n      const iconChat = '<svg viewBox=\"0 0 24 24\"><path d=\"M21 11.5a8.38 8.38 0 0 1-.9 3.8 8.5 8.5 0 0 1-7.6 4.7 8.38 8.38 0 0 1-3.8-.9L3 21l1.9-5.7a8.38 8.38 0 0 1-.9-3.8 8.5 8.5 0 0 1 4.7-7.6 8.38 8.38 0 0 1 3.8-.9h.5a8.48 8.48 0 0 1 8 8v.5z\"/></svg>';\r\n      const itemsHtml = contactItems.map(ci => {\r\n        if(ci.type === 'email'){\r\n          return `<a class=\"contact-item\" href=\"mailto:${ci.value}\"><div class=\"contact-icon\">${iconEmail}</div><div class=\"contact-details\"><div class=\"contact-label\">${ci.label}</div><div class=\"contact-value\">${ci.value}</div></div></a>`;\r\n        }\r\n        if(ci.type === 'phone'){\r\n          return `<a class=\"contact-item\" href=\"tel:${ci.value}\"><div class=\"contact-icon\">${iconPhone}</div><div class=\"contact-details\"><div class=\"contact-label\">${ci.label}</div><div class=\"contact-value\">${ci.value}</div></div></a>`;\r\n        }\r\n        if(ci.type === 'website'){\r\n          const site = ci.value;\r\n          const href = site && site.startsWith('http') ? site : `https://${site}`;\r\n          return `<a class=\"contact-item\" href=\"${href}\" target=\"_blank\" rel=\"noopener noreferrer\"><div class=\"contact-icon\">${iconWeb}</div><div class=\"contact-details\"><div class=\"contact-label\">${ci.label}</div><div class=\"contact-value\">${site}</div></div></a>`;\r\n        }\r\n        return `<div class=\"contact-item\"><div class=\"contact-icon\">${iconChat}</div><div class=\"contact-details\"><div class=\"contact-label\">${ci.label}</div><div class=\"contact-value\">${ci.value}</div></div></div>`;\r\n      }).join('');\r\n      return `<div class=\"tab-content ${initialTab==='contact'?'active':''}\" data-tab=\"contact\"><div class=\"tab-section\"><div class=\"tab-section-title\">Support & Contact Information</div><div class=\"contact-grid\">${itemsHtml}</div></div></div>`;\r\n    }\r\n\r\n    function renderRelated(){\r\n      if(!hasRelated) return '';\r\n      const items = agent.related.map(r => {\r\n        const relName = r.name || '';\r\n        const relDesc = r.description || r.decription || '';\r\n        return `<div class=\"document-item\" onclick=\"openAgentByName('${relName.replace(/'/g, \"&#39;\")}')\"><div class=\"document-icon\"><svg viewBox=\"0 0 24 24\"><circle cx=\"12\" cy=\"12\" r=\"10\"/></svg></div><div class=\"document-info\"><div class=\"document-name\">${relName}</div><div class=\"document-meta\">${relDesc}</div></div></div>`;\r\n      }).join('');\r\n      return `<div class=\"tab-content ${initialTab==='related'?'active':''}\" data-tab=\"related\"><div class=\"tab-section\"><div class=\"tab-section-title\">Related Agents</div><div class=\"document-list\">${items}</div></div></div>`;\r\n    }\r\n\r\n    const tabsNavHtml = renderTabsNav();\r\n    const docsHtml = renderDocs();\r\n    const imagesHtml = renderImages();\r\n    const videosHtml = renderVideos();\r\n    const contactHtml = renderContact();\r\n    const relatedHtml = renderRelated();\r\n\r\n    // Get the current tabs-wrapper\r\n    const tabsWrapper = $('#paneContent').querySelector('.tabs-wrapper');\r\n    if (tabsWrapper) {\r\n      // Update existing tabs-wrapper\r\n      tabsWrapper.innerHTML = `${tabsNavHtml}${docsHtml}${imagesHtml}${videosHtml}${contactHtml}${relatedHtml}`;\r\n    } else {\r\n      // Create new tabs-wrapper if it doesn't exist\r\n      const actionButtons = $('#paneContent').querySelector('.action-buttons');\r\n      if (actionButtons) {\r\n        actionButtons.insertAdjacentHTML('afterend', `<div class=\"tabs-wrapper\">${tabsNavHtml}${docsHtml}${imagesHtml}${videosHtml}${contactHtml}${relatedHtml}</div>`);\r\n      } else {\r\n        // Fallback: append to paneContent\r\n        $('#paneContent').insertAdjacentHTML('beforeend', `<div class=\"tabs-wrapper\">${tabsNavHtml}${docsHtml}${imagesHtml}${videosHtml}${contactHtml}${relatedHtml}</div>`);\r\n      }\r\n    }\r\n    \r\n    // Update tab navigation\r\n    setTimeout(updateTabNavigation, 100);\r\n  }\r\n\r\n  // Function to update pane content when media loads\r\n  function updatePaneContent(agent) {\r\n    // Check if pane is still open for this agent\r\n    if (!$('#sidePane').classList.contains('open')) return;\r\n    if ($('#paneTitle').textContent !== agent.name) return;\r\n\r\n    // Re-evaluate tabs based on loaded media - only show tabs if content exists OR if actively loading\r\n    const hasDocs = Array.isArray(agent.documents) && agent.documents.length > 0;\r\n    const hasDocsPath = agent.docs_path && agent.docs_path.trim() !== '' && !agent._docsLoaded;\r\n    const hasImages = Array.isArray(agent.images) && agent.images.length > 0;\r\n    const hasImagesPath = agent.images_path && agent.images_path.trim() !== '' && !agent._imagesInitialized;\r\n    const hasVideos = Array.isArray(agent.videos) && agent.videos.length > 0;\r\n    const hasVideosPath = agent.videos_path && agent.videos_path.trim() !== '' && !agent._videosLoaded;\r\n    const contactItems = (() => {\r\n      const items = [];\r\n      if(Array.isArray(agent.contact)){\r\n        agent.contact.forEach(c => {\r\n          if(c.email) items.push({ type:'email', value:c.email, label:c.name || 'Email' });\r\n          if(c.phone) items.push({ type:'phone', value:c.phone, label:c.name || 'Phone' });\r\n          if(c.website) items.push({ type:'website', value:c.website, label:c.name || 'Website' });\r\n          if(c.chat) items.push({ type:'chat', value:c.chat, label:c.name || 'Live Chat' });\r\n        });\r\n      } else if(agent.contact && typeof agent.contact === 'object'){\r\n        const c = agent.contact;\r\n        if(c.email) items.push({ type:'email', value:c.email, label:'Email' });\r\n        if(c.phone) items.push({ type:'phone', value:c.phone, label:'Phone' });\r\n        if(c.website) items.push({ type:'website', value:c.website, label:'Website' });\r\n        if(c.chat) items.push({ type:'chat', value:c.chat, label:'Live Chat' });\r\n      }\r\n      return items;\r\n    })();\r\n    const hasContact = contactItems.length > 0;\r\n    const hasRelated = Array.isArray(agent.related) && agent.related.length > 0;\r\n\r\n    // Only add tabs if content exists or is loading\r\n    const tabs = [];\r\n    const shouldShowDocsTab = hasDocs || hasDocsPath;\r\n    const shouldShowImagesTab = hasImages || hasImagesPath;\r\n    const shouldShowVideosTab = hasVideos || hasVideosPath;\r\n    if(shouldShowDocsTab) tabs.push('documents');\r\n    if(shouldShowImagesTab) tabs.push('images');\r\n    if(shouldShowVideosTab) tabs.push('videos');\r\n    if(hasContact) tabs.push('contact');\r\n    if(hasRelated) tabs.push('related');\r\n\r\n    // Preserve the current active tab instead of resetting to first tab\r\n    const currentActiveTab = activeTab && tabs.includes(activeTab) ? activeTab : (tabs[0] || null);\r\n    activeTab = currentActiveTab || activeTab;\r\n\r\n    // Update specific tab content without re-rendering everything (prevents tab switching)\r\n    if ((hasDocs || hasDocsPath) && activeTab === 'documents') {\r\n      // Update documents tab when docs finish loading\r\n      const docsTabContent = $(`.tab-content[data-tab=\"documents\"]`);\r\n      if (docsTabContent) {\r\n        const items = agent.documents.map(doc => {\r\n          const isExternal = doc.url && doc.url.startsWith('http');\r\n          const fileType = (doc.type || '').toLowerCase();\r\n          const fileSize = doc.size ? formatFileSize(doc.size) : '';\r\n          if (isExternal) {\r\n            return `\r\n              <a class=\"document-item\" href=\"${doc.url}\" target=\"_blank\" rel=\"noopener noreferrer\">\r\n                <div class=\"document-icon\">\r\n                  <svg viewBox=\"0 0 24 24\">\r\n                    <path d=\"M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z\"/>\r\n                    <polyline points=\"14 2 14 8 20 8\"/>\r\n                  </svg>\r\n                </div>\r\n                <div class=\"document-info\">\r\n                  <div class=\"document-name\">${doc.name || 'Document'}</div>\r\n                  <div class=\"document-meta\">${[fileType, fileSize].filter(Boolean).join(' \u2022 ')}</div>\r\n                </div>\r\n              </a>`;\r\n          }\r\n          return `\r\n            <div class=\"document-item\" style=\"cursor:pointer\"\r\n                 onclick=\"openDocModal('${doc.url || ''}', '${(doc.name || 'Document').replace(/'/g, \"\\\\'\")}', '${fileType}')\">\r\n              <div class=\"document-icon\">\r\n                <svg viewBox=\"0 0 24 24\">\r\n                  <path d=\"M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z\"/>\r\n                  <polyline points=\"14 2 14 8 20 8\"/>\r\n                </svg>\r\n              </div>\r\n              <div class=\"document-info\">\r\n                <div class=\"document-name\">${doc.name || 'Document'}</div>\r\n                <div class=\"document-meta\">${[fileType, fileSize].filter(Boolean).join(' \u2022 ') || 'File'}</div>\r\n              </div>\r\n            </div>`;\r\n        }).join('');\r\n        docsTabContent.innerHTML = `\r\n          <div class=\"tab-section\">\r\n            <div class=\"tab-section-title\">Product Documents</div>\r\n            <div class=\"document-list\">${items}</div>\r\n          </div>`;\r\n      }\r\n    } else if ((hasImages || hasImagesPath) && activeTab === 'images') {\r\n      // Only update the images tab content with pagination, keep other tabs as-is\r\n      const imagesTabContent = $(`.tab-content[data-tab=\"images\"]`);\r\n      if (imagesTabContent) {\r\n        renderImagesWithPagination(agent, imagesTabContent).catch(err => {\r\n          console.error('Error rendering images:', err);\r\n        });\r\n      }\r\n    } else if ((hasVideos || hasVideosPath) && activeTab === 'videos') {\r\n      // Update videos tab when videos finish loading\r\n      const videosTabContent = $(`.tab-content[data-tab=\"videos\"]`);\r\n      if (videosTabContent) {\r\n        // If videos are loaded but empty, show \"No videos found\"\r\n        if (agent._videosLoaded && (!agent.videos || agent.videos.length === 0)) {\r\n          videosTabContent.innerHTML = `\r\n            <div class=\"tab-section\">\r\n              <div class=\"tab-section-title\">Tutorial Videos</div>\r\n              <div class=\"video-list\" style=\"text-align:center;padding:40px;color:var(--text-muted);\">No videos found</div>\r\n            </div>`;\r\n          return;\r\n        }\r\n        \r\n        // If videos exist, render them\r\n        if (hasVideos && agent.videos.length > 0) {\r\n          const items = agent.videos.map(v => {\r\n          const isExternal = v.url && v.url.startsWith('http');\r\n          const thumbHTML = v.thumb\r\n            ? `<img src=\"${v.thumb}\" alt=\"${v.title || 'Video'}\" style=\"width:100%;height:100%;object-fit:cover;border-radius:8px\"/>`\r\n            : `<svg viewBox=\"0 0 24 24\"><polygon points=\"5 3 19 12 5 21 5 3\"/></svg>`;\r\n          if (isExternal) {\r\n          return `\r\n              <a class=\"video-item\" href=\"${v.url}\" target=\"_blank\" rel=\"noopener noreferrer\">\r\n                <div class=\"video-thumb\">${thumbHTML}</div>\r\n                <div class=\"video-info\">\r\n                  <div class=\"video-title\">${v.title || 'Video'}</div>\r\n                  <div class=\"video-duration\">${v.duration || ''}</div>\r\n          </div>\r\n              </a>`;\r\n          }\r\n          return `\r\n            <div class=\"video-item\" style=\"cursor:pointer\"\r\n                onclick=\"openVideoModal('${v.url}', '${v.title || 'Video'}')\">\r\n              <div class=\"video-thumb\">${thumbHTML}</div>\r\n              <div class=\"video-info\">\r\n                <div class=\"video-title\">${v.title || 'Video'}</div>\r\n                <div class=\"video-duration\">${v.duration || ''}</div>\r\n              </div>\r\n            </div>`;\r\n        }).join('');\r\n          videosTabContent.innerHTML = `\r\n          <div class=\"tab-section\">\r\n              <div class=\"tab-section-title\">Tutorial Videos</div>\r\n              <div class=\"video-list\">${items}</div>\r\n          </div>`;\r\n        }\r\n        // If videos_path exists but videos aren't loaded yet, show loading state\r\n        else if (hasVideosPath && !agent._videosLoaded) {\r\n          videosTabContent.innerHTML = `\r\n            <div class=\"tab-section\">\r\n              <div class=\"tab-section-title\">Tutorial Videos</div>\r\n              <div class=\"video-list\" style=\"text-align:center;padding:40px;color:var(--text-muted);\">\r\n                <div style=\"display:flex;align-items:center;justify-content:center;gap:12px;\">\r\n                  <svg width=\"24\" height=\"24\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" style=\"animation:spin 1s linear infinite;\">\r\n                    <circle cx=\"12\" cy=\"12\" r=\"10\" stroke-opacity=\"0.25\"/>\r\n                    <path d=\"M12 2 A10 10 0 0 1 22 12\" stroke-linecap=\"round\"/>\r\n                  </svg>\r\n                  <span>Loading videos...</span>\r\n                </div>\r\n              </div>\r\n            </div>`;\r\n        }\r\n      }\r\n    } else {\r\n      // Re-render all tabs if we're not on a specific tab (shouldn't happen often)\r\n      renderPaneContent(agent, currentActiveTab, tabs, shouldShowDocsTab, shouldShowImagesTab, shouldShowVideosTab, hasContact, hasRelated, contactItems, hasImagesPath, hasDocsPath, hasVideosPath);\r\n    }\r\n  }\r\n\r\n  function closePane(){\r\n    $('#sidePane').classList.remove('open');\r\n    $('.main-content').classList.remove('pane-open');\r\n    $('#backdrop').classList.remove('active');\r\n  }\r\n\r\n  window.switchTabGlobal = switchTab;\r\n  window.scrollTabsGlobal = scrollTabs;\r\n  window.openAgentByName = (name)=>{\r\n    if(!name) return;\r\n    const a = agents.find(x => x.name && x.name.toLowerCase() === String(name).toLowerCase());\r\n    if(a) openPane(a);\r\n  };\r\n\r\n  $('#closePane').addEventListener('click', closePane);\r\n  // Note: backdrop click handler removed since backdrop no longer blocks pointer events\r\n  // Users can close pane using the close button, or clicking outside will be handled by the pane itself\r\n\r\n\r\n  function renderAll(){\r\n    const isSearching = searchQuery.trim() !== '';\r\n    \r\n    if(isSearching){\r\n      $('#sectionsContainer').classList.add('search-active');\r\n      $('#searchResults').classList.add('active');\r\n      $('#searchResultsGrid').innerHTML = '';\r\n      \r\n      // Filter by selected projects - if none selected, show no agents\r\n      let base = agents;\r\n      if (selectedProjects && selectedProjects.length > 0) {\r\n        // Filter by selected projects\r\n        base = agents.filter(a => selectedProjects.includes(a.ai_project_name));\r\n        console.log(`\ud83d\udd0d Filtered by selected projects (${selectedProjects.length}):`, base.length, 'agents');\r\n      } else {\r\n        // No projects selected - show no agents\r\n        base = [];\r\n        console.log('\ud83d\udd0d No projects selected - showing no agents');\r\n      }\r\n      // Then filter by enabled state and Deployed status\r\n      const beforeEnabled = base.length;\r\n      base = base.filter(a => String(a.state || '').toLowerCase() === 'enabled');\r\n      const beforeStatus = base.length;\r\n      base = base.filter(a => String(a.status || '').toLowerCase() === 'deployed');\r\n      console.log(`\ud83d\udd0d After enabled filter:`, beforeStatus, 'agents (from', beforeEnabled, ')');\r\n      console.log(`\ud83d\udd0d After status filter (Deployed):`, base.length, 'agents (from', beforeStatus, ')');\r\n      \r\n      let filteredAgents = base.filter(agent => {\r\n        const name = (agent.name || '').toLowerCase();\r\n        const category = (agent.category || '').toLowerCase();\r\n        const tagsText = Array.isArray(agent.tags) ? agent.tags.join(' ').toLowerCase() : '';\r\n        const description = (agent.description || '').toLowerCase();\r\n        return (\r\n          name.includes(searchQuery) ||\r\n          category.includes(searchQuery) ||\r\n          tagsText.includes(searchQuery) ||\r\n          description.includes(searchQuery)\r\n        );\r\n      });\r\n\r\n      filteredAgents.sort((a, b) => {\r\n        const nameA = a.name || '';\r\n        const nameB = b.name || '';\r\n        const categoryA = a.category || '';\r\n        const categoryB = b.category || '';\r\n        const versionA = a.version || '';\r\n        const versionB = b.version || '';\r\n        if(sortBy === 'name') return nameA.localeCompare(nameB);\r\n        if(sortBy === 'category') return categoryA.localeCompare(categoryB);\r\n        if(sortBy === 'recent') return new Date(b.lastRun || 0) - new Date(a.lastRun || 0);\r\n        if(sortBy === 'version') return versionB.localeCompare(versionA);\r\n        return 0;\r\n      });\r\n\r\n      if(filteredAgents.length === 0){\r\n        $('#searchResultsGrid').innerHTML = '<div class=\"empty-section\">No agents found matching your search.</div>';\r\n        $('#searchCount').textContent = '0 agents found';\r\n      } else {\r\n        const categorizedResults = {};\r\n        filteredAgents.forEach(agent => {\r\n          // Normalize category name (case-insensitive)\r\n          const normalizedCategory = (agent.category || '').toLowerCase();\r\n          if(!categorizedResults[normalizedCategory]){\r\n            categorizedResults[normalizedCategory] = [];\r\n          }\r\n          categorizedResults[normalizedCategory].push(agent);\r\n        });\r\n        \r\n        // Convert normalized keys back to display names (use first agent's category)\r\n        const displayCategoryMap = {};\r\n        Object.keys(categorizedResults).forEach(normalized => {\r\n          const firstAgent = categorizedResults[normalized][0];\r\n          displayCategoryMap[firstAgent.category || normalized] = categorizedResults[normalized];\r\n        });\r\n\r\n        Object.entries(displayCategoryMap).forEach(([category, categoryAgents]) => {\r\n          const groupDiv = document.createElement('div');\r\n          groupDiv.className = 'search-category-group';\r\n          \r\n          const headerDiv = document.createElement('div');\r\n          headerDiv.className = 'search-category-header';\r\n          headerDiv.textContent = `${category} (${categoryAgents.length})`;\r\n          groupDiv.appendChild(headerDiv);\r\n\r\n          const tilesDiv = document.createElement('div');\r\n          tilesDiv.className = 'search-category-tiles';\r\n          categoryAgents.forEach(agent => {\r\n            tilesDiv.appendChild(tile(agent));\r\n          });\r\n          groupDiv.appendChild(tilesDiv);\r\n\r\n          $('#searchResultsGrid').appendChild(groupDiv);\r\n        });\r\n\r\n        $('#searchCount').textContent = `${filteredAgents.length} agent${filteredAgents.length === 1 ? '' : 's'} found in ${Object.keys(categorizedResults).length} categor${Object.keys(categorizedResults).length === 1 ? 'y' : 'ies'}`;\r\n      }\r\n      \r\n      // Update project counts\r\n      updateProjectCounts();\r\n      \r\n    } else {\r\n      $('#sectionsContainer').classList.remove('search-active');\r\n      $('#searchResults').classList.remove('active');\r\n      \r\n      // Clear dynamic categories and favorites\r\n      $('#categoriesContainer').innerHTML = '';\r\n      $('#favorites-grid').innerHTML = '';\r\n\r\n      let sortedAgents = [...agents];\r\n      sortedAgents.sort((a, b) => {\r\n        const nameA = a.name || '';\r\n        const nameB = b.name || '';\r\n        const categoryA = a.category || '';\r\n        const categoryB = b.category || '';\r\n        const versionA = a.version || '';\r\n        const versionB = b.version || '';\r\n        if(sortBy === 'name') return nameA.localeCompare(nameB);\r\n        if(sortBy === 'category') return categoryA.localeCompare(categoryB);\r\n        if(sortBy === 'recent') return new Date(b.lastRun || 0) - new Date(a.lastRun || 0);\r\n        if(sortBy === 'version') return versionB.localeCompare(versionA);\r\n        return 0;\r\n      });\r\n\r\n      // Filter by selected projects - if none selected, show no agents\r\n      let filteredByProject = sortedAgents;\r\n      if (selectedProjects && selectedProjects.length > 0) {\r\n        // Filter by selected projects\r\n        filteredByProject = sortedAgents.filter(a => selectedProjects.includes(a.ai_project_name));\r\n        console.log(`\ud83d\udd0d Main view - Filtered by selected projects (${selectedProjects.length}):`, filteredByProject.length, 'agents');\r\n      } else {\r\n        // No projects selected - show no agents\r\n        filteredByProject = [];\r\n        console.log('\ud83d\udd0d Main view - No projects selected - showing no agents');\r\n      }\r\n      // Then filter by enabled state and Deployed status (always show only enabled and deployed)\r\n      const beforeEnabled = filteredByProject.length;\r\n      const filteredByState = filteredByProject.filter(a => String(a.state || '').toLowerCase() === 'enabled');\r\n      const beforeStatus = filteredByState.length;\r\n      const filteredByStatus = filteredByState.filter(a => String(a.status || '').toLowerCase() === 'deployed');\r\n      console.log(`\ud83d\udd0d Main view - After enabled filter:`, beforeStatus, 'agents (from', beforeEnabled, ')');\r\n      console.log(`\ud83d\udd0d Main view - After status filter (Deployed):`, filteredByStatus.length, 'agents (from', beforeStatus, ')');\r\n\r\n      const favAgents = filteredByStatus.filter(a => favSet.has(a.id));\r\n      if(favAgents.length === 0){\r\n        $('#favorites-grid').innerHTML = '<div class=\"empty-section\">No favorites yet. Star some agents to see them here!</div>';\r\n      } else {\r\n        favAgents.forEach(agent => {\r\n          $('#favorites-grid').appendChild(tile(agent));\r\n        });\r\n      }\r\n      $('#fav-count').textContent = favAgents.length;\r\n\r\n      // Derive categories from agents.json (normalize to lowercase for grouping)\r\n      const categoryMap = new Map(); // Maps normalized name to original name\r\n      filteredByStatus.forEach(a => {\r\n        const normalized = (a.category || '').toLowerCase();\r\n        if (normalized && !categoryMap.has(normalized)) {\r\n          // Use the first occurrence's original case as the display name\r\n          categoryMap.set(normalized, a.category || normalized);\r\n        }\r\n      });\r\n      const categories = Array.from(categoryMap.values()).sort();\r\n      categories.forEach(catName => {\r\n        const normalizedCat = catName.toLowerCase();\r\n        const section = document.createElement('div');\r\n        section.className = 'category-section';\r\n        const headerId = `${normalizedCat.replace(/\\s+/g,'-')}-count`;\r\n        section.innerHTML = `\r\n          <div class=\"section-header\" data-section=\"${normalizedCat}\">\r\n            <div class=\"section-title\">\r\n              <h2>${catName.toUpperCase()}</h2>\r\n              <span class=\"section-count\" id=\"${headerId}\">0</span>\r\n            </div>\r\n            <span class=\"toggle-icon\">\u25bc</span>\r\n          </div>\r\n          <div class=\"section-grid\"></div>\r\n        `;\r\n        const grid = section.querySelector('.section-grid');\r\n        const catAgents = filteredByStatus.filter(a => (a.category || '').toLowerCase() === normalizedCat);\r\n        if(catAgents.length === 0){\r\n          grid.innerHTML = '<div class=\"empty-section\">No agents in this category</div>';\r\n        } else {\r\n          catAgents.forEach(a => grid.appendChild(tile(a)));\r\n        }\r\n        const countEl = section.querySelector('.section-count');\r\n        if(countEl) countEl.textContent = catAgents.length;\r\n        section.querySelector('.section-header').addEventListener('click', (e)=>{\r\n          const header = e.currentTarget;\r\n          header.classList.toggle('collapsed');\r\n          header.nextElementSibling.classList.toggle('collapsed');\r\n        });\r\n        $('#categoriesContainer').appendChild(section);\r\n      });\r\n      \r\n      // Update project counts\r\n      updateProjectCounts();\r\n    }\r\n  }\r\n  // Expose globally so we can trigger manually or from console\r\n  window.renderAll = renderAll;\r\n\r\n  $$('.section-header').forEach(header => {\r\n    header.addEventListener('click', () => {\r\n      header.classList.toggle('collapsed');\r\n      header.nextElementSibling.classList.toggle('collapsed');\r\n    });\r\n  });\r\n\r\n  $('#searchInput').addEventListener('input', (e) => {\r\n    searchQuery = e.target.value.toLowerCase();\r\n    renderAll();\r\n  });\r\n\r\n  // Populate project filter multi-select\r\n  // Function to calculate agent counts per project\r\n  function getProjectCounts() {\r\n    const counts = {};\r\n    if (!agents || agents.length === 0) {\r\n      return counts;\r\n    }\r\n    \r\n    agents.forEach(agent => {\r\n      // Only count agents that are enabled and deployed\r\n      if (String(agent.state || '').toLowerCase() === 'enabled' && \r\n          String(agent.status || '').toLowerCase() === 'deployed') {\r\n        const project = agent.ai_project_name;\r\n        if (project) {\r\n          counts[project] = (counts[project] || 0) + 1;\r\n        }\r\n      }\r\n    });\r\n    \r\n    return counts;\r\n  }\r\n  \r\n  // Function to get total count of all agents (sum of all project counts for user's projects)\r\n  function getTotalAgentCount() {\r\n    const projectCounts = getProjectCounts();\r\n    // Only sum up counts for projects that are in userAIProjects\r\n    if (!userAIProjects || userAIProjects.length === 0) {\r\n      return 0;\r\n    }\r\n    return userAIProjects.reduce((sum, project) => {\r\n      return sum + (projectCounts[project] || 0);\r\n    }, 0);\r\n  }\r\n  \r\n  // Function to update project counts in the dropdown\r\n  function updateProjectCounts() {\r\n    const projectCounts = getProjectCounts();\r\n    const filterOptions = $('#projectFilterOptions');\r\n    if (!filterOptions) return;\r\n    \r\n    // Update total count for \"Select All\"\r\n    const selectAllCountBadge = $('#selectAllCountBadge');\r\n    if (selectAllCountBadge) {\r\n      const totalCount = getTotalAgentCount();\r\n      selectAllCountBadge.textContent = totalCount;\r\n    }\r\n    \r\n    // Update individual project counts\r\n    filterOptions.querySelectorAll('.project-filter-item').forEach(item => {\r\n      const checkbox = item.querySelector('input[type=\"checkbox\"]');\r\n      if (!checkbox) return;\r\n      \r\n      const project = checkbox.value;\r\n      const count = projectCounts[project] || 0;\r\n      \r\n      let countBadge = item.querySelector('.project-count-badge');\r\n      if (!countBadge) {\r\n        countBadge = document.createElement('span');\r\n        countBadge.className = 'project-count-badge';\r\n        const label = item.querySelector('label');\r\n        if (label) {\r\n          label.appendChild(countBadge);\r\n        }\r\n      }\r\n      countBadge.textContent = count;\r\n    });\r\n  }\r\n\r\n  function populateProjectFilter() {\r\n    const filterButton = $('#projectFilterButton');\r\n    const filterDropdown = $('#projectFilterDropdown');\r\n    const filterOptions = $('#projectFilterOptions');\r\n    const selectAllCheckbox = $('#projectFilterSelectAll');\r\n    \r\n    console.log('\ud83d\udd0d Debug - Populating project filter');\r\n    console.log('\ud83d\udd0d Debug - userAIProjects:', userAIProjects);\r\n    console.log('\ud83d\udd0d Debug - userAIProjects length:', userAIProjects ? userAIProjects.length : 0);\r\n    \r\n    if (!filterButton || !filterDropdown || !filterOptions) {\r\n      console.error('\u274c Project filter elements not found!');\r\n      return;\r\n    }\r\n    \r\n    if (!userAIProjects || userAIProjects.length === 0) {\r\n      console.warn('\u26a0\ufe0f No user AI projects found');\r\n      filterButton.textContent = 'No Projects Available';\r\n      return;\r\n    }\r\n    \r\n    // By default, select all projects\r\n    selectedProjects = [...userAIProjects];\r\n    updateFilterButtonText();\r\n    \r\n    // Clear existing options\r\n    filterOptions.innerHTML = '';\r\n    \r\n    // Get project counts\r\n    const projectCounts = getProjectCounts();\r\n    \r\n    // Update total count for \"Select All\"\r\n    const selectAllCountBadge = $('#selectAllCountBadge');\r\n    if (selectAllCountBadge) {\r\n      const totalCount = getTotalAgentCount();\r\n      selectAllCountBadge.textContent = totalCount;\r\n    }\r\n    \r\n    // Add project checkboxes\r\n    userAIProjects.forEach(project => {\r\n      if (project) { // Only add non-empty project names\r\n        const item = document.createElement('div');\r\n        item.className = 'project-filter-item';\r\n        \r\n        const checkbox = document.createElement('input');\r\n        checkbox.type = 'checkbox';\r\n        checkbox.id = `projectFilter_${project}`;\r\n        checkbox.value = project;\r\n        checkbox.checked = true; // All selected by default\r\n        checkbox.addEventListener('change', handleProjectFilterChange);\r\n        \r\n        const label = document.createElement('label');\r\n        label.htmlFor = checkbox.id;\r\n        \r\n        const projectName = document.createTextNode(project);\r\n        label.appendChild(projectName);\r\n        \r\n        const countBadge = document.createElement('span');\r\n        countBadge.className = 'project-count-badge';\r\n        countBadge.textContent = projectCounts[project] || 0;\r\n        label.appendChild(countBadge);\r\n        \r\n        item.appendChild(checkbox);\r\n        item.appendChild(label);\r\n        filterOptions.appendChild(item);\r\n        \r\n        console.log('\u2705 Added project option:', project, 'with count:', projectCounts[project] || 0);\r\n      }\r\n    });\r\n    \r\n    // Select All checkbox handler\r\n    selectAllCheckbox.addEventListener('change', (e) => {\r\n      const isChecked = e.target.checked;\r\n      const checkboxes = filterOptions.querySelectorAll('input[type=\"checkbox\"]');\r\n      checkboxes.forEach(cb => {\r\n        cb.checked = isChecked;\r\n      });\r\n      \r\n      if (isChecked) {\r\n        selectedProjects = [...userAIProjects];\r\n      } else {\r\n        selectedProjects = [];\r\n      }\r\n      updateFilterButtonText();\r\n      renderAll();\r\n    });\r\n    \r\n    // Toggle dropdown\r\n    filterButton.addEventListener('click', (e) => {\r\n      e.stopPropagation();\r\n      const isOpen = filterDropdown.classList.contains('show');\r\n      if (isOpen) {\r\n        filterDropdown.classList.remove('show');\r\n        filterButton.classList.remove('open');\r\n      } else {\r\n        filterDropdown.classList.add('show');\r\n        filterButton.classList.add('open');\r\n      }\r\n    });\r\n    \r\n    // Close dropdown when clicking outside\r\n    document.addEventListener('click', (e) => {\r\n      if (!filterButton.contains(e.target) && !filterDropdown.contains(e.target)) {\r\n        filterDropdown.classList.remove('show');\r\n        filterButton.classList.remove('open');\r\n      }\r\n    });\r\n    \r\n    console.log('\u2705 Project filter populated with', userAIProjects.length, 'projects');\r\n  }\r\n  \r\n  function handleProjectFilterChange(e) {\r\n    const project = e.target.value;\r\n    const isChecked = e.target.checked;\r\n    \r\n    if (isChecked) {\r\n      if (!selectedProjects.includes(project)) {\r\n        selectedProjects.push(project);\r\n      }\r\n    } else {\r\n      selectedProjects = selectedProjects.filter(p => p !== project);\r\n    }\r\n    \r\n    // Update Select All checkbox\r\n    const selectAllCheckbox = $('#projectFilterSelectAll');\r\n    const allCheckboxes = $('#projectFilterOptions').querySelectorAll('input[type=\"checkbox\"]');\r\n    const allChecked = Array.from(allCheckboxes).every(cb => cb.checked);\r\n    selectAllCheckbox.checked = allChecked;\r\n    \r\n    updateFilterButtonText();\r\n    renderAll();\r\n  }\r\n  \r\n  function updateFilterButtonText() {\r\n    const filterButton = $('#projectFilterButton');\r\n    if (!filterButton) return;\r\n    \r\n    if (selectedProjects.length === 0) {\r\n      filterButton.textContent = 'No Projects Selected';\r\n    } else if (selectedProjects.length === userAIProjects.length) {\r\n      filterButton.textContent = 'All Projects';\r\n    } else if (selectedProjects.length === 1) {\r\n      filterButton.textContent = selectedProjects[0];\r\n    } else {\r\n      filterButton.textContent = `${selectedProjects.length} Projects Selected`;\r\n    }\r\n  }\r\n  \r\n  // Populate filter when DOM is ready\r\n  if (document.readyState === 'loading') {\r\n    document.addEventListener('DOMContentLoaded', populateProjectFilter);\r\n  } else {\r\n    // DOM is already ready\r\n    populateProjectFilter();\r\n  }\r\n  \r\nlet currentImageIndex = 0;\r\nlet currentImages = [];\r\n\r\nfunction openImageModal(src, name) {\r\n  const modal = document.getElementById(\"imgModal\");\r\n  const modalImg = document.getElementById(\"imgModalContent\");\r\n  const caption = document.getElementById(\"imgModalCaption\");\r\n  const closeBtn = document.getElementById(\"imgModalClose\");\r\n  const prevBtn = document.getElementById(\"imgModalPrev\");\r\n  const nextBtn = document.getElementById(\"imgModalNext\");\r\n\r\n  // collect all visible images in current gallery\r\n  currentImages = Array.from(document.querySelectorAll(\".image-gallery img\"));\r\n  currentImageIndex = currentImages.findIndex(img => img.src === src);\r\n\r\n  modal.style.display = \"block\";\r\n  modalImg.src = src;\r\n  caption.textContent = name || '';\r\n\r\n  updateArrowState(); // \u2705 disable arrows if needed\r\n\r\n  closeBtn.onclick = () => closeImageModal();\r\n  prevBtn.onclick = showPrevImage;\r\n  nextBtn.onclick = showNextImage;\r\n\r\n  modal.onclick = e => {\r\n    if (e.target === modal) closeImageModal();\r\n  };\r\n}\r\n\r\nfunction showPrevImage() {\r\n  if (currentImageIndex > 0) {\r\n    currentImageIndex--;\r\n    const img = currentImages[currentImageIndex];\r\n    openImageModal(img.src, img.alt);\r\n  }\r\n}\r\n\r\nfunction showNextImage() {\r\n  if (currentImageIndex < currentImages.length - 1) {\r\n    currentImageIndex++;\r\n    const img = currentImages[currentImageIndex];\r\n    openImageModal(img.src, img.alt);\r\n  }\r\n}\r\n\r\nfunction updateArrowState() {\r\n  const prevBtn = document.getElementById(\"imgModalPrev\");\r\n  const nextBtn = document.getElementById(\"imgModalNext\");\r\n\r\n  prevBtn.style.opacity = currentImageIndex === 0 ? \"0.3\" : \"1\";\r\n  prevBtn.style.pointerEvents = currentImageIndex === 0 ? \"none\" : \"auto\";\r\n\r\n  nextBtn.style.opacity = currentImageIndex === currentImages.length - 1 ? \"0.3\" : \"1\";\r\n  nextBtn.style.pointerEvents = currentImageIndex === currentImages.length - 1 ? \"none\" : \"auto\";\r\n}\r\n\r\nfunction closeImageModal() {\r\n  const modal = document.getElementById(\"imgModal\");\r\n  modal.style.display = \"none\";\r\n  document.getElementById(\"imgModalContent\").src = '';\r\n}\r\n\r\n\r\n\r\nasync function openDocModal(url, name, type) {\r\n  const modal = document.getElementById(\"docModal\");\r\n  const frame = document.getElementById(\"docModalFrame\");\r\n  const caption = document.getElementById(\"docModalCaption\");\r\n  const closeBtn = document.getElementById(\"docModalClose\");\r\n  const textEl = document.getElementById(\"docModalText\");\r\n  const markdownEl = document.getElementById(\"docModalMarkdown\");\r\n  const wrapper = document.getElementById(\"docModalContentWrapper\");\r\n\r\n  // Reset views\r\n  frame.style.display = \"none\";\r\n  wrapper.style.display = \"none\";\r\n  textEl.textContent = '';\r\n  markdownEl.innerHTML = '';\r\n\r\n  modal.style.display = \"block\";\r\n  caption.textContent = name + (type ? ` (${type.toUpperCase()})` : '');\r\n\r\n  try {\r\n    // Fetch the file as blob first (handles .data files)\r\n    const response = await fetch(url);\r\n    if (!response.ok) {\r\n      throw new Error(`Failed to fetch document: ${response.status}`);\r\n    }\r\n    \r\n    const blob = await response.blob();\r\n    \r\n    // Determine content type from file type or blob\r\n    // Map file type to MIME type (important for .data files)\r\n    const mimeTypes = {\r\n      'pdf': 'application/pdf',\r\n      'txt': 'text/plain',\r\n      'md': 'text/markdown',\r\n      'html': 'text/html',\r\n      'json': 'application/json',\r\n      'xml': 'application/xml',\r\n      'csv': 'text/csv',\r\n      'doc': 'application/msword',\r\n      'docx': 'application/vnd.openxmlformats-officedocument.wordprocessingml.document'\r\n    };\r\n    \r\n    let contentType = blob.type;\r\n    if (!contentType || contentType === 'application/octet-stream') {\r\n      contentType = mimeTypes[type] || 'application/octet-stream';\r\n    }\r\n    \r\n    // Create a new blob with correct MIME type if needed (important for PDFs from .data files)\r\n    const typedBlob = contentType !== blob.type ? new Blob([blob], { type: contentType }) : blob;\r\n    \r\n    // Create blob URL for display\r\n    const blobUrl = URL.createObjectURL(typedBlob);\r\n    \r\n    if (type === 'pdf') {\r\n      // Show PDF in iframe using blob URL\r\n      frame.src = blobUrl;\r\n      frame.style.display = \"block\";\r\n    } \r\n    else if (type === 'txt' || type === 'md' || type === 'html' || type === 'json' || type === 'xml' || type === 'csv') {\r\n      // Fetch and render text-based files\r\n      const text = await blob.text();\r\n      wrapper.style.display = \"block\";\r\n      if (type === 'md') {\r\n        // render markdown \u2192 basic HTML formatting\r\n        markdownEl.innerHTML = renderMarkdown(text);\r\n        markdownEl.style.display = \"block\";\r\n        textEl.style.display = \"none\";\r\n      } else {\r\n        textEl.textContent = text;\r\n        textEl.style.display = \"block\";\r\n        markdownEl.style.display = \"none\";\r\n      }\r\n    } \r\n    else {\r\n      // For other file types, try to display in iframe\r\n      frame.src = blobUrl;\r\n      frame.style.display = \"block\";\r\n    }\r\n    \r\n    // Clean up blob URL when modal closes\r\n    closeBtn.onclick = function() {\r\n      modal.style.display = \"none\";\r\n      frame.src = '';\r\n      URL.revokeObjectURL(blobUrl);\r\n    };\r\n\r\n    modal.onclick = function(e) {\r\n      if (e.target === modal) {\r\n        modal.style.display = \"none\";\r\n        frame.src = '';\r\n        URL.revokeObjectURL(blobUrl);\r\n      }\r\n    };\r\n  } catch (err) {\r\n    console.error('Error loading document:', err);\r\n    wrapper.style.display = \"block\";\r\n    textEl.textContent = 'Error loading document: ' + err.message;\r\n    textEl.style.display = \"block\";\r\n    \r\n    closeBtn.onclick = function() {\r\n      modal.style.display = \"none\";\r\n      frame.src = '';\r\n    };\r\n\r\n    modal.onclick = function(e) {\r\n      if (e.target === modal) {\r\n        modal.style.display = \"none\";\r\n        frame.src = '';\r\n      }\r\n    };\r\n  }\r\n}\r\n\r\n// simple markdown renderer (headings, bold, italics, links, lists)\r\nfunction renderMarkdown(md) {\r\n  let html = md\r\n    .replace(/^### (.*$)/gim, '<h3>$1</h3>')\r\n    .replace(/^## (.*$)/gim, '<h2>$1</h2>')\r\n    .replace(/^# (.*$)/gim, '<h1>$1</h1>')\r\n    .replace(/\\*\\*(.*?)\\*\\*/gim, '<b>$1</b>')\r\n    .replace(/\\*(.*?)\\*/gim, '<i>$1</i>')\r\n    .replace(/\\[(.*?)\\]\\((.*?)\\)/gim, '<a href=\"$2\" target=\"_blank\">$1</a>')\r\n    .replace(/^\\s*[-*] (.*$)/gim, '<ul><li>$1</li></ul>')\r\n    .replace(/\\n$/gim, '<br />');\r\n  \r\n  // Merge adjacent <ul> tags (for multiple list items)\r\n  html = html.replace(/<\\/ul>\\s*<ul>/g, '');\r\n  return html.trim();\r\n}\r\n\r\nfunction openVideoModal(url, title) {\r\n  const modal = document.getElementById(\"videoModal\");\r\n  const player = document.getElementById(\"videoModalPlayer\");\r\n  const caption = document.getElementById(\"videoModalCaption\");\r\n  const closeBtn = document.getElementById(\"videoModalClose\");\r\n\r\n  if (!modal || !player) {\r\n    console.error('Video modal elements not found');\r\n    return;\r\n  }\r\n\r\n  console.log('Opening video modal with URL:', url);\r\n\r\n  modal.style.display = \"block\";\r\n  \r\n  // Reset video element completely\r\n  player.pause();\r\n  player.currentTime = 0;\r\n  player.removeAttribute('src');\r\n  \r\n  // Remove existing source elements\r\n  const existingSources = player.querySelectorAll('source');\r\n  existingSources.forEach(src => src.remove());\r\n  \r\n  // Clear any previous error handlers\r\n  player.onerror = null;\r\n  player.onloadstart = null;\r\n  player.onloadedmetadata = null;\r\n  player.oncanplay = null;\r\n  player.onloadeddata = null;\r\n  \r\n  // Try to detect video type from URL extension\r\n  let videoType = 'video/mp4'; // default\r\n  const urlLower = url.toLowerCase();\r\n  if (urlLower.includes('.webm')) {\r\n    videoType = 'video/webm';\r\n  } else if (urlLower.includes('.ogg') || urlLower.includes('.ogv')) {\r\n    videoType = 'video/ogg';\r\n  } else if (urlLower.includes('.mov')) {\r\n    videoType = 'video/quicktime';\r\n  } else if (urlLower.includes('.avi')) {\r\n    videoType = 'video/x-msvideo';\r\n  } else if (urlLower.includes('.mkv')) {\r\n    videoType = 'video/x-matroska';\r\n  }\r\n  \r\n  // Set up error handling FIRST before loading\r\n  player.onerror = function(e) {\r\n    console.error('Video playback error:', e, player.error);\r\n    let errorMsg = 'Unable to load video';\r\n    if (player.error) {\r\n      switch(player.error.code) {\r\n        case player.error.MEDIA_ERR_ABORTED:\r\n          errorMsg = 'Video loading aborted';\r\n          break;\r\n        case player.error.MEDIA_ERR_NETWORK:\r\n          errorMsg = 'Network error loading video';\r\n          break;\r\n        case player.error.MEDIA_ERR_DECODE:\r\n          errorMsg = 'Video decoding error';\r\n          break;\r\n        case player.error.MEDIA_ERR_SRC_NOT_SUPPORTED:\r\n          errorMsg = 'Video format not supported';\r\n          break;\r\n        default:\r\n          errorMsg = `Error ${player.error.code}: ${player.error.message || 'Unknown error'}`;\r\n      }\r\n    }\r\n    caption.textContent = title + ' - ' + errorMsg;\r\n    caption.style.color = '#ff4444';\r\n  };\r\n  \r\n  // Try setting src directly first (simpler approach)\r\n  try {\r\n  player.src = url;\r\n    player.load();\r\n  } catch (error) {\r\n    console.error('Error setting video source:', error);\r\n    // Fallback: use source element\r\n    const sourceElement = document.createElement('source');\r\n    sourceElement.src = url;\r\n    sourceElement.type = videoType;\r\n    player.appendChild(sourceElement);\r\n    player.load();\r\n  }\r\n  \r\n  // Reset caption\r\n  caption.textContent = title;\r\n  caption.style.color = '#fff';\r\n  \r\n  // Handle load start\r\n  player.onloadstart = function() {\r\n    console.log('Video loading started');\r\n    caption.textContent = title + ' - Loading...';\r\n    caption.style.color = '#fff';\r\n  };\r\n  \r\n  // Handle loaded metadata\r\n  player.onloadedmetadata = function() {\r\n    console.log('Video metadata loaded, duration:', player.duration);\r\n    caption.textContent = title;\r\n    caption.style.color = '#fff';\r\n  };\r\n  \r\n  // Handle canplay event and auto-play\r\n  player.oncanplay = function() {\r\n    console.log('Video can play');\r\n    caption.textContent = title;\r\n    caption.style.color = '#fff';\r\n    // Try to auto-play the video\r\n    const playPromise = player.play();\r\n    if (playPromise !== undefined) {\r\n      playPromise.catch(error => {\r\n        console.log('Auto-play was prevented:', error);\r\n        // Browser prevented autoplay, user will need to click play button\r\n        caption.textContent = title + ' - Click play to start';\r\n      });\r\n    }\r\n  };\r\n  \r\n  // Handle loaded data\r\n  player.onloadeddata = function() {\r\n    console.log('Video data loaded');\r\n    // Try to auto-play if canplay didn't trigger it\r\n    if (player.paused) {\r\n      const playPromise = player.play();\r\n      if (playPromise !== undefined) {\r\n        playPromise.catch(error => {\r\n          console.log('Auto-play on loadeddata was prevented:', error);\r\n        });\r\n      }\r\n    }\r\n  };\r\n\r\n  closeBtn.onclick = function() {\r\n    modal.style.display = \"none\";\r\n    player.pause();\r\n    player.currentTime = 0;\r\n    // Clear sources\r\n    const sources = player.querySelectorAll('source');\r\n    sources.forEach(src => src.remove());\r\n    player.src = '';\r\n  };\r\n\r\n  modal.onclick = function(e) {\r\n    if (e.target === modal || e.target === closeBtn) {\r\n      modal.style.display = \"none\";\r\n      player.pause();\r\n      player.currentTime = 0;\r\n      // Clear sources\r\n      const sources = player.querySelectorAll('source');\r\n      sources.forEach(src => src.remove());\r\n      player.src = '';\r\n    }\r\n  };\r\n  \r\n  // Prevent video clicks from closing modal\r\n  player.onclick = function(e) {\r\n    e.stopPropagation();\r\n  };\r\n}\r\n\r\ndocument.addEventListener('keydown', e => {\r\n  const modal = document.getElementById('imgModal');\r\n  if (modal.style.display !== 'block') return;\r\n  if (e.key === 'ArrowLeft') showPrevImage();\r\n  if (e.key === 'ArrowRight') showNextImage();\r\n  if (e.key === 'Escape') closeImageModal();\r\n});\r\n\r\n  loadData();\r\n</script>\r\n<!-- === Image Modal (Enhanced with Navigation + Keyboard) === -->\r\n<div id=\"imgModal\" style=\"display:none;\">\r\n  <span id=\"imgModalClose\">&times;</span>\r\n  <span id=\"imgModalPrev\" class=\"nav-arrow\">&#10094;</span>\r\n  <span id=\"imgModalNext\" class=\"nav-arrow\">&#10095;</span>\r\n  <img id=\"imgModalContent\" src=\"\">\r\n  <div id=\"imgModalCaption\"></div>\r\n</div>\r\n\r\n<style>\r\n  #imgModal {\r\n    display:none;\r\n    position:fixed;\r\n    z-index:9999;\r\n    left:0; top:0;\r\n    width:100%; height:100%;\r\n    background-color:rgba(0,0,0,0.95);\r\n    overflow:hidden;\r\n  }\r\n  #imgModalContent {\r\n    margin:auto;\r\n    display:block;\r\n    max-width:90%;\r\n    max-height:85%;\r\n    border-radius:10px;\r\n    margin-top:3%;\r\n    box-shadow:0 0 20px rgba(0,0,0,0.8);\r\n  }\r\n  #imgModalClose {\r\n    position:absolute;\r\n    top:20px; right:35px;\r\n    color:white;\r\n    font-size:35px;\r\n    font-weight:bold;\r\n    cursor:pointer;\r\n  }\r\n  .nav-arrow {\r\n    position:absolute;\r\n    top:50%;\r\n    color:white;\r\n    font-size:48px;\r\n    cursor:pointer;\r\n    padding:16px;\r\n    user-select:none;\r\n    transform:translateY(-50%);\r\n  }\r\n  #imgModalPrev { left:20px; }\r\n  #imgModalNext { right:20px; }\r\n  #imgModalCaption {\r\n    text-align:center;\r\n    color:#ccc;\r\n    font-size:18px;\r\n    margin-top:12px;\r\n  }\r\n</style>\r\n\r\n\r\n<!-- === Document Modal === -->\r\n<div id=\"docModal\" style=\"display:none;position:fixed;z-index:9999;left:0;top:0;width:100%;height:100%;background-color:rgba(0,0,0,0.9);overflow:auto;\">\r\n  <span id=\"docModalClose\" style=\"position:absolute;top:15px;right:35px;color:#fff;font-size:35px;font-weight:bold;cursor:pointer;\">&times;</span>\r\n  <div style=\"margin:60px auto;padding:20px;max-width:90%;background:#fff;border-radius:10px;\">\r\n    <div id=\"docModalContentWrapper\" style=\"width:100%;height:80vh;overflow:auto;display:none;\">\r\n    <pre id=\"docModalText\" style=\"white-space:pre-wrap;word-break:break-word;padding:20px;font-family:monospace;font-size:15px;color:#111;\"></pre>\r\n    <div id=\"docModalMarkdown\" style=\"padding:20px;font-family:system-ui;font-size:16px;line-height:1.6;color:#111;\"></div>\r\n  </div>\r\n  <iframe id=\"docModalFrame\" src=\"\" style=\"width:100%;height:80vh;border:none;border-radius:8px;display:none;\"></iframe>\r\n    <div id=\"docModalCaption\" style=\"text-align:center;margin-top:10px;font-size:18px;color:#111;\"></div>\r\n  </div>\r\n</div>\r\n\r\n<!-- === Video Modal === -->\r\n<div id=\"videoModal\" style=\"display:none;position:fixed;z-index:9999;left:0;top:0;width:100%;height:100%;background-color:rgba(0,0,0,0.9);overflow:auto;\">\r\n  <span id=\"videoModalClose\" style=\"position:absolute;top:15px;right:35px;color:#fff;font-size:35px;font-weight:bold;cursor:pointer;\">&times;</span>\r\n  <div style=\"margin:60px auto;padding:20px;max-width:90%;text-align:center;\">\r\n    <video id=\"videoModalPlayer\" controls preload=\"metadata\" playsinline style=\"width:100%;max-height:80vh;border-radius:8px;box-shadow:0 0 15px #000;\">\r\n      Your browser does not support HTML5 video.\r\n    </video>\r\n    <div id=\"videoModalCaption\" style=\"text-align:center;margin-top:10px;font-size:18px;color:#fff;\"></div>\r\n  </div>\r\n</div>\r\n\r\n</body></html>\r\n",
      "additionalFields": {
        "valueType": "",
        "description": ""
      }
    }
  ],
  "storyboards": [
    {
      "name": "AI Agent Catalog",
      "value": "{\n    \"stage\": {\n        \"size\": {\n            \"width\": 1920,\n            \"height\": 1080\n        },\n        \"background\": {\n            \"fill\": \"#161B21\",\n            \"src\": null,\n            \"gradient\": null\n        },\n        \"theme\": null,\n        \"autoFitByDefault\": true,\n        \"autoFitMinWidth\": 480\n    },\n    \"widgets\": [\n        {\n            \"widget_id\": \"agentcatalog\",\n            \"layoutConfig\": {\n                \"rotation\": 0,\n                \"width\": 1918.54,\n                \"height\": 1073.85,\n                \"scaleX\": 1,\n                \"scaleY\": 1,\n                \"fill\": \"#fafafa33\",\n                \"opacity\": 1,\n                \"name\": \"widget-0\",\n                \"draggable\": true,\n                \"x\": -1.456923076922906,\n                \"y\": 2.303076923076945,\n                \"id\": \"cfx-f5bd852a-d4ed-4019-fb88-319e15c25fc3\",\n                \"skewX\": 0,\n                \"skewY\": 0,\n                \"offsetX\": 0,\n                \"offsetY\": 0\n            }\n        }\n    ],\n    \"shapes\": [],\n    \"lines\": [],\n    \"sbBookmarkList\": []\n}"
    }
  ],
  "menu_folder": "Top Level",
  "pstream_dashboards": [
    {
      "title": "LLMs",
      "show_filter": true,
      "widgets": [
        {
          "title": "Agent Catalog2",
          "widget_type": "persistent_custom_widget",
          "widget_implementation": "ai_agents_Catalog_rd1/agent_catalog_minio_with_proj",
          "min_width": 12,
          "max_width": 12,
          "height": 10,
          "widget_id": "agent_catalog_minio_with_proj"
        }
      ]
    }
  ]
}